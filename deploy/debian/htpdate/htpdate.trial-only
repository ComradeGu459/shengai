# qimao-terms-cloud 一天试用专用 HTTP 时间共识配置。
#
# 该配置不是可信生产 NTP，不得用于公网 TLS、生产 JWT/Access、真实云 Secret
# 或任何厂商/付费 API。服务器到期前必须停止 htpdate，并恢复原
# systemd-timesyncd enabled 配置。
#
# 注意：htpdate 1.3.7 每次发生校正后会固定额外休眠 30 分钟；因此即使
# -m/-M 均为 300，该 daemon 配置也不能证明“每 5 分钟校验”。在规划明确
# 批准新的单一路径调度方式前，不得启动此配置。
HTP_DAEMON="yes"
HTP_IFUP="no"
HTP_PROXY=""
HTP_SERVERS="www.debian.org www.cloudflare.com www.microsoft.com www.aliyun.com www.qq.com www.baidu.com"
HTP_OPTIONS="-D -a -l -d -4 -n -m 300 -M 300"
