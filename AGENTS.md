# 审改工作台云端版开发约定

本文件适用于本仓库全部目录。用户的明确要求优先。

## 当前里程碑

- 里程碑 1 的工程骨架和本地 Git 基线已经完成；里程碑 2 的 `DEV-M2-01`、`DEV-M2-02`、`DEV-M2-03` 已经规划独立复验通过。
- 当前已落地技术栈、本地 PostgreSQL、项目中心、整剧素材元数据清单、内存 Storage fake 分片协议、正式上传队列和项目回收站；M2 当前纵向功能切片已经关闭，真实 R2、容量演练与孤儿核对仍后置。
- 当前界面正式验收只覆盖常见电脑桌面与笔记本屏幕；移动端和窄屏不属于支持范围，现有降级样式不构成兼容承诺。
- 里程碑 2 只实现项目中心、大文件上传生命周期和项目回收站，范围以 `docs/milestones/M2-upload-lifecycle.md` 为准。
- 用户已确认下一阶段以最晚 2026-08-27 的内部可用工作台为目标并允许更早交付；术语、ASR 与前置审改进入规划范围。真实付费 ASR 调用、供应商凭据、云资源、账号权限和旧数据迁移仍须分别满足授权门。
- 用户已在 S3 关闭后把画面字 OCR 提升为下一主线并确认 S4 产品规则；先实现供应商无关、零网络的画面字完整业务闭环，再进入字幕验收、单剧联调和交付产品库。真实 OCR API、密钥、费用、服务器和管理员控制仍须分别过授权门。
- 长期产品已确认前置审改、视频字幕验收、双源字幕采纳和后台学习库边界；复杂内容及人物画面风险暂不作为近期主流程或交付硬门禁。这些长期方向都不得扩大当前 M2 开发范围。
- 进入里程碑 2 业务开发前，必须先完成 `docs/status/CURRENT.md` 中列出的技术选型门禁并建立可回退的里程碑 1 Git 基线。
- 每个纵向切片达到验收标准后停止，不顺带扩大到后续工作流；内部可用版按可工作增量尽早交付，不为凑满两周等待。

## 四角色协作规则

- 本项目固定四类对话角色：规划对话、UI 设计对话、后端开发对话、前端开发对话。职责、文件所有权、任务颗粒度和交接协议以 `docs/WORKFLOW.md` v4.4 为准。
- 四个角色使用同一个 `qimao-terms-cloud` 仓库和同一工作目录；当前 M2 存在未提交主线实现，未经规划和用户批准不得为角色协作复制项目、拆仓库或另建隔离 worktree。
- 每个对话开始工作前必须先读 `docs/status/CURRENT.md`，确认当前里程碑、自己的任务、输入和停止条件；不得只依赖对话记忆。
- 只处理 `CURRENT.md` 中 `Current actor` 指向本角色的任务：`ready` 可领取实现，`review` 可执行验收，`blocked` 只处理明确写给本角色的解阻事项。任务属于其他角色时，不代替对方扩大实现。
- 完成工作后必须更新 `docs/logs/DEVELOPMENT_LOG.md` 的真实结果，并把任务转为 `review / Current actor=规划对话`；只通知规划审核，不得直接交给 Reviewer、下一 Owner 或用户开始动作。
- 作出领域验收结论的 UI、前端或后端对话必须在回复用户前完成证据写回，并把 `Current actor` 交回规划对话；不得自行标记 `done`、退回 `ready`、解锁下一任务或直接通知其他执行角色。规划审核后统一完成状态变化和通知。
- 每次修改看板前重读 `CURRENT.md` 的全局同步版本和任务 `Rev`；每次有效写回递增同步版本，只有工作包内容、验收、边界或返工轮次变化才递增任务 `Rev`，避免无意义版本膨胀。对话回复中的计划或结论不能替代本地写回。
- `CURRENT.md` 是当前进度唯一来源；`PRODUCT.md`、里程碑文档、契约、`DESIGN.md` 分别拥有自己的长期规则，不在开发日志中复制全文。
- 外部购买、Git 提交或推送、部署发布仍需用户明确授权，角色分派不能替代授权。
- 用户已长期授权本项目范围内的本地文件编辑、状态与日志写回、非破坏性测试、规划审核、固定对话自动唤醒和送达确认；这些动作不得逐次要求用户批准或转述。该授权不包含购买、真实付费 API、云资源创建、提交/推送、部署、外部发送或破坏性操作。若某一轮工具因临时只读或审批服务故障被系统拦截，应如实记录技术原因；权限恢复后按权威 `CURRENT` 自动续接，不得把临时工具故障解释为用户撤销授权。
- 每次规划派发后，目标固定对话除确认 `inProgress` 外，必须明确回执当前工作区根包含 `C:\Users\ComradeGu\Documents\七猫兼职`，且仓库路径处于 `workspace-write` / `:workspace` 可写范围。普通仓库内编辑必须直接使用工作区写入能力，不得触发 `auto_review`；只有越界、网络、外部副作用或保留授权门的动作才申请审批。
- 目标对话若为 `read-only`、工作区根不匹配或普通仓库写入异常触发审批，必须立即停止写入并回报 `permission_blocked`。规划负责把任务标记为权限阻塞、修正或重新派发并确认新环境可写；不得让用户事后复制消息或把权限故障误记为业务失败。
- 规划角色提出新的范围、优先级、技术路径、成本档位、文件规则、模块对接或里程碑切片时，必须先向用户提供具体问题、方案和取舍并进入用户评审；不得在首次提出方案的同一轮直接标记规划完成或解锁开发。
- UI 设计对话不修改 `frontend/src/`；后端开发不修改 `frontend/`；前端开发不修改 `backend/`、迁移或 `packages/contracts/`。共享契约由规划确定语义、后端唯一实现、前端只消费；接口不足必须通过看板退回，不得复制字段或增加临时业务状态。
- 历史 `DEV-*` 任务保留原编号；v3 以后新开发任务使用 `BACK-*` 和 `FRONT-*`，禁止再创建含混的全栈任务。
- 所有角色可以并且应该提出有证据的专业异议，但不得自行推翻已确认方案。发现 UI/接口、数据状态、成本、性能、安全或文件所有权冲突时，将受影响任务转为 `blocked`、`Current actor=规划对话`，按 `docs/WORKFLOW.md` 的“专业异议与冲突审查”格式交回；规划负责调和、裁决或与用户共同取舍并一次性同步所有角色。
- 交付、验收、退审或冲突写回成功后，UI、前端、后端只自动通知规划任务；只有规划对话可以通知 Reviewer、原 Owner、下一 `Current actor` 或用户进行下一动作。消息只负责唤醒，接收者仍须重读权威文件。
- 跨角色交接只有同时完成“状态与日志写回、向规划总线或 `CURRENT` 指定接收对话主动唤醒、通过 `read_thread`/`wait_threads` 确认消息已送达且目标进入 `inProgress` 或给出明确回执”才算完成。Owner 负责确认规划已被唤醒；规划负责确认 Reviewer、返工 Owner 或下一角色已被唤醒。发送失败或未唤醒必须立即重试并报告，禁止把只写 `CURRENT` 当作已通知，也禁止要求用户传话。
- 每次交接完成汇报必须分别写明“状态已写回、唤醒已发送、送达/运行态已确认”。三项任一缺失时保持 `notification_pending`，不得宣称交接结束或让 FIFO 静默关闭。
- 所有自动交接都必须进入规划对话的“规划接力收件队列”，按首次有效到达序号排队；规划审核同时最多一个 `active` 交接，但已解锁且文件不重叠的 UI、后端、前端任务可以受控并行，同一纵向切片默认最多三个执行任务。详细规则见 `docs/WORKFLOW.md` 9、12.2。
- Reviewer 应完成整张验收矩阵后一次性交回问题；除 P0 外不在发现第一个缺陷时提前结束。返工只跑定向验证，完整 `pnpm check` 留在切片关闭或内部发布门。
- 用户确认并由规划冻结的 UI 设计是正式前端的权威视觉与交互输入。前端必须对照 `DESIGN.md`、对应 `docs/ui/` 验收记录及其指定最终原型/截图，实现相同的信息结构、层级、核心交互和状态；不得因功能已接通、测试已通过或实现更省事而自行简化、重排或重新设计。
- 规划对话负责 UI→前端的完整统筹：在解锁前端前核对 UI 与真实共享契约，形成明确的页面/状态/视口/参考路径/允许差异交接包；前端交付后先由规划检查映射与证据完整性，再路由 UI 验收正式 React。UI 与前端的异议和差异都必须交规划统一裁决，不要求用户传话，也不允许双方各自采用不同版本。
- 有 UI 的前端任务进入 `review` 前，必须提供正式 React 与权威 UI 的“同状态、同视口”对照证据，以及有意差异清单。功能测试、类型检查和构建不能代替一致性验收；结构、信息层级、主要比例、密度、颜色语义或组件形态存在肉眼明显偏差时不得标记 `done`。
- 一致性不得以伪造业务事实换取：真实集数、媒体可用性、AI/风险能力、请求结果和后端状态必须如实呈现。接口或运行条件确实无法表达已确认设计时，前端应按专业异议流程阻塞并交规划裁决，不得静默删除能力或用常量、假数据和前端推算补齐截图。
- 同一根因且最多涉及 2 个生产/测试文件、不改接口/迁移/业务状态/视觉方向的修正走规划总线“微返工快车道”：只做定向代码审计、专项回归和变化场景复验，不重复完整矩阵或全量截图。
- 带异步查询/命令的前端任务首次送 UI 前必须覆盖加载、失败、请求标识、唯一恢复、重试中、再次失败、恢复成功后的焦点以及关闭后触发点恢复；不能只证明数据恢复。
- 同一根因的原始失败经一次修复后仍在真实复验中失败，必须先由规划做根因/状态时序审查并合并缺口，不再继续第三个零散补丁。
- 每个角色都可以按 `docs/WORKFLOW.md` 12.4 提出协作改进建议；建议只交规划统一试运行和版本化，任何角色不得自行维护另一套协议。
- 规划在连续 2–3 次交接、阶段变化、交付风险出现或两日无简报时，按 12.3 向用户提供一次六项阶段简报；普通交接不逐条打扰用户。

## 只读参考边界

- `C:\Users\ComradeGu\Documents\七猫兼职\apps\short-drama-terms-workbench` 只能只读参考。
- 禁止任何脚本、测试、构建或开发命令写入、移动、删除或重命名该目录内容。
- 禁止复制其中的视频、字幕、术语表、SQLite、缓存、API Key、凭据、输出、打包产物或临时文件。
- 借鉴旧应用时，只记录可验证的业务行为、状态转换和输入输出约束。

## 架构边界

- 采用模块化单体：一个后端代码库、清晰业务模块、单一权威状态源。
- 后端拥有任务状态、审核状态、审计记录和持久化规则；前端状态只用于展示与交互。
- 将来 PostgreSQL 是业务元数据的权威存储；对象存储保存大文件；队列只传任务标识和最小上下文。
- Worker 必须无状态、可横向扩容、可租约恢复，并通过后端定义的任务协议工作。
- 对象存储使用短时授权直传，浏览器不得获得长期云凭据。
- ASR 与其他付费 API 调用必须具备幂等键、可审计状态和明确重试边界；结果未知时不得无限自动重试。
- 没有真实调用点前不增加抽象层或微服务；部署单元可以分离，但业务边界仍属于同一模块化单体。

## 目录职责

- `backend/`：后端入口与业务模块。
- `frontend/`：Web 界面与浏览器端交互。
- `packages/contracts/`：唯一共享代码契约，由后端开发任务按规划语义维护；前端不得复制或私改。
- `tests/`：跨模块、集成和仓库安全测试；不得存放真实业务资料。
- `deploy/`：部署清单、基础设施声明和环境说明。
- `docs/`：项目、架构、接口和运维文档。
- `tools/`：开发与仓库验证工具，不承载业务逻辑。

后续模块优先按业务能力组织，例如项目、素材、字幕、术语、审改、导出、ASR 作业；不要把功能堆入单个全局 `service` 或 `utils` 文件。

生产文件默认所有权：后端及 `tests/backend/` 属于后端开发，前端及 `tests/frontend/` 属于前端开发，`DESIGN.md`/`docs/ui/` 属于 UI 设计，产品/里程碑/文档契约/架构属于规划。根级构建和锁文件只有在看板指定集成责任人时才允许修改。

## 数据与 Git 安全

- 严禁提交视频、音频、字幕、术语数据、批量表格、数据库、数据库转储、API Key、`.env`、凭据、日志、缓存和生成物。
- 原始输入只读放入被忽略的 `inputs/`；处理中间物放入 `work/`；输出放入 `outputs/`。
- 代码、测试夹具和文档不得包含真实客户、项目或生产数据。必要测试只使用手写、最小、匿名的代码内样例。
- 新增任何配置示例前确认其不含真实地址、账户、桶名、密钥或令牌。
- 提交前必须运行 `pnpm check:repo` 和 `git status --short --ignored` 检查边界。
- 未经用户明确要求，不提交、不添加远程、不推送、不部署、不发布。

## 工具与命令

- Node.js：`24.19.0`，由 Volta 和 `.node-version` 固定。
- pnpm：`11.21.0`，由 Volta 和 `packageManager` 固定。
- 不无理由更换包管理器或新增生产依赖。
- 安装：`pnpm install --frozen-lockfile`
- 开发：`pnpm dev`
- 验证：`pnpm check`

修改后运行与风险相称的真实验证；没有本轮命令结果时不得宣称完成或通过。

## 基础设施故障分流

- `503 Service Unavailable` 且错误来自模型网关、自动审查或供应商路由时，先记录脱敏的网关域名、模型、协议、`request_id` 和首次失败时间；同一请求最多做一次受控重试，随后标记 `provider_blocked`，不得把它写成业务缺陷，也不得让 FIFO 继续堆积。
- 普通仓库内编辑不应依赖自动审查模型。若工作区已是 `workspace-write` 且动作未越界，仍被 `auto_review` 拦截，应标记 `approval_route_blocked`，停止重复申请并由规划统一恢复本地直写路径；只有越界、网络、付费、部署、推送或破坏性操作保留审批。
- `CreateProcessAsUserW failed: 5`、PowerShell/执行器无法启动等错误统一标记 `executor_blocked`。它与 API 503、仓库权限和业务代码无关；不重复运行同一命令，不修改业务代码，待宿主执行器恢复后从权威 `CURRENT.md` 自动续接。
- 交接报告必须把 `provider_blocked`、`approval_route_blocked`、`executor_blocked` 与 `permission_blocked` 分开，分别写出状态、影响任务和下一步，不允许用一个笼统的“系统错误”替代根因。
