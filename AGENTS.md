# 审改工作台云端版：代理执行约定

本文件适用于本仓库全部目录。用户最新明确要求优先；详细协作状态机见 `docs/WORKFLOW.md`，当前任务只看 `docs/status/CURRENT.md`。

## 1. 权威来源

按以下顺序解决冲突：

1. 用户当前明确要求；
2. 本文件；
3. `docs/WORKFLOW.md`；
4. `docs/status/CURRENT.md` 的活跃任务与 execution lease；
5. 对应产品、契约、设计、测试或部署文档；
6. 历史日志与归档只用于审计，不参与当前调度。

账号、Provider、模型、推理强度、Codex 重启或工作区变化不会修改业务事实。发生这些变化时只重新做权限与消息通道探针；模型和推理强度始终由用户在界面中控制，任何任务或协议都不得自行调整。

## 2. 角色与文件所有权

| 角色 | 默认所有权 | 禁止越界 |
| --- | --- | --- |
| 规划 | 产品范围、问题诊断、最优路径、任务拆分、交付审核、跨角色冲突裁决、`CURRENT`、开发日志 | 直接领取常规业务实现或让隐藏执行者代做 |
| UI | `DESIGN.md`、`docs/ui/`、UI 验收 | 修改生产前后端 |
| BACK | `backend/`、`packages/contracts/`、迁移、`tests/backend/` | 修改员工或管理员前端 |
| FRONT | `frontend/`、`system-frontend/`、`tests/frontend/` | 修改后端、迁移或共享契约语义 |
| TEST | 独立测试、隔离数据库、浏览器验收、`docs/testing/` | 修改生产实现或替 Owner 修复 |
| SERVER | `deploy/`、`docs/deployment/`、服务器运行与问题台账 | 修改产品业务实现 |

同一物理文件同一时刻只能属于一个活动 execution lease。工作树存在大量未提交连续实现；不得创建平行仓库、复制源码目录或新 worktree 来规避重叠。

### 规划总管约束

- 规划是项目“大脑”和总管家，默认只做问题分析、方案冻结、拆单派发、证据审核、返工裁决、角色衔接、冲突消解和最终收口。
- 规划不得为了加速而自行领取 FRONT、BACK、UI、TEST 或 SERVER 的常规实现路径，也不得调用隐藏子代理、内部 successor 或临时工作树替角色施工。
- 领域角色遇到问题时，规划先提供可执行的最优解、验收门和停止条件，再交原 Owner 返工；不能仅因任务较慢、只读或尚未回应就接管实现。
- “角色无法解决”必须有可审计事实：同一根因已完成至少两次有证据的尝试，角色提交正式 `blocked` 差量，且没有其他已登记可写角色能够在职责范围内承接。
- 即使满足上述条件，规划也只先重新诊断和裁决。只有用户明确授权规划直接介入后，才能建立一次性、精确路径的异常租约；不得使用隐藏代理，完成后必须交回原领域角色独立复核。

## 3. 执行租约

任何写入前必须取得包含以下字段的 execution lease：

- `task_id` / `rev` / `handoff_token`；
- `role`、`authority_generation`；
- `repo_root`；
- `permission_profile=workspace-write/:workspace`；
- `ordinary_writes=direct`；
- 精确 `allowed_paths`；
- predecessor、停点、验收和停止条件。

目标任务必须原样回执租约并检查 `git status/diff` 重叠。若为 `read-only`、根目录不匹配或普通仓库写入不可用：零修改，标记 `permission_blocked`，立即交回规划。

项目只使用用户在侧栏可见的六个固定角色任务：规划、UI、BACK、FRONT、TEST、SERVER。禁止用隐藏子代理、内部 successor、临时线程或新 worktree 代替只读角色写业务代码、测试或领域文档。确需替换角色任务时，必须创建一个用户可见、名称明确的新任务，完成权限握手后再由规划更新注册表；旧任务随即冻结，不双写。

## 4. 接力的真实完成条件

文档不会自行启动任务。只有向注册表中的用户可见目标任务发送消息并观察到该任务的新 turn，才表示被唤醒；发送 API 成功、共享文件变化或内部代理消息都不等于送达。

跨角色只使用三个阶段：

1. `artifact_written`：Owner 已完成产物并提供新鲜证据；
2. `planning_received`：规划在新 turn 中审核并写回当前状态；
3. `reviewer_started`：注册表中的可见 Reviewer 回显同一 token、权限通过并开始。

规划任务结束后不会仅凭文档在后台继续调度。需要连续推进时，规划必须在结束前启动下一任务，或由用户/自动化再次唤醒规划。不得把“规则已写入”描述成“接力已自动运行”。

## 5. 状态和文档

- `docs/status/CURRENT.md` 只保留当前目标、活动任务、租约、阻塞和下一跳，目标不超过 180 行。
- 关闭任务和旧同步版本进入 `docs/status/archive/`；完整过程进入 `docs/logs/DEVELOPMENT_LOG.md`。
- `CURRENT` 与开发日志只由规划或规划文档书记写；领域角色只交结构化差量和证据。
- 执行角色不得自行标记 `done`、解锁下一任务或直接通知其他领域角色。
- 领域文档 Owner 缺权限时标记 `permission_blocked` 并交规划，不创建隐藏 docs successor，不把 UI 文档交给 FRONT，也不把产品契约交给 TEST。规划只能修订协作、状态和日志文档，不能代写领域产物。

## 6. 实现与恢复边界

- 每个业务状态只有一个服务端权威来源；前端只投影，不创建第二状态源。
- 创建、发布、回滚、取消、重试、上传和派发使用稳定资源 ID、稳定命令 ID、`Idempotency-Key` 与规范化请求摘要。
- 写请求结果未知时，只查询同一稳定身份；禁止自动重发 POST、扫描列表猜测结果或新建第二事实。
- 新权威路径启用时，同一切片删除旧写契约、旧路由、旧调用和只验证旧行为的测试；不得叠加长期兼容分支。
- 后端数据库测试使用精确隔离临时库并在结束时只清理该库；不得清空或改写共享默认库。
- 原始视频、字幕、术语、凭据、Cookie/Auth、对象键、连接串和供应商载荷不得进入仓库、截图或日志。

## 7. 验证与停止

- 修改规模决定验证强度：定向测试 → 类型检查/构建 → 切片关闭时一次完整 `pnpm check`。
- 完成、通过或部署结论必须来自本轮新鲜证据；计划和代理口述不能冒充事实。
- 同一根因连续两次真实修复失败，停止第三个局部补丁，回到唯一状态源和时序根因审查。
- P0 数据破坏、越权、敏感泄露、重复付费或重复写立即停止；普通 P1/P2 集中完成矩阵后一次性交规划。
- 未经用户明确授权，不购买、不续费、不提交、不推送、不部署、不开放公网、不接真实 Secret、付费 API、COS 或 Sentry，不执行破坏性数据库/服务器动作。

## 8. 项目边界与命令

- 只读参考：`C:\Users\ComradeGu\Documents\七猫兼职\apps\short-drama-terms-workbench`，禁止任何写入或数据复制。
- Node.js `24.19.0`；pnpm `11.21.0`。
- 安装：`pnpm install --frozen-lockfile`；开发：`pnpm dev`；验证：`pnpm check`。
- 使用 `apply_patch` 编辑文件；保留用户和其他任务的未提交改动。
- 禁止在仓库中保存视频、音频、真实字幕、数据库、转储、`.env`、密钥、日志、缓存和生成物。
