# 前端

当前使用 React 19、Vite 8、React Router 8 与 TanStack Query，已实现项目中心的项目列表、搜索、加载、空数据、失败状态和就地创建表单，并提供单项目素材配对页：浏览器只扫描文件元数据，按集自动配对三类素材，允许人工调整并显式确认版本。

前端只管理筛选、表单、本地未确认素材草稿和服务端请求缓存，不保存权威项目状态、已确认清单、长期云凭据或 API Key。文件字节上传、真实对象存储和回收站尚未实现。
