# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是公司内部的海外短剧字幕与术语审改人员。他们在桌面环境中高频处理项目素材、识别任务、候选证据、人工裁决和交付记录。

## Product Purpose

将项目、素材、识别任务、术语候选、人工审改、证据和导出纳入一套可协作、可审计、可恢复的云端流程。成功意味着用户可以清楚判断当前状态、定位失败原因并安全继续工作，同时保持原始输入只读。

## Positioning

继承原本地工作台已验证的业务流程和人工证据边界，用统一的项目身份、任务状态与审计记录把分散操作整合为云端生产流程，而不是重新发明一套通用网页工作流。

## Operating Context

- 以项目为工作单位，素材、识别、审核和交付共享同一项目身份。
- 处理视频、音频、字幕、术语表、截图证据和异步任务。
- 两个原本地应用是云端产品功能等价迁移的只读权威输入，不复制其真实业务数据、凭据、缓存或构建产物，但不得只把它们当作可任意删减的灵感参考。
- 当前优先验证项目中心、上传任务、回收站、全局应用外壳和可扩展的单项目工作台框架。

### 已确认的工作量基线

- 公司内部员工峰值不超过 10 人，当前不以大规模公众访问为目标。
- 同时工作的员工通常约 5 人；系统按 5–6 个项目或上传批次并行设计。
- 当前预计每天处理 10–20 部剧，后续可能因积单而升高，容量参数必须可配置并支持横向扩展。
- 每部剧通常 50 集左右，上限按 100 集规划；每集视频不超过 5 分钟。
- 单项目或单批次素材可能达到 10–30 GB，不假定每一个文件都达到 30 GB。
- 原始视频在交付完成后默认保留 48 小时，再进入 7 天可恢复回收期；正式交付物与原始素材分开管理并长期归档，具体容量阈值可配置。

## Capabilities and Constraints

- 全局工作区与单项目工作台采用两层信息架构。
- PostgreSQL/后端是项目、任务、审核、权限与交付状态的唯一权威来源；浏览器只保存展示和未提交交互状态。
- 大文件通过短时授权直传；任务必须可恢复，付费调用必须幂等并可审计。
- 当前不实现应用内账号、密码或成员后台；员工工作台与系统控制台采用外部邮箱身份入口，应用只消费经验证的访问身份并隐藏未完成入口。
- 里程碑 2 只实现项目创建与列表、大文件分片直传与恢复、完整性校验、回收站、48 小时恢复和后台清理。
- 中文识别、画面字、术语审核、前置审改、视频字幕验收、任务中心和导出交付属于后续兼容范围，不进入里程碑 2。复杂内容风险与人物画面风险保留长期扩展位，但不作为当前主流程或近期交付的硬门禁。
- 后续整剧生产流程必须先从公司 SRT 形成项目术语候选并完成人工确认；已确认术语同时作为中文语音识别和画面字识别的项目级约束。术语未确认时，不允许把 ASR、画面字识别或后续审改标记为可正式开始。
- 术语表格是项目术语数据的导出表现，不是唯一权威来源；候选、人工状态、别称、证据和后续识别所引用的术语版本由后端保存和审计。
- 术语候选只有在全部进入“已确认、已修改、已驳回”之一且员工显式确认后才形成正式版本；允许人工补充遗漏术语。
- 正式术语修改后创建新版本。已完成识别保留原结果并标记所用版本过期，默认由员工只重跑受影响集数，也允许选择重跑整剧；系统不得静默覆盖人工结果或自动产生不可控费用。
- SRT 解析和术语确认可以与大视频上传并行；某集 ASR 或画面字识别只有在项目术语版本已确认且该集所需视频已上传校验后才能开始。
- 首期身份入口已确认采用“两站一后台”：员工工作台和管理员系统控制台使用独立公网域名、独立 Cloudflare Access Application 与不同 audience；员工站只允许显式员工邮箱，控制台只允许部署所有者邮箱。后端必须验证 Access JWT，不能只靠隐藏网址或前端路由。多租户、正式应用账号和未来甲方总库接口仍待确认。
- 员工工作台可以从公网访问，但不得匿名开放真实公司数据、上传、删除、导出或付费任务。无法配置员工邮箱名单时，只能开放匿名 fake 演示环境。
- 两个网站共用同一模块化后端、PostgreSQL、对象存储和任务系统，不为第二个前端复制一套业务数据库或要求第二台服务器；详细边界见 `docs/contracts/SYSTEM_CONTROL_CENTER.md`。

## 已确认的长期生产流程

长期主链路固定为：素材确认 → 术语确认 → ASR / 画面字识别 → 前置审改 → 待验收版本 → 视频字幕验收 → 完整交付确认 → 交付产品库。员工不再单独执行“冻结”或返回本地软件；生成交付时由后端原子固定不可变验收快照与正式产品。两个工作台不得合并成同一个模糊的“风险审查页”。

- **前置审改工作台**继承原“海外短剧审改工作台”的职责：以公司 SRT 和已确认术语为基线，对照 ASR、OCR/画面字候选和证据，进行较大范围的台词修正、画面字确认、候选裁决与术语引用，产出待验收的台词 SRT、画面字 SRT、术语快照和视频映射。
- 公司 SRT 与 ASR 都是只读证据源，不再把公司稿写死为所有剧集的唯一主基准。每集可选择“公司稿优先”或“ASR 优先”，系统依据音频覆盖、术语命中、低置信片段、时间轴完整度和异常长句比例给出推荐；推荐不能自动覆盖人工结果。
- 对照主基准采用“系统默认 → 项目/整剧 → 选中集批量 → 单集覆盖”的层级。新项目继承可配置的系统默认值；高层设置变化只影响尚未人工确认的项目，不能覆盖已经确认的单条采纳稿。初始建议默认公司稿优先，真实样本验证 ASR 达标后可改为 ASR 优先。
- 每条对齐结果必须形成唯一“采纳稿”，允许采用公司稿、采用 ASR、合并两者或人工修改，并记录来源。公司稿、ASR 原文和采纳稿分别保存，只有采纳稿进入后续待验收版本。
- 公司稿与 ASR 按时间范围及相邻字幕组对齐，比较时忽略空格和常规标点，避免把纯标点或分轴差异误报为正文错误。ASR 的原始标点和词级时间戳可以保留为断句证据。
- 最终台词轨不保留逗号、句号、问号、感叹号、分号、省略号等常规中英文标点；停顿使用空格，多人同时说话时只保留每行行首的半角减号 `-`。该规则不作用于画面字轨，画面字仍忠实记录画面内容。
- 自动断句优先使用语音停顿、词级时间戳和语义边界，不得从人名、地名、数字或已确认术语中间切开。单轴目标不超过 18 个有效字，19–28 字提示优化，超过 28 字阻止发布待验收版本；无法可靠切分时转人工确认，不能强行截断。
- **视频字幕验收工作台**继承原“字幕验收工作台”的职责：以待验收字幕和视频为输入，完成最终看片、时间轴与画面贴合检查。首期编辑能力只包括文字、开始/结束时间、拖动字幕、增加和删除字幕，不在此重新运行 ASR、OCR 或制作术语。
- 单个、局部问题允许在视频字幕验收中直接修正；涉及整集或批量的大范围问题必须退回前置审改并生成新的待验收修订，不能在验收端悄悄重做上游。
- 支持逐集通过和整剧一键通过。验收后形成不可变冻结版本；冻结后若字幕、视频或上游修订发生变化，原验收自动失效并要求重新验收。
- 近期验收只设置文件完整性、字幕时间轴重叠/共端点、视频证据缺失等技术门禁。复杂内容风险、人物/人脸画面风险和严重等级阻断待真实业务使用后另行设计。
- 一部剧默认由一名工作人员负责；人员变更时保存转交记录。当前采用工作人员直接确认并生成交付物的方案，未来账号体系可在冻结与正式交付之间插入管理员批准。
- 正式交付只包含台词 SRT、画面字 SRT 和术语 XLSX。成品库采用顶层目录 `剧名-V1`、`剧名-V2`，支持搜索、时间排序、负责人和备注；版本号不再嵌套在剧名目录内。
- 云端播放器只生成一个 360p 流畅代理和一个最高 1080p 的高清版本，不额外生成 720p；默认播放 360p，可切换“高清”或仍在保留期内的“原片”，低分辨率源文件不得被无意义放大。

## 本地双应用功能等价门禁

- 最终成品必须覆盖 `apps/short-drama-terms-workbench`（海外短剧审改/术语工作台）和 `apps/short-drama-subtitle-review`（字幕验收工作台）的基本全部生产能力。云端不是只复刻术语页，也不是另做一套删减流程。
- “等价”以用户目标、输入输出、状态转换、证据、失败恢复和保护规则为准，不要求逐像素照搬 Tk/WPF 界面、Windows 菜单或本地目录操作。可以按第一性原理合并步骤、减少重复操作、改成批量/异步/云端协作，但优化后必须达到或超过原能力。
- 每项原功能只能落入四类：`等价保留`、`优化后等价`、`实现方式替换但结果等价`、`经用户明确确认后置`。禁止静默删除、用“网页不方便”作为缺失理由，或把未实现功能伪装成不再需要。
- 两周内部可用版是分阶段交付检查点，不是最终功能删减清单。暂不阻塞首个闭环的 OCR、完整视频字幕验收、风险词/人脸风险、连续学习、交付管理等能力仍必须留在等价矩阵和后续里程碑中；只有用户明确取消的能力才可永久移出。
- 每个云端切片立项前必须先读取对应本地功能、测试和真实操作说明；交付时在 `docs/contracts/LOCAL_APP_PARITY.md` 写回本地来源、云端实现、行为差异、验证证据和剩余缺口。没有映射或没有验收证据的能力不得宣称“本地应用已经迁移完成”。
- Windows 专属机制必须转换为云端等价安全机制：本地工作副本只读保护对应对象存储原件不可变与版本化；Windows 凭据管理器对应服务器 Secret；本地缓存/恢复对应持久化任务与幂等恢复；本地导出目录对应可追溯的云端成品库和下载。

## 已确认的学习与自动化边界

- 每次人工采纳、修改、合并、断句和时间轴修正都生成后台不可变学习事件，至少记录公司稿、ASR、修改前后文本、采纳来源、项目/集数/字幕轴、时间范围、ASR 引擎与模型、术语版本、质量证据、修改类型和操作者身份（账号启用后）。
- 学习事件独立于项目素材生命周期保存；项目删除或回收不能删除已经归档的学习证据。普通员工可以看到当前字幕的保存、撤销和工作版本，但不显示跨项目学习库入口、聚合规则或其他项目记录。
- 当前没有账号系统时，学习库不提供前端页面或普通业务查询接口，只允许后台写入和受控维护；未来仅向管理员或获授权的维护角色开放管理界面。公司内部应通过使用规范说明存在工作质量与模型优化记录，不进行超出工作数据范围的隐蔽采集。
- 学习分为三级：当前项目的已确认修正可立即成为后续集数的热词和纠错建议；跨项目重复证据只聚合为隐藏候选；候选经过人工批准、真实回归案例和版本化评估后，才能进入公司级热词包、纠错规则或训练数据版本。
- 单次人工修改不得直接触发全局自动替换。任何自动规则都必须可追溯、可停用、可回退，并记录所用学习版本；新版本不能静默改写已经人工确认或冻结的字幕。
- 第一阶段只保存结构化文字、时间范围和质量证据，用于热词、提示词和后处理纠错，不保存音频或视频副本。未来若开展 ASR 微调或模型蒸馏，需要另行确认音频片段授权、脱敏、保留期限、训练范围、评估集和成本。

## 内部可用工作台快速目标

- 用户确认以 2026-08-27 为最晚内部可用日期，但不要求耗满两周；任何达到质量门的可工作增量应立即交付预览和试用。
- 首个内部闭环固定为“素材 → 术语确认 → ASR → 前置审改 → 导出 SRT/XLSX”，具体切片、检查点和非目标见 `docs/milestones/M3-internal-workbench-sprint.md`。
- 速度优化优先来自范围收敛、受控并行、批量审查和减少重复验证，不得削减权威数据源、原始证据只读、付费幂等、失败恢复和真实主流程验收。
- 账号、OCR、字幕验收、交付产品库完整管理、人脸/复杂风险、自建模型和正式公网发布不阻塞首个内部可用版，但除经用户明确取消者外，原本地应用已有的对应生产能力仍属于最终功能等价范围。

## Milestone 2 Success Criteria

- 用户能创建项目并看到由后端返回的真实项目状态。
- 用户能上传多个文件，刷新页面或短暂断网后能够安全继续，不重复生成已完成对象。
- 服务端能校验上传结果，失败时展示可操作的原因，不把未知结果标记为成功。
- 5–6 个匿名测试上传批次可以并行执行，并发数、分片大小和限速不是散落在代码中的常量。
- 项目进入回收站后 48 小时内可以恢复；到期清理可重试、可审计，并能发现数据库与对象存储之间的孤儿数据。
- 所有测试只使用合成、匿名数据，原本地应用和真实业务资料保持只读且不进入仓库。

## Milestone 2 Non-goals

- 不实现账号、组织、角色、多租户和完整权限后台。
- 不实现 ASR、OCR、AI 术语提取、风险审查中心或正式交付。
- 不迁移旧应用数据库、历史项目、素材、缓存、凭据或构建产物。
- 不为未来规模提前拆微服务，也不在里程碑 2 引入付费识别调用。

## Brand Commitments

- 产品名称使用“审改工作台云端版”。
- 员工网站使用“审改工作台云端版”，管理员网站固定显示“系统控制台”；两个网站共享设计语言但不共享导航和高权限入口。
- `DESIGN.md` 是唯一 UI 设计权威来源；其他产品文档不得创建冲突的视觉规范。
- 界面文案使用简体中文，表达直接、功能性强，不使用营销化语言。

## Evidence on Hand

- `DESIGN.md`：已确认的界面框架、信息架构、视觉基线和验收门槛。
- `docs/architecture.md`：领域边界、状态所有权、失败恢复和待确认事项。
- 原本地应用：仅可只读验证业务行为、状态转换和输入输出约束。
- 当前没有可用于正式产品展示的真实客户数据、素材或生产截图，预览内容必须使用匿名示例。

## Product Principles

- 状态清晰优先于装饰表达。
- 一个业务状态只有一个权威来源。
- 人工裁决与证据边界不被 AI、ASR 或 OCR 结果替代。
- 复杂风险能力启用后仍须保留证据来源和人工决定，检测器没有命中不等于内容安全；当前不把该能力提前设为主流程硬门禁。
- ASR 和画面字识别必须记录所使用的已确认术语版本，不能只读取一份会被覆盖的当前表格。
- 异步工作必须可解释、可恢复、可审计。
- 所有模块共享同一应用外壳、导航语言和交互组件。

## Accessibility & Inclusion

支持键盘操作、清晰焦点、非颜色单一编码和可读对比度。正式验收覆盖常见电脑桌面与笔记本屏幕；移动端和窄屏不属于当前支持范围，现有窄屏降级不构成兼容承诺。
