# 审改工作台云端版 UI 系统

状态：既有设计基线，供产品规划与后续开发共同读取。统一应用外壳与项目中心已按本文落地；其余章节仍作为后续开发约束，不代表对应业务功能已经实现。

## 1. 设计目标

- 面向公司员工的高频生产工具，优先保证状态清晰、操作一致、失败可恢复。
- 继承本地工作台已经验证的业务流程，不按网页重新发明一套流程。
- 所有后续模块进入同一个应用外壳、导航系统和组件体系，禁止各模块独立设计。
- 正式验收只覆盖电脑屏幕尺寸下的生产操作；已有窄屏样式仅提供非承诺降级，不作为当前交付门槛。
- 当前不实现账号管理，但在顶栏右侧预留组织、用户和权限入口位置；功能启用前不展示空入口。

## 2. 两层信息架构

云端版采用“全局工作区 + 单项目工作台”两层结构，避免把十几个业务页面平铺在一个侧栏中。

### 2.1 全局工作区

全局侧栏只放跨项目能力：

| 分组 | 模块 | 当前状态 | 职责 |
| --- | --- | --- | --- |
| 项目 | 项目中心 | 当前阶段 | 创建、检索、打开、删除和恢复项目 |
| 项目 | 上传任务 | 当前阶段 | 大文件直传、暂停、续传、失败恢复 |
| 作业 | 项目工作台 | 后续接入 | 打开选中项目的完整生产流程 |
| 作业 | 中文识别任务 | S2.1 Wave B | 只汇总跨项目 ASR 派发与项目子批次，负责刷新恢复与进入单项目详情 |
| 作业 | 任务中心 | 后续接入 | 汇总 ASR、OCR、AI、导出等异步任务 |
| 交付 | 交付产品库 | S7 | 跨项目检索、查看和下载不可变正式产品，展示负责人、备注与创建/更新时间 |
| 资产 | 规则与资产 | 后续接入 | 术语资产、已批准热词与纠错规则的修订和版本；普通员工不展示跨项目原始学习记录 |
| 生命周期 | 回收站 | 当前阶段 | 延迟删除、恢复、到期清理 |

### 2.2 单项目工作台

打开项目后，主内容区切换为项目级二级导航。以下模块共享同一个项目身份和状态，不再各自选择文件夹：

1. 项目概览与素材：项目资料、文件校验、公司 SRT、视频和台词来源；整剧素材以项目上下文中的按集配对台账完成确认。
2. 术语：AI 提取、候选审核、别称、证据、项目术语表和正式导出；确认后的术语版本是识别与审改的前置门禁。
3. 中文识别：ASR 任务、双引擎结果、热词提示、质量门禁和历史批次。
4. 画面字：OCR 候选、截图证据、初始时间轴和人工保留或排除。
5. 前置审改：以公司 SRT、已确认术语、ASR 和画面字证据完成较大范围修正，产出新的待验收修订。
6. 视频字幕验收：结合视频完成最终看片、时间轴检查、轻量字幕修正、逐集通过和整剧交付就绪确认；就绪后进入本项目的完整交付确认页。

复杂内容风险、人物/人脸画面风险和严重等级阻断暂不进入近期项目级导航；模块注册表中保留 `hidden` 扩展位，待真实业务使用后再决定是否建设独立风险审查中心。

项目级模块只在打开项目后出现。全局侧栏不重复罗列这些页面。

以上六个项目级环节只定义长期项目工作台的信息架构，不是当前上传页的标签组。交付产品库属于全局跨项目空间，不在项目二级导航重复建立一套产品列表；项目上下文只提供“前往交付确认”和“在产品库查看本项目”的入口。即使未来全部实现，全局“上传任务”仍只按素材角色、集数、文件类型和上传状态组织，不复制项目工作台流程导航。

## 3. 应用外壳

所有页面必须由同一个 `AppShell` 渲染：

- 左侧全局侧栏：桌面展开宽度约 `204px`，收起宽度约 `72px`；底部提供明确的收起/展开按钮。收起后保留产品标识、模块图标、当前选中态与可访问名称，不保留被截断文字；开合状态只属于浏览器展示偏好，不参与业务判断。
- 顶部栏：高度 `64px`，只展示当前页面标题、服务状态和未来账号区域。
- 主内容区：承载页面工具栏、流程区、数据表或工作台。
- 项目上下文条：进入具体项目后展示项目名称、项目状态和二级流程，不在全局页面出现。
- 全局任务入口：后续通过顶栏入口或任务中心统一查看 ASR、OCR、AI 和导出的异步执行事实，不允许每个模块单独实现任务弹窗；上传会话继续属于独立“上传任务”，人工编辑继续属于对应项目工作台。

### 唯一标题规则

- 当前页面标题只在顶部栏显示一次。
- 内容区不得再次重复同名大标题。
- 侧栏中的导航名称属于定位信息，不作为第二个页面标题。
- 页面说明仅在确实影响操作时出现，普通列表页直接从流程条或工具栏开始。

## 4. UI 控制中心：模块注册表

前端技术栈确认后，应建立唯一模块注册表。侧栏、路由、页面标题、功能开关和权限可见性全部从注册表生成，禁止在多个组件中分别维护。

建议的描述结构：

```ts
type ModuleDescriptor = {
  id: string;
  scope: "global" | "project";
  title: string;
  route: string;
  icon: string;
  group: "project" | "work" | "asset" | "lifecycle" | "system";
  order: number;
  status: "active" | "planned" | "hidden";
  featureFlag?: string;
  permission?: string;
  taskKinds?: string[];
};
```

控制规则：

- `active`：显示并允许进入。
- `planned`：只允许在产品预览中以禁用状态出现；正式环境默认隐藏。
- `hidden`：不进入导航，但可保留路由迁移或实验用途。
- 账号与权限模块当前为 `hidden`，只保留插槽，不显示占位按钮。
- 一个模块启用时，同时获得统一标题、导航高亮、加载态、错误态和面包屑，不另写一套外壳。
- 项目级模块必须先校验当前项目身份；没有项目时统一回到项目中心。

## 5. 页面模板

后续页面从以下模板选择，不重新设计页面骨架：

### 5.1 列表页

用于项目中心、任务中心、规则资产和交付记录。

固定顺序：页面工具栏 → 搜索与筛选 → 数据表 → 分页。主操作放在工具栏右侧，行级操作放在对应数据行。

### 5.2 上传任务页

只用于已确认项目素材的文件直传、暂停、续传、服务端校验和失败恢复。

统一展示项目、清单版本、素材角色、文件、上传状态、进度、失败原因和恢复操作。上传权威状态来自后端，前端不得自行推断完成。

当前整剧素材确认是本模块内的项目级工作区，不提升为全局页面模板。桌面首屏同时保留项目身份与流程、只扫描本地元数据且不上传文件内容的隐私边界、选择文件夹主操作、最新后端清单版本和配对工作区入口。扫描后以横向按集台账配对公司 SRT、ASR 视频和画面字视频，并在台账下方集中列出确认阻断与待分配文件；用户先消除阻断，再显式确认形成新的后端清单版本。

正式上传队列属于全局“上传任务”，只负责把已确认素材清单中的素材上传并恢复失败，不承载术语、中文识别、画面字、前置审改、字幕验收或交付流程；这些后续处理归属任务中心或对应项目工作台。上传页只使用素材类型、集数和上传状态组织队列，不展示后续生产流程标签。

批次身份必须同时显示项目名、清单版本、创建时间和后端 `workflow_status`，避免用户把浏览器本地进度误认为项目已经可处理。顶部摘要只突出待处理与失败数量，其他总量留在列表或筛选结果中。一个上传任务可以一次选择多个素材文件并同时推进多个物理文件；页面展示整批总体进度和每个素材的独立状态与恢复动作，不能退化为单素材上传表单。上传百分比只在整批总体进度中展示；单个物理文件只显示“上传中”等文字状态，不显示逐文件百分比或逐文件进度条。

#### 上传队列结构

- 整剧批次按“待上传的物理文件”聚合，不按角色槽位重复列行。默认主列表保持紧凑，只展示逐行勾选、文件/标题与集数、素材角色、文字加语义色状态、大小、更新时间、唯一主恢复动作和行尾三点菜单；相对路径、逐文件百分比、逐文件进度条和技术事实不常驻主列表。
- 文件/标题、集数、状态、大小和更新时间表头必须提供明确的升降序入口。默认排序先按“失败 → 待处理 → 进行中 → 其他”突出行动优先级，再按集数升序；状态筛选或用户排序后仍保留明确的当前排序反馈。
- 首列支持逐行勾选和当前页全选；未选中时不显示批量操作栏，选中后批量栏就地替代普通工具区并明确选中数量。批量栏采用“已选择 N 项 / 已完成 Asset 不受影响 / 暂停上传 / 继续上传 / 取消未完成上传”的紧凑排列。批量能力只作用于选中的未完成会话，不得把已完成 Asset 混入取消或删除语义。创建任务入口并列提供主按钮“选择素材文件夹”和次按钮“选择多个文件”；选择结果按物理文件拆成并行队列行，单文件失败不得阻塞其他素材继续上传。
- “整剧批次”是项目与清单版本下的只读展示分组，不新增批次业务状态。每个物理文件仍对应一个后端上传会话；批次总进度和批量动作只能由单文件后端事实汇总或逐项执行，局部失败必须逐行回显。
- 同一集的 `asr_video` 与 `screen_video` 指向同一 MP4 时只显示一行，并同时显示“中文识别视频”“画面字视频”两个角色标签和“共享文件 · 上传一次”说明；其他跨集、公司 SRT 或跨类型重复占用仍表现为阻断，不提供上传动作。
- 单文件层始终只直接展示与当前状态对应的一个主恢复动作；低频动作收进行尾三点菜单，不能与主恢复动作竞争。高风险动作只命名为“取消未完成上传”，必须二次确认并说明已完成 Asset 不删除、未完成会话将终止。项目或素材删除等待回收站契约，不得在上传列表伪造通用“删除”。
- 单文件详情在原行渐进展开，默认一次只展开一行，不使用会遮挡队列上下文的模态框。详情展示相对路径、已确认分片、缺失分片、会话有效期、文件指纹核对、最近错误和请求标识；对象键、长期凭据和内部存储标识不向普通员工展示。已完成行降低视觉权重，并允许按完成分组折叠，但仍可筛选和回查。

#### 暂停、刷新与重新选择

- “暂停”只停止当前浏览器发起新分片，不能把服务端会话显示为暂停。界面同时显示“本机已暂停”和服务端真实状态 `created` / `uploading`，并说明会话仍会到期。
- 刷新后先读取后端会话、已确认分片和最新清单，再进入“等待重新选择原文件”。浏览器不得假装仍持有本地文件；用户可重新选择整剧文件夹或单个文件，界面按相对路径、大小、修改时间组成的指纹逐项核对。
- 指纹匹配后只续传服务端缺失分片；同一物理 MP4 的两个视频角色只匹配一次。指纹不符时保留已上传分片和服务端状态，但禁用继续并显示预期文件与本次选择的差异。
- 授权过期优先自动续期，短暂断网恢复后继续缺失分片；会话过期必须明确“重新创建上传任务”，不能复用旧会话或把按钮文案写成普通重试。

#### 状态与错误动作

- 队列必须区分：加载、空任务、等待本地文件、排队、上传中、本机已暂停、断网等待、校验中、已完成、失败可重试、会话过期、指纹不符和项目不可用。
- `verifying` 只显示后端校验过程，不显示可编辑的 100% 假完成；只有服务端返回 `completed` 且已绑定 Asset 后才显示“已完成”。项目是否 `ready` 继续以后端归约结果为准。
- 错误文案必须同时给出原因与唯一下一动作：授权过期→重新授权；分片缺失→继续缺失分片；临时存储失败→重试；会话过期→重建任务并重新选择；指纹不符→重新选择原文件；对象不存在/大小或校验值不符→重新上传；清单版本变化→读取最新清单；项目已回收或清理中→禁用上传并返回项目/回收站。
- 同一批次中，局部失败不遮挡其他文件；批次状态按后端事实汇总，失败行必须保持可见，不能只用总进度覆盖。

### 5.3 项目回收站

回收站只管理整个项目的 M2 生命周期，不承担上传会话取消、单文件删除或交付物回收。项目中心是唯一“移入回收站”入口；全局回收站只提供搜索、排序、状态查看和仍处于恢复窗口内的项目恢复。

#### 项目中心回收动作

- “移入回收站”是项目中心的行级危险动作，文案必须完整，不使用含糊的“删除”。操作只针对当前整项目，不能出现在上传文件行或伪装为已完成 Asset 删除。
- 二次确认必须显示项目名称，并在确认按钮前同时说明三项影响：立即终止全部未完成上传且恢复后不会自动复活；已完成 Asset、素材清单和项目资料在 48 小时恢复窗口内保留；到期进入后台清理后不可恢复。
- 提交期间禁用重复操作并保留原列表上下文；只有后端确认项目为 `recycled` 后才从项目中心移除并显示成功反馈。未知结果、超时或失败不得先行隐藏项目或显示已回收。
- 成功反馈使用“项目已移入回收站，48 小时内可以恢复；N 个未完成上传已终止”。没有未完成上传时省略数量句，不虚构终止动作。

#### 回收站列表结构

- 复用全局 `AppShell` 和列表页模板。固定顺序为：搜索/状态筛选/排序 → 紧凑数据表；没有新增项目级二级导航、总量卡片或立即清理主操作。
- 表格字段为“项目、状态、回收时间、恢复窗口、处理说明、操作”。项目显示名称和编号；状态同时使用文字与语义色；回收时间和截止时间使用易读本地时间，并保留精确后端时间作为排序依据。
- 默认按“恢复窗口最短优先”排序；同时支持项目名称、回收时间排序，以及“全部状态 / 已回收 / 清理中 / 等待后台重试”筛选。倒计时由后端 `recycle_expires_at` 投影，前端只负责显示，不决定能否恢复。
- `recycled` 显示“已回收”、剩余小时/分钟和明确截止时间，唯一主操作为“恢复项目”。项目恢复后从当前结果集中移除并明确提示：已完成 Asset 与清单保留，先前终止的未完成上传需重新选择文件并新建任务。
- `purging` 显示“清理中 / 已到期 / 不可恢复”，恢复按钮禁用或不出现，并在页面级提示说明后台已开始处理；不得提供强制恢复、立即永久删除或中止清理。
- 清理失败仍属于不可恢复的清理流程，显示“等待后台重试”、稳定失败原因、尝试次数和请求标识；不得显示“已删除”。普通员工只等待系统重试，不提供手动立即清理。
- `purged` 不进入普通列表。当前视图收到清理完成刷新时只显示轻量“项目已清理”反馈；项目随后从列表消失，系统只保留最小审计墓碑，不提供恢复操作。

#### 状态、反馈与布局

- 必须覆盖加载、空数据、已回收倒计时、恢复成功、恢复失败、清理中不可恢复、清理失败等待重试和项目已清理。恢复失败时项目继续留在列表，文案同时给出原因、请求标识和唯一“恢复项目”重试动作。
- 空状态只说明当前没有可恢复或清理中的项目，不提供创建项目或永久删除按钮。加载态保留筛选与表头骨架，避免把未知列表表现为空回收站。
- 1440×900 与 1024×768 都不得产生页面级横向滚动。1024 展开侧栏时允许紧凑表格只在自身容器内横向滚动；侧栏仍支持 `204px → 72px` 收起，收起状态不改变业务数据或恢复资格。
- 回收站不显示真实 R2 对象键、内部清理租约或长期凭据；普通员工只看到可行动的生命周期事实、失败原因和请求标识。

### 5.4 项目工作台 / 术语

术语页只承担“公司 SRT 形成候选 → 人工裁决 → 显式确认不可变版本”的项目级工作，不上传素材、不承载全局任务事实，也不混入 ASR、OCR、视频播放或字幕正文审改。页面继承统一 `AppShell`、`204px → 72px` 侧栏、`64px` 顶栏、项目上下文条和紧凑表格语言；顶部栏是唯一页面标题，内容区不重复显示“术语”。

#### 页面层级与唯一主动作

- 项目上下文条固定展示“返回项目中心 / 项目名称 / 项目状态”和本切片已启用的“概览与素材 / 术语”二级导航；未完成的后续模块不以可点击占位混入正式页面。
- 内容区第一层为一条横向来源摘要：公司 SRT 覆盖、当前来源摘要、提取状态、待确认数，以及右侧当前导出模板名称/版本和“确认并导出”。不增加总量卡片、八类标签墙或模型参数面板。
- “确认并导出”是零待确认时唯一页面级主操作；`pending > 0`、公司 SRT 未全部就绪、提取运行中或失败、来源摘要已变化、项目不可编辑时必须禁用，并在按钮下只显示当前唯一下一步。全量待确认被显式选中时，普通工具栏切换为批量栏，唯一主操作临时切换为“全部采用并导出 XLSX”；两个主操作不得同时出现。
- 提取属于术语模块内的异步准备事实，页面只显示可理解的覆盖、运行阶段、失败位置、请求标识和重试入口；供应商、模型、Token、分块和密钥不向员工展示，也不复制全局任务中心。

#### 筛选、排序与紧凑表格

- 普通工具栏固定为“搜索术语或别称 / 状态筛选 / 类型筛选 / 排序 / 人工新增”。八种正式类型只进入下拉筛选，不展开为一级标签。
- 默认排序为“人名优先 → 八类正式顺序 → 同类首次证据集数/轴号”；同时支持首次出现升序、术语名称升序和待确认优先。任何排序都不得把同类首次出现的稳定事实改写成浏览器临时业务状态。
- 表格固定列为：当前页选择、中文术语、类型、别称、首次出现、证据数、状态、主操作、三点菜单。行高约 `60–64px`，表格最小宽度约 `1040px`；`1024×768` 侧栏展开时，只允许表格容器内部横向滚动，页面根不得横溢。
- 状态必须同时显示文字与语义色：`pending / 待确认` 使用琥珀色，`approved / 已确认` 使用绿色，`edited / 已修改` 使用靛蓝，`rejected / 已驳回` 使用红色；色块内保留状态文字和形状标记，不能只靠颜色区分。
- 行内唯一主操作只在 `pending` 时显示“确认”。编辑、驳回、恢复为待确认和查看证据进入同页侧栏或行尾三点菜单；已经处理的行显示“已处理”，避免一行出现多个同权按钮。

#### 证据侧栏、人工新增和编辑

- 点击中文术语、整行主要内容或证据数，从右侧打开约 `420px` 的 `EvidencePane`，表格上下文留在原位。侧栏展示类型、别称、性别、备注、当前状态和全部真实证据；每条证据至少包含集数、轴号、开始/结束时间和公司 SRT 原文。
- S1 证据侧栏不嵌视频播放器、不编辑公司 SRT，也不展示对象键、模型原始响应或跨项目学习事件。关闭侧栏后焦点返回原触发项。
- 编辑复用同一侧栏，字段为中文术语、八类之一、别称、性别和备注；保存成功后状态进入“已修改”。修改规范术语不能删除既有真实证据或改写只读字幕原文。
- “人工新增”也复用同一侧栏，必须先关联至少一条真实公司 SRT 证据才允许保存；保存后状态直接为“已修改”，并产生后端人工新增决定记录，不伪装成 AI 原候选。
- 驳回项保留审计记录并允许恢复为待确认；恢复后重新参与版本门禁。

#### 多选、版本与来源变化

- 首列支持逐行勾选与当前页全选；只有选中后，普通工具栏才就地切换为批量栏。部分选择显示“已选择 N 项 / 操作逐项保存，失败项保留原状态 / 批量确认 / 批量驳回”，并在选中了待确认项时提供独立的“选择全部 N 项待确认候选”。当前页恰好已覆盖全部待确认项时也不能自动推断全量语义，仍须用户显式切换。
- 显式切换为全部待确认后，批量栏必须写明“已选择全部 N 项待确认候选 / 跨当前筛选范围”，收起普通批量确认/驳回，只保留“全部采用并导出 XLSX”。系统按“分批采用全部 `pending` → 创建不可变版本 → 绑定当前模板版本生成 XLSX”执行；任一项失败时停止版本和文件生成，顶部给出整批结论，并在失败行逐项显示原因与重试，不把部分成功伪装成整批成功或整批回滚。
- 全部候选离开待确认且当前来源摘要仍一致时，页首主操作显示“确认并导出”。确认框必须说明将创建不可变版本、纳入 `approved + edited`、驳回项不进入版本，显示本次绑定的模板名称/版本，并说明后续编辑只建立新草稿而不覆盖旧版本。
- 确认框打开后聚焦“取消”，`Tab / Shift+Tab` 在弹窗内闭环，`Escape` 或取消关闭并恢复触发按钮；提交期间禁用重复操作。成功后页首显示 `V1 / V2…`、来源摘要和下载 XLSX，不把浏览器表格当作版本数据源。
- 公司 SRT 内容变化时，页首显示“公司 SRT 已更新 / 草稿来源已过期”，同时保留旧版本和旧草稿只读事实；禁用版本确认，唯一下一步为基于新来源重新提取。只更换视频不触发此状态，系统也不得自动重跑任何付费识别。

#### 公司导出模板

- 页首在主操作旁显示当前启用的公司模板名称与版本，例如“公司术语模板 · V3（已启用）”，并提供“模板管理”入口；确认和全部采用确认框再次显示本次绑定的模板名称/版本，避免导出结构不透明。
- 模板管理使用受控窗口，不进入自由表格设计器。首版仅能编辑模板名称、五个固定字段 `type / name / aliases / gender / note` 的显示表头与顺序；五个字段均必须且只能出现一次，不可删除、增加任意字段、改变类型或借模板修改术语数据。
- 字段顺序用明确的上移/下移按钮，兼容键盘操作，不把拖拽作为唯一方式。保存分为“保存为新版本”和“保存并启用新版本”；每次保存产生不可变新版本，不原地覆盖。
- 历史版本列表只读，可查看名称、版本、创建时间和当前启用状态；“启用”只切换权威启用指针，不改写历史版本。历史导出继续绑定生成时的模板版本，当前模板变化不重排或重写旧文件。
- 模板窗口打开后焦点进入关闭按钮，`Tab / Shift+Tab` 在窗口内双向闭环，`Escape` 或关闭恢复“模板管理”触发项；保存错误保留输入并就地解释，保存成功反馈新版本及是否启用。

#### 全状态与桌面适配

- 加载：保留来源摘要、筛选位置和表头骨架，不把未知表现为空结果。
- 未提取或 SRT 未就绪：明确缺少的集数/坏文件并把唯一动作指向“返回概览与素材”或“开始提取”；不能允许确认版本。
- 运行中：显示正在解析或校验证据的阶段与集数，可离开页面；已有草稿若存在保持只读，不出现假百分比完成。
- 失败：定位到具体集数/轴号或稳定错误摘要，显示请求标识和唯一“重试提取”；失败不能生成不完整版本。
- 空结果：说明全剧已扫描但没有可审核术语，保留“人工新增”；来源完整且无待确认时允许用户显式确认空版本，不静默跳过门禁。
- 已确认版本：页首显示不可变版本号、创建时间、来源摘要、绑定模板名称/版本和“下载 Vn XLSX”；后续编辑建立新草稿，重新下载继续使用原模板版本。
- 项目不可编辑：保留草稿与历史版本只读，禁用新增、编辑、批量与确认，并给出恢复项目或返回项目中心的唯一下一步。
- `1440×900` 使用完整四段来源摘要与全宽紧凑表格；`1024×768` 允许来源摘要折为两行、工具栏换行，并让表格只在自身容器内横向滚动。侧栏收起到 `72px` 只增加工作区宽度，不改变筛选、选中、审核或版本状态。

### 5.5 项目工作台 / 中文识别

中文识别页只承担“以不可变术语版本和已校验 `asr_video` Asset 创建 ASR 批次 → 查看批次与逐集运行事实 → 处理取消、失败集重试和需对账”的项目级工作。它不上传素材、不编辑字幕正文、不嵌播放器，也不复制全局任务中心。页面继承统一 `AppShell`、`204px → 72px` 侧栏、`64px` 顶栏、项目上下文条和紧凑表格语言；顶部栏是唯一页面标题。

#### 页面层级与唯一主动作

- 项目上下文条只显示已启用的“概览与素材 / 术语 / 中文识别”，未完成模块不作为可点击占位。页面第一层是一条双门禁摘要，依次展示当前已确认术语版本、已校验中文识别视频覆盖、当前适配器模式和运行中批次数；右侧只保留唯一主操作“新建模拟识别批次”。
- 内部适配器代码不得出现在员工页面；fake 只以“模拟识别”文字标签和辅助说明持续可见，明确“不连接外部供应商、只用于链路验证、不代表真实准确率”。不能只在确认框或技术详情里偶尔出现，也不能使用与真实生产相同但无文字的颜色标记。
- 新建批次使用右侧受控面板。员工显式选择“整剧 / 选中集 / 单集”，逐集预览视频门禁、已有同源可复用结果和阻断原因；系统不得静默排除未满足门禁的集数。请求中任一集被阻断时，主提交禁用并把唯一下一步指向“概览与素材”或“术语”。
- 创建确认框再次显示规范化后的集数范围、固定术语版本、来源视频数量、模拟适配器、预计任务数和可复用结果数。成功后批次进入列表并聚焦页面状态消息；未知结果保留同一创建意图供显式恢复，不在浏览器生成第二套批次事实。

#### 批次列表与逐集工作区

- 主列表固定为“批次 / 范围 / 模式 / 状态 / 集数进度 / 质量 / 处理用量 / 更新时间 / 操作”。员工侧处理用量只显示处理时长、任务数、已记录或待对账；不显示每次请求精确金额。默认按“需对账与失败 → 运行中与排队 → 其他”后按更新时间倒序；支持批次编号搜索、状态筛选和更新时间排序。表格最小宽度约 `1120px`，只在自身容器横向滚动。
- 批次状态必须同时使用中文文字、语义色和形状标记：`blocked / 已阻断`、`queued / 排队中`、`running / 识别中`、`partial / 部分完成`、`completed / 已完成`、`failed / 失败`、`cancel_requested / 取消请求中`、`reconciliation_required / 需对账`、`cancelled / 已取消`。进度只显示“已形成结果或明确复用的集数 / 请求集数”，不能根据浏览器计时猜测完成。
- 每个批次行只有一个直接主操作“查看批次”。展开后在原位显示批次身份、固定术语版本、来源清单摘要、请求标识、结果修订、质量/用量汇总和逐集表；历史批次保持可回查，不使用遮挡上下文的整页弹窗。详情提供低频次级入口“本批次热词”，打开同一侧栏模式，不给主表增加常驻热词列。
- “本批次热词”首先显示同项目不可变术语版本、投影版本、摘要、实际纳入数、规则过滤数和适配器截断数；随后可展开已纳入热词示例、过滤/截断原因和五类回执。不得混入其他项目术语、草稿、跨项目汇总或普通员工日志。
- 逐集表固定为“集数 / 来源视频 / 运行状态 / 质量 / Cue 与时长 / 处理用量 / 尝试 / 操作”。运行状态只映射 `queued / leased / running / completed / failed / cancel_requested / cancelled / reconciliation_required`；其中 `leased` 对员工显示“准备中”，`cancel_requested` 显示“取消请求中”，`reconciliation_required` 显示“需对账”。
- 运行状态和质量状态分列。成功结果另显示 `pass / 质量通过`、`warning / 质量警告` 或 `rejected / 结果不可用`；不能用供应商单一置信度替代质量结论。逐集低频的尝试、请求标识和诊断进入行内展开或质量侧栏，主表不展示字幕正文。
- 质量侧栏只显示音频覆盖、空结果、Cue 数量与公司稿差异、异常长段、时间轴异常、术语命中、低置信片段、已知幻觉信号和尝试历史；不提供播放、逐轴采纳或正文修改。关闭后焦点返回原“查看质量”触发项。

#### 取消、重试、需对账与版本过期

- 运行或排队批次可发起“取消批次”。确认框必须说明：未开始集将停止；已经进入外部执行的集可能先变为“取消请求中”或“需对账”；已完成结果和历史用量不会删除。提交后批次先显示“取消请求中”，逐集再按权威事实区分取消请求中、已取消、需对账或既有终态，不能把所有集立即伪装成已取消。
- `partial` 或 `failed` 且存在明确 `failed` 集时，批次工作区唯一恢复主操作为“仅重试失败集”。确认框列出具体集数并说明成功、警告、已复用、取消和需对账集不重复；重试创建新尝试并保留旧尝试、成功结果和整剧历史。局部重试失败逐集回显，不覆盖此前可用结果。
- `reconciliation_required` 不显示普通重试。页面用“需对账 / 结果或处理用量未知 / 暂停自动重试”同时编码，给出请求标识和“查看对账说明”；只有未来契约提供可执行命令后才能增加处理按钮。
- 热词应用回执必须使用中文文字、语义色和稳定代码同时表达：`simulated / 模拟已应用`、`submitted / 已提交`、`partially_submitted / 部分提交`、`unsupported / 不支持热词`、`unknown / 回执未知`。`partially_submitted` 必须显示已发送/省略数量和原因；`unsupported` 必须警告识别可能在无热词下继续；`unknown` 不得伪装成功，并可随结果/用量事实进入需对账。所有成功或部分成功回执旁持续提示“已发送不等于准确率已提升”；没有同样本 A/B 证据时统一写“热词链路已接通，准确率未验证”。
- 员工页只展示处理时长、任务数、质量摘要和用量“已记录 / 待对账”，不显示精确金额；按请求、项目、日期、供应商和模型的精确费用归入独立系统控制台。本页只说明这一边界，不提供系统控制台入口或界面。普通员工不得在本页修改限额、并发、模型、供应商或密钥。
- 新术语版本不会改写旧批次。历史批次继续显示实际 `termVersionId` 并以“术语版本已过期”文字标识；默认建议重跑受影响集，也可在新建面板中显式整剧重跑，任何重跑都形成新的不可变结果修订。

#### 全状态、焦点与桌面适配

- 必须覆盖加载、无批次、读取失败、双门禁阻断、排队、运行、部分完成、全部完成、无可用结果的失败、批次取消请求、批次需对账、已取消、逐集需对账、五类热词回执、质量警告、结果不可用、创建/取消/重试失败和项目不可编辑。错误同时显示原因、请求标识和唯一下一动作；加载不能表现为空，失败不能抹掉已成功集。
- 新建侧栏、质量侧栏与三类确认框打开后焦点进入对应语境，`Tab / Shift+Tab` 双向闭环；非提交期间 `Escape`、取消或遮罩关闭并恢复原触发项。提交期间禁用重复操作、焦点不得逃到背景；失败后保留输入并聚焦安全操作，成功后聚焦现有 `role=status` 反馈。
- `1440×900` 使用单行门禁摘要、全宽批次表和展开的逐集工作区。`1024×768` 侧栏展开与收起两态都不得产生根页面横溢；门禁摘要折为两行，工具栏换行，批次表与逐集表分别只在自己的容器横向滚动。侧栏 `204px → 72px` 只改变可用宽度，不改变批次、选择、门禁、取消或重试资格。

#### Wave B：项目中心多剧选择与批量确认

- 多剧入口仍属于项目中心，选择单位固定为 `Project`，不是上传会话、文件、素材槽位或单集。项目列表增加服务端中文识别资格筛选“可识别 / 缺术语 / 缺视频 / 识别中 / 失败”；筛选、搜索、排序、总数和翻页都读取后端结果，浏览器不得按项目文案重新计算双门禁。
- 项目中心表格延续紧凑列表和行动优先排序。首列支持逐行勾选及当前页全选；勾选不隐藏阻断项目，因为员工必须在确认阶段看见完整选择与阻断原因。单次最多选择 20 部，达到上限后保留已选项并明确说明上限，不静默截断。
- 选中后普通工具栏替换为批量栏，持续显示“已选择 N 部 / 可创建 N 部 / 阻断 N 部”。当前页全选后可显式升级为“选择全部 N 部符合当前服务端条件的项目”。项目搜索、中文识别资格、项目状态等会改变服务端成员集合的查询一旦提交，必须清除跨页全选并以 `role=status` 播报；纯排序和翻页只改变同一权威集合的观察顺序，必须保留选择并播报仍选择的范围，不能把两类查询统一当作“筛选变化”。
- 批量栏只有一个主操作“批量中文识别”。它打开右侧确认面板，不在列表页直接创建批次。阻断项目仍计入 `selected`，但提交只在员工显式选择“只为可执行项目创建”后包含当前 `canCreate` 集合；页面不得用“系统已自动跳过”掩盖局部阻断。
- 确认面板首屏固定展示“已选 N 部 / 可创建 N 部 / 阻断 N 部 / 总集数 / 预计新任务 / 可复用结果”。逐项目列表显示项目、术语版本、热词数量、视频覆盖、集数、新建/复用和资格结果；每个阻断项目只给一个返回术语或概览与素材的修复入口。
- 提交使用同一多项目意图。提交中由面板自身成为可编程焦点语境，所有关闭、取消和提交控件禁用；`Tab / Shift+Tab` 均留在面板，`Escape`、遮罩、关闭和重复激活不产生退出或第二次创建请求。确定失败保留规范化项目集合和同一意图，恢复安全取消焦点；未知响应保留同一意图并只提供重新读取，不得重发创建；只有权威成功返回后才进入结果面板。返回结果逐项目显示 `accepted / 已接受` 或 `blocked / 已阻断`；部分接受不能显示成整批成功，已接受项目保留其子批次入口，阻断项目保持原因和修复入口。

#### Wave B：员工侧“中文识别任务”列表与详情

- 全局侧栏在“作业”组增加一个专用“中文识别任务”入口，只聚合 `ASRDispatchGroup` 与各项目子批次，不升级为通用任务中心，不包含上传、OCR、导出、字幕编辑或项目内逐集正文。
- 列表固定为“派发 / 项目 / 接受结果 / 状态 / 项目进度 / 质量与用量 / 更新时间 / 操作”。默认先显示需对账、失败、取消请求和运行项，再按更新时间倒序；搜索、状态筛选和排序由服务端执行。每行只有一个直接动作“查看任务”，低频标识进入详情。
- 派发摘要必须分开显示“已选择 / 已接受 / 已阻断”和“已完成项目 / 已接受项目”。`queued / 排队中` 不写成识别中；`partial / 部分完成` 同时列出完成、失败、取消与需对账数量；`cancel_requested / 取消请求中` 保留已完成结果；`reconciliation_required / 需对账` 没有普通重试入口。
- 详情侧栏先显示派发身份、创建时间、规范化项目集合、接受/阻断结果与刷新状态，再显示逐项目子批次。逐项目行只展示项目、集数、新建/复用、子批次状态、质量/处理用量摘要和唯一下一动作；逐集质量、失败集重试和项目级取消继续回到该项目“中文识别”页处理。同一 Dispatch 的部分接受结果、列表摘要、详情行、总集数和新建/复用汇总必须引用同一不可变 `accepted` 集合，`blocked` 项目不得因刷新或示例拼装进入详情。
- 派发取消只表达对仍未终结且允许取消的项目子批次的取消意图。确认框逐项列出本次影响数量和项目，并明确已完成结果、需对账事实与历史处理用量不会删除；需对账项目不作为普通取消目标。打开后聚焦安全取消，提交中确认框自身获得焦点，按钮、`Escape`、遮罩、关闭和重复激活全部锁定。确定失败保留原派发与同一取消意图并恢复安全焦点；成功只把实际受影响子批次和派发投影显示为“取消请求中”，关闭确认框后聚焦现有 `role=status`，不能把全部项目立即写成已取消。
- 员工页面不得显示供应商、模型内部代码、并发、预算、服务器、密钥、精确金额或管理员入口。处理信息只使用项目数、集数、媒体时长、质量摘要和“用量已记录 / 待对账”。
- 覆盖加载、空数据、读取失败、部分接受、排队、运行、部分完成、完成、失败、取消请求、已取消、需对账和项目不可编辑。所有状态以中文文字、语义色和形状同时编码；错误包含原因、请求标识和唯一恢复动作。
- `1440×900` 使用展开 204px 侧栏和约 `1120px` 的任务表。`1024×768` 必须分别验收 204px 展开与 72px 收起，两态根页面水平可滚范围为 0；项目中心表格、确认面板逐项目表和任务表只在各自容器横向滚动，侧栏自身纵向滚动，不推动根页面。
- 批量确认面板、任务详情侧栏和取消确认框打开后焦点进入关闭或安全取消项，`Tab / Shift+Tab` 在语境内闭环；非提交期 `Escape`、遮罩或取消关闭并恢复原触发项。提交中焦点留在容器并禁止重复提交；失败后保留选择并聚焦安全操作，成功后聚焦既有 `role=status`。

### 5.5A 项目工作台 / 画面字

画面字页只承担“以已确认素材清单中的 `screen_video`、不可变术语版本和项目内 OCR 批次形成候选 → 逐集人工保留、忽略或修订 → 发布不可变画面字版本”的项目级工作。它不上传素材，不展示供应商、模型、密钥、金额、并发或策略配置，也不承接字幕验收、全局任务中心、人物风险或管理员能力。页面沿用统一 `AppShell`、`204px → 72px` 侧栏、项目上下文条、紧凑表格和文字加语义色的状态语言。

服务端返回的逐集状态、候选四态 `pending / approved / edited / rejected`、候选修订和决定事件是唯一业务事实；本地行选择、筛选、右侧面板开合和媒体播放仅为展示状态，不得生成第二套候选状态、推算完成或覆盖服务端决定。

#### 页面层级与批次入口

- 项目上下文条只保留现有项目模块。页首四项摘要依次显示固定术语版本、画面字视频覆盖、当前批次及发布门禁；页面在无可用批次时只有一个主操作“新建识别批次”，存在可审核批次后主操作切换为“发布画面字版本”。员工必须先在受控确认框选择“整剧 / 选中集 / 单集”，查看规范化集数、固定术语版本、来源视频和阻断原因，再提交同一创建意图。
- 双门禁固定为“目标集均有已确认 `screen_video` Asset”和“项目存在已确认术语版本”；还必须确认项目可编辑、素材清单是最新确认版本且目标集没有重叠活动批次。阻断时不静默跳过集数，主操作禁用并给出返回“概览与素材”或“术语”的唯一修复入口。
- 当前批次摘要只显示批次编号、范围、已处理集数、失败/需对账数量及真实处理帧数；不显示浏览器估算进度或精确费用。取消只影响非终态集，确认框必须说明已完成候选、人工决定和已记录用量保持不变。

#### 三栏审核工作区

- 桌面工作区固定为“集/批次队列 — 紧凑候选表 — 截图与视频证据/编辑”。左栏同时提供集列表与批次历史切换；集状态用中文文字、语义色和形状标记完整覆盖：未开始、排队中、识别中、待审核、已完成、明确空集、失败、取消请求中、已取消、需对账和来源已变化。默认把待处理、失败和需对账排在可见区，不把已完成事实折叠成不可回查。
- 中栏工具条固定为搜索、状态筛选、分类筛选和排序。默认“人名条优先、同类首次出现、其余按集数与时间”，候选表最小宽度约 `760px`，列为“选择 / 文字与分类 / 时间 / 位置 / 术语证据 / 状态 / 操作”，只在自身容器横向滚动。状态分开表达系统建议与人工决定；`pending / 待确认`、`approved / 已保留`、`edited / 已修改`、`rejected / 已忽略` 都保留原始 OCR 事实和审计历史。
- 中表高亮行与右侧证据必须引用同一个服务端 `candidateId + revision`：鼠标点击、键盘选择或决定成功后的自动前进都要同时更新行选中态、代表截图、同源视频定位时间和编辑字段；右栏仍在读取新证据时不得保留上一行截图或允许基于旧证据写入。右栏固定呈现代表截图、视频时间、前后 2 秒、跳回候选、分辨率、置信度、来源、当前文字、分类、位置、开始/结束时间和术语回执。视频与截图按媒体元数据 `object-fit: contain`，竖屏、横屏或方形都不裁切；放大层保留同一候选与时间身份，关闭后焦点返回原“放大”。候选/批次技术 ID 不占主界面；requestId 只在错误恢复或详情中显示。

#### 人工决定、拆分和空集

- 高频决定的可见文案固定为“保留”和“忽略”。“保留”仍提交既有 `approved` 决定，权威成功后自动选择下一条待确认候选；提交中或失败时保持当前行和证据，不提前跳转。“忽略”明确说明“本条不纳入画面字字幕，可从‘已忽略’恢复”，仍提交既有 `rejected` 决定；已忽略项唯一决定动作是“恢复为待确认”，沿用现有恢复事件，不增加第五种候选状态。编辑可修改文字、分类、位置和候选时间，保存后形成 `edited` 决定，不覆盖原始识别值。人工新增从当前集进入，必须填写文字、分类、位置、开始/结束时间并绑定截图或当前视频时间证据。
- “拆分左右”只用于同一画面同时出现左右两条独立文字；确认框显示两条文字、共同来源时间和各自位置，提交后形成两个独立 Cue，不合并成一条文本，也不提供任意时间轴编辑器。人名条分类必须关联已确认术语或明确标记“身份待核对”，不能由 OCR 文本自动确认为人物身份。
- 当前页勾选后才用批量栏替换普通工具栏，提供批量保留或忽略；批量栏必须持续写明“当前页已选 N 项”或“当前服务端筛选范围内全部 N 项待确认”，不得按可见行、旧筛选或浏览器缓存扩大范围。当前页全选后只能由用户显式升级为跨页范围；搜索、状态或分类改变服务端成员集合时必须清除跨页全选并播报，纯排序和翻页保持同一权威集合。批量决定只作用显式范围内仍为 `pending` 且修订匹配的候选；其他四态事实不改。局部失败不伪装整批成功，失败原因和唯一重试入口留在原行。
- 零候选不等于完成。员工必须使用“确认本集无画面字”，确认框说明该决定可审计、后续仍可重新识别；成功后集状态为“明确空集”。只有 `failed` 或 `confirmed_empty` 集显示“重新识别本集”，且重试保留旧候选、人工决定和历史用量。`reconciliation_required` 不显示普通重试，只显示请求标识、未知事实说明和对账入口。

#### 来源变化、发布门禁与全状态

- 批次固定素材清单、目标集、视频 Asset、术语版本、能力与请求标识。来源 SRT、视频或术语版本变化后，旧批次和旧草稿保持只读并显示“来源已变化”；用户只能从最新来源新建批次，不原地重算或覆盖人工决定。
- 发布前必须满足：目标范围内每集为“已完成”或“明确空集”；不存在待确认、失败、需对账或活动任务；所有保留项文字非空，时间合法且不超过视频时长；左右拆分为独立 Cue；人名条身份有术语或证据；同一身份写法一致。任何失败都在门禁摘要中给出具体集数/候选和唯一定位入口。
- 发布成功形成不可变 `ScreenTextRelease`、Cue 集和 UTF-8 BOM 的画面字 SRT，按自然集数顺序下载。历史版本只读可回查；后续来源或人工修订必须建立新草稿和新版本。画面字 SRT 是后续前置审改/字幕链路的只读输入，不是最终交付物。
- 页面覆盖加载、无批次、候选为空、读取失败、再次失败、创建/取消/重试/发布提交中与失败、禁用、来源失效、明确空集、局部批量失败和项目不可编辑。代表截图不可用、摘要不一致或正在重读时，当前候选的保留、忽略、恢复、编辑、拆分、人工新增、行选择和全部批量写入统一锁定；错误区显示安全原因、requestId 和恰好一个“重新读取候选证据”，重读失败仍保持同一锁，成功后聚焦右侧证据容器并恢复当前候选合法动作。加载不能表现为空，失败不能抹去已成功集，也不能用视频可播放替代代表截图门禁。

#### 键盘、焦点与桌面适配

- 创建、人工新增、左右拆分、空集、重试、取消和发布确认框打开后聚焦安全操作；`Tab / Shift+Tab` 在活动语境双向闭环。非提交期 `Escape`、取消或遮罩关闭并恢复原触发项；提交中禁用关闭与重复提交，焦点留在可编程容器；失败保留原意图并聚焦唯一恢复，成功聚焦现有 `role=status`。证据侧栏、表格行和图像不得只靠颜色表达状态。
- `1440×900` 使用展开侧栏和完整三栏，右侧证据始终与中表当前行同屏；`1024×768` 的 `204px` 展开与 `72px` 收起两态都不得产生根页面横溢，摘要折为两行、证据栏下移到表格之后，但仍与同一个当前行选择绑定并在“处理”后进入可达焦点顺序。表格只在自己的容器横向滚动；侧栏宽度与证据栏换行只改变布局，不改变选择、审核、重试、取消或发布资格。

### 5.6 统一任务中心

统一任务中心是员工 `AppShell`“作业”分组唯一全局入口，正式路由 `/tasks`。采用紧凑数据表汇总 ASR Dispatch、未归属 Dispatch 的单项目 ASR、画面字 OCR、术语提取、前置审改准备和交付生成等真实异步事实；列表按服务端搜索、类型、状态、项目、注意优先级和时间稳定分页，详情抽屉显示范围、真实进度、最新安全错误/requestId、有界历史及所属工作台入口。

首屏按“需处理 / 运行中 / 排队 / 失败或待对账”横向显示四项真实数量和一句解释；四项是服务端查询捷径，不是独立大卡、趋势图或浏览器聚合。其下依次为查询工具条、紧凑表格和稳定分页。查询固定包含项目名或任务短号搜索、任务类型、规范状态、项目与排序；任何查询身份变化立即清除旧页、旧详情、旧恢复和写意图，迟到响应不得覆盖新查询。

任务表固定为“任务 / 项目 / 状态 / 进度 / 最近结果 / 更新时间 / 操作”。任务列同时显示员工名称、稳定短号与类型；状态必须同时使用中文文字、形状和语义色，并在详情忠实解释原领域状态。进度只有两种合法表达：服务端 `completedCount / totalCount` 加阶段说明，或“进度未知”加真实阶段说明；不得按时间、列表条数或前端轮询次数补百分比。ASR Dispatch 已包含的项目 Batch 只在详情抽屉显示，不能重复成为列表行。

点击任务名称或“查看详情”打开同页右侧抽屉。抽屉宽约 `650px`，顶部固定任务类型、名称、短号、项目范围、创建时间与关闭动作；正文先显示当前状态、最近更新、真实进度或未知、最新安全原因/requestId，再显示最多 5 条最新优先历史。多项目 Dispatch 追加内部横滚的项目子结果表，并明确这些子结果没有在统一列表重复计数。详情读取失败只读取同一 `taskType + resourceId`；再次失败仍保留唯一原查询动作，成功聚焦抽屉标题。

允许的取消/恢复只调用领域既有命令。ASR Dispatch、独立 ASR Batch 和画面字 Batch 在服务端允许时可确认取消；交付生成失败可确认恢复同一 `deliveryId`。画面字失败集、ASR 失败集、前置审改重新准备和术语重新提取等复杂动作返回对应工作台：术语原 Run 不提供虚构重试，员工从术语工作台显式新建 Run。确认 Modal 初始聚焦“安全返回”，`Tab / Shift+Tab` 双向闭环；pending 时容器获焦、全部关闭与重复提交受锁。unknown 或 retryable 只显示同一领域命令身份的人工查询，不自动 POST；确定冲突关闭 Modal、清旧意图、刷新权威事实并聚焦唯一错误，下次显式动作生成新身份。

列表或详情处于 loading、初始 error、stale、refreshing 或命令 pending 时，所有受影响写入口锁定。stale 保留上次事实、真实 requestId 和恰好一个原查询重读；再次失败仍保留同一动作，成功后列表聚焦“任务中心”标题、详情聚焦抽屉标题。后台任务刷新不得抢活动 Modal、抽屉或输入焦点；抽屉关闭、`Escape` 或遮罩退出后恢复原行触发点。

`1440×900` 使用 `204px` 展开侧栏，表格可用宽度约 `1170px`；`1024×768` 默认 `72px` 收起侧栏，表格最小宽 `1050px` 且仅自身容器横滚。1024 的侧栏展开为 `216px` 覆盖层，保留正文起点和宽度、不挤压正文；根页面始终无横向溢出。长项目名、阶段、原因、requestId 和稳定短号不能逐字符断行，状态不能只靠颜色表达。

上传继续属于独立“上传任务”，术语/OCR/字幕正文仍在项目工作台，策略、预算、Secret 和服务器任务仍在管理员 ControlShell。员工响应和页面不展示供应商、模型私参、费用、提示词、对象键、内部地址、Worker owner 或完整原始载荷。

统一中心启用时删除独立“中文识别任务”全局导航与 `/asr-dispatches` 页面路径；ASR 多项目 Dispatch 的列表、详情、取消与未知恢复并入 `/tasks`，不保留第二套全局任务入口。权威合同见 `docs/contracts/M3-unified-task-center.md`。

### 5.7 前置审改工作台

前置审改只处理公司 SRT 与中文识别文本的逐轴人工裁决，并产出唯一待验收修订稿。页面不承接术语审核、OCR 画面字、字幕验收或交付；术语版本只作为固定证据身份，不在本页编辑。

页面继承统一 AppShell 和项目上下文导航。工作区采用“集/问题队列 + 当前决定 + 直接 MP4 证据”三栏：左栏 190px 管理剧集与问题，中央为唯一主工作区，右栏 306px 固定证据。顶部来源条同时显示公司稿摘要、ASR 覆盖、固定术语版本和当前会话来源一致性，避免员工在错误版本上继续决定。

#### 5.7.1 队列与唯一最终稿

- 集队列显示已完成、待处理、格式阻断和有限审改，问题队列显示组合轴差异、仅公司轴、仅识别轴及格式问题；状态必须使用文字、数字与语义色共同表达。
- 公司稿原文、ASR 原文和组合轴上下文均只读。公司稿与中文识别保持同屏，但必须分别使用“来源 A / 公司稿”和“来源 B / 中文识别”的标题、只读标签、边界色与足够间距建立独立来源身份；两者之间不得显示文字、数字、比例、交叠率或组合轴编号，也不得建立第三个内容列，只保留低占用、无文字的双色过渡分隔。当前问题的一对多/多对一、时间交叠等关系放在问题元数据行，完整关系进入“本集全轴”抽屉。唯一最终稿仍为来源对照下游第三层，决定始终绑定到唯一公司轴或唯一未配对 ASR 轴，不得把整组文本误写入其他轴。
- 页面只维护一个“最终采纳稿”。来源徽标明确人工选择来自公司稿、ASR、手动修改、删除公司轴、补入 ASR 轴或忽略 ASR 轴；公司稿和 ASR 原文不随决定改变。
- 六类上下文动作分别为：保留公司稿、采用 ASR 文本、手动修改、删除公司轴、补入 ASR 轴、忽略 ASR 轴。动作集合和决定语义必须随轴关系切换，不能只替换按钮文字：普通双源只允许保留公司稿、采用 ASR 文本或手动修改，最终时间仍取公司轴；`company_only` 只允许保留公司稿、手动修改公司轴或删除公司轴；`asr_only` 只允许补入识别轴、手动修改后补入或忽略识别轴，补入后的文本和时间均取该 ASR 轴。删除或忽略必须二次确认并说明影响范围，避免把旧本地补丁按钮平铺回主界面。
- 决定成功后播报结果并自动定位下一条未解决问题；短时提供“撤销”。进入手动修改后，切集、切问题或离开页面前必须要求保存或放弃，不得静默丢失编辑。

#### 5.7.2 文本基准、格式门禁与完成

文本基准支持整剧、选中集和当前单条三个范围，并只允许“公司稿优先”或“ASR 优先”。确认面板必须显示可安全更新数、保留人工决定数和仍需人工数。基准只为尚未人工决定且可确定处理的条目生成或清除系统决定，不得覆盖任何人工决定。键盘顺序必须经过当前范围 radio 和当前基准 radio；同组用方向键切换，Tab/Shift+Tab 在关闭、两组 radio、取消和确认之间完整闭环，Escape/取消后回“调整文本基准”。

最终稿行内显示格式事实：禁用标点、有效字数、时间轴有效性以及需人工处理的阻断原因。单轴 `<=18` 字为正常，`19–28` 字提示优化，`>28` 字为阻断且必须选择“手动拆句或裁短”或“明确保留并填写理由”；保留理由至少说明语义或节奏必要性，保存后形成绑定当前来源修订的可审计格式决定。完成门禁只读取已保存的裁短文本或保留理由事实，不因打开表单或输入未保存理由而解除阻断。禁用标点等其他阻断仍须独立解决；本页不提供任意时间轴编辑。仅当本集待处理数和格式阻断数均为零时，唯一主动作“完成本集并进入下一未完成集”可用。整剧完成后主动作切换为“生成待验收修订 SRT”，形成不可变版本并固定来源身份、完成签名和决定事件；本页不执行字幕验收或交付。

#### 5.7.3 证据、失效与异步状态

右栏直接使用当前集已绑定 MP4 Asset，进入问题时定位到轴前约 1.8 秒，允许播放、前后 2 秒和跳回当前轴。默认播放器始终保持 306px 证据栏内的紧凑高度，不因竖屏素材自动变大；标题栏提供低强调“放大”。放大层继续读取同一视频宽高元数据，竖屏、横屏和方形均使用 `object-fit: contain` 和中性留黑完整显示，保留字幕、播放、前后 2 秒和跳回当前轴，不得用 `cover` 裁切竖屏人物或字幕。放大层打开后焦点进入关闭项，Tab/Shift+Tab 在播放控制与关闭项内闭环；播放或其他提交状态不能使焦点逃到遮罩后页面，Escape/关闭后回到原“放大”触发点。证据窗口以文字显示原始分辨率/画幅、轴前后范围、可继续播放整集和“本页不修改时间轴”，不设计 360p/1080p 转码或云资源控制。

当前问题标题旁提供低强调“本集全轴”入口，打开底部抽屉分页浏览本集全部公司轴与识别轴的轴号、时间、关系、文本差异、已保存决定、格式事实和风险提示。点击一行只定位主工作区当前审改项并同步视频，不在抽屉内编辑时间轴、改正文或复制一套决定状态。抽屉覆盖加载、空、读取失败、请求标识、唯一重新读取、再次失败和恢复；打开聚焦关闭项，Tab/Shift+Tab 保持在抽屉语境，Escape、遮罩或关闭后回原“本集全轴”。表格最小宽度留在抽屉自身横向滚动容器，不能推动根页面。

公司 SRT、ASR 结果、术语版本或格式策略变化后，旧会话进入来源已变化只读态。历史采纳稿仍可查看，唯一恢复动作是从新来源创建会话，旧人工事件不得原地改写。ASR 缺失的剧集进入“有限审改”：ASR 来源块明确不可用，采用 ASR、补入 ASR、忽略 ASR 等动作必须移除或禁用，只保留公司稿与公司稿格式处理；本集不能伪装成已完成双源对照，待 ASR 可用后重新建立完整对照会话。

加载态说明正在核对固定来源和决定；空态区分“无待处理问题”与“仍需格式门禁”；失败态显示具体原因、请求标识和唯一“重新读取会话”，再次失败仍保留原意图。提交中焦点落到可编程工作区或弹窗，Tab/Shift+Tab、Escape、遮罩和重复提交均不得离开当前语境；成功后焦点进入 `role=status` 结果，失败后回唯一恢复动作。

#### 5.7.4 响应式与滚动归属

- `1440×900`：204px 展开侧栏下保持三栏，根页面无横向滚动；队列和页面各自承担纵向滚动。
- `1024×768`：支持 204px 展开和 72px 收起两态。工作区改为队列 + 决定两栏，证据区下置为紧凑横排；根页面无横溢，项目模块导航和队列筛选只在自身容器横滚。
- 状态、阻断和来源一致性不得只靠颜色；键盘焦点轮廓、对话框首末闭环、关闭后回原触发点和提交后状态播报为正式验收条件。

#### 5.7.5 自动筛选、风险提示与按需判断依据

- 员工页只在来源条下显示一行紧凑摘要：“自动筛选完成 / 无需处理数 / 转人工数 / 本地规则优先 / AI 复核状态”。摘要是工作量说明，不增加新的主操作、任务模块或配置墙。摘要右侧可预留小型“AI 快速筛选”入口；运行中只在同一位置显示“已处理 / 总数”和 2px 细进度，不产生大卡片或独立页面。设计状态覆盖未启用、可运行、运行中、完成和失败不阻断。
- “查看判断依据”按需展开当前条的本地规则命中、转人工原因和 AI 是否参与。默认先执行可复现的本地确定性规则；AI 只作为可选复核，可关闭、失败或超限，任何一种情况都只回到人工队列，不覆盖唯一最终稿、不阻断审改。
- 当前共享契约没有正式 AI 运行、进度、筛选数量或成功投影，风险命中也没有正式后端投影；原型仅演示未来状态。正式前端首版必须在没有权威事实时隐藏或显示“未启用”，不得用常量、前端推算或模拟请求伪造生产调用、数量、进度、成功或风险命中。
- 风险词只作为公司稿、中文识别和唯一最终稿的非阻断核对提示，并在“本集全轴”中提供快速定位。双源命中优先提示核对当前音频；仅公司稿命中说明需核对音频是否一致，仅识别命中说明需核对公司稿是否遗漏，最终稿仍命中只显示风险提示。不得增加独立风险模块、严重等级、硬门禁或员工侧风险配置入口。
- 员工页不得出现供应商、模型名、提示词、密钥、精确费用、预算或优化智能体入口；这些事实属于系统控制台。本页也不逐条展示后台学习日志，只保留员工完成当前决定所需的最小依据。

### 5.8 视频字幕验收工作台

用于最终看片、共享时间标尺复核与轻量修正，不用于重新运行 ASR、OCR、术语提取或大规模审改。用户在 `UI-M3-05 Rev 3` 明确选择方案 A“时间轴主导的均衡三栏”，且未要求额外调节；因此本节从 Rev 3 起是生产前端唯一权威方向。页面采用“专业编辑套件”的均衡紧凑工作面：应用外壳和项目上下文条之下仅保留一个页面标题；标题右侧依次为来源状态、自动保存状态、次操作“整剧预检”和唯一主操作“通过并下一集”。整剧达到服务端交付就绪后，预检结果抽屉只提供“前往交付确认”，不在编辑工作台直接生成、下载或管理交付产品。

#### 5.8.0 Rev 3 唯一方向与实现变量

- 唯一生产方向为方案 A：左侧剧集队列、中央播放器与共享时间轴、右侧字幕/问题编辑器。默认视觉密度为“均衡”，中央播放器占中央工作区纵向约 `54%`，时间轴吸收其余高度；默认左右栏宽分别为 `220px / 326px`。这些值是起始布局变量，不得改造成运行时主题或让员工自行切换的偏好设置。
- 方案 B“文本审校主导”和方案 C“看片主导”只保留在仓库外候选原型中作为选择历史，不进入生产需求、模块注册、CSS 变体、设置项或 A/B 运行时代码。正式页面不得出现“方向、密度、画面、主色、三栏”候选选择器或“暂存当前组合”。
- 方案 A 的“靛蓝”统一解析为全局设计令牌 `#4458D8`，不另建页面私有近似蓝。S5 页面变量固定使用：主强调 `#4458D8`、按压/悬停 `#3549BD`、选中浅面 `#F0F3FF`、主文字 `#20202A`、弱文字 `#747481`、边界 `#E7E7EE`、工作面 `#FFFFFF`、冷灰画布 `#FAFAFD`、成功 `#1F8F58`、警告 `#9A6700`、硬错误 `#C73939`、画面字语义 `#087F83`。
- 台词轨使用 `#DFE4FF` 浅面与靛青描边，画面字轨使用 `#D9F0EE` 浅面与青绿描边，问题轨使用 `#FFF0CE` 浅面与琥珀描边。红色仅用于真实硬错误、阻断和播放头；状态必须同时有文字或图形标识。主要分区使用 `1px` 边界，不增加悬浮卡片、渐变、装饰阴影或深色主题。

#### 5.8.1 信息结构与桌面几何

- 主工作区固定三栏：左栏 `220px`，中央 `minmax(440px, 1fr)`，右栏 `326px`；栏间 `1px` 边界，不把每个区域包成悬浮卡片。中央使用 `grid-template-rows: minmax(210px, 54%) minmax(180px, 1fr)` 分为上部播放器和下部时间轴；三栏各自滚动，根页面不得横向溢出。
- 左栏顶部是全部 / 有画面字 / 多层 / 问题集四个分段筛选与异常、提醒快捷入口；下方剧集自然排序。每集分行显示逐集状态、台词层数/条数、画面字层数/条数和未关闭问题数，不能压缩为一行省略号。当前集使用靛青浅底与清晰焦点；已通过用绿色文字与图形共同编码。
- 播放器保持画面主体而非装饰预览，按媒体真实宽高使用 `object-fit: contain` 完整显示横屏、竖屏或方形。短剧以 `9:16` 竖屏为主要验收场景：默认画面高度应尽量占满播放器可用高度并中性留黑，不得按中央栏宽把竖屏缩成难以辨认的小预览，也不得用 `cover` 裁切人物、画面字或底部台词；同源放大/全屏继续使用完整画面。顶部只显示本集真实存在的“流畅 / 高清 / 原片”和“有画面字版 / 无画面字版”；缺失版本不留禁用占位。默认优先有画面字版，否则回退无画面字版；版本选择只改变验收视图。
- 播放器底部采用成熟视频网站的紧凑基础控制：同一播放/暂停切换、可拖动进度、当前时间/总时长、音量/静音、`0.75× / 1× / 1.25× / 1.5× / 2×` 倍速、前后逐帧、前后 1 秒和全屏/放大。不得只保留“暂停”而缺少显式播放、倍速、音量或全屏。视频、叠字、播放头、列表定位和问题时间始终使用同一个当前毫秒值。
- 叠字使用两条稳定安全区：新增画面字默认显示在画面上方安全区，新增台词默认显示在画面下方字幕安全区；二者同时命中时不得互相覆盖。轨道、编辑器、播放器叠字和导出事实继续使用同一 `track`，前端不得仅凭文字内容猜测位置。
- 时间轴顶部是撤销、恢复、明确的“新增台词 / 新增画面字”、缩放、适配全长与当前选择摘要。不得使用会静默默认轨道的单一“播放头新增”。轨道依次为视频、台词、画面字、问题；共享整数毫秒刻度。真实重叠或共用端点动态排层，空画面字轨完全收起。轨道底部保留自身横向滚动，不把横向滚动转移到根页面。
- 右栏使用“字幕 / 问题”一级页签。字幕页内部先显示“全部 / 台词 / 画面字”轨道筛选，再显示紧凑的新增、删除、复制、剪切、粘贴和选择摘要；下方才是 Cue 列表以及当前 Cue 的文字、入点、出点、轨道和“应用修改”。问题页提供“记录问题”，并呈现问题类型、时间、轨道、备注、状态、解决/豁免和定位动作。右栏底部固定保存反馈，但不得遮住列表或表单末项。
- 项目上下文中的“返回项目中心”和“切换项目”都回到全局项目中心；项目搜索与 active 项目列表只在项目中心呈现，工作台内不复制第二套跨项目选择器。项目中心搜索结果分行显示项目名、负责人、更新时间和 `active` 状态，打开后以所选 `projectId` 进入该项目的字幕验收。
- 项目内会话选择器位于项目上下文条右端，默认显示“当前可写 · 草稿修订”；展开后先列唯一可写会话，再列 released/stale 历史会话及来源版本、时间和只读原因。选择历史会话后，页面标题、来源摘要与会话选择器同时显示“历史只读”，所有写控件原生禁用；恢复可写会话不新建或覆盖历史。

#### 5.8.2 编辑、选择与快捷键

- 单击 Cue 同步选择时间轴块、右侧行并定位播放头；`Ctrl` 切换离散多选，`Shift` 扩展连续范围。复制、剪切、粘贴保留相对时间与原轨；跨轨粘贴必须先明确选择目标轨道。
- 拖动 Cue 主体保持时长并整体移动；左右边缘分别修改入点和出点。拖动过程中视频与叠字跟随预览，松手后才形成条件写命令；保存未确认前显示“有未提交修改”。
- 文字、时间、新增、删除、移动、缩放、剪切和粘贴都进入本集撤销栈。切集前必须完成自动保存或明确保留未提交状态；不能因为切集清空服务端已确认编辑。
- `Space` 播放/暂停，方向键逐帧，`J/K/L` 后退 1 秒/暂停/播放，`PageUp/PageDown` 上一/下一可见 Cue，`I/O` 设置入点/出点，`Ctrl+N` 新增，`Delete` 确认删除，`Ctrl+Enter` 应用编辑，`Ctrl+S` 立即保存，`Ctrl+Z/Y` 撤销/恢复。`Ctrl+N` 在台词或画面字筛选下使用当前轨道，在“全部”下先打开紧凑目标轨道选择，绝不静默默认台词。文本框、时间框和问题备注聚焦时，复制、剪切、粘贴、全选、撤销、恢复及空格保持浏览器原生输入语义。
- 删除、跨轨粘贴、返工和发布需要受保护确认；普通文字/时间编辑、选择和定位不弹窗。对话框打开后聚焦安全返回操作，提交中防重复，关闭后焦点回原触发点。
- “删除”确认必须列出选中数量与涉及轨道，并明确“只删除当前验收工作副本，不修改上游 SRT、源视频或不可变历史版本”；完成后保留撤销入口。复制、剪切和粘贴继续保留相对时间与原轨，当前为“全部”或发生跨轨粘贴时先选择目标轨道。
- “记录问题”从问题页打开紧凑对话框，默认带入统一播放头时间，并允许选择问题类型、台词/画面字/当前 Cue 绑定和备注。处理动作必须明确“已解决 / 有理由豁免”并填写可审计理由；定位始终回到问题记录的同一毫秒值。

#### 5.8.3 质量检查、通过与返工

- 技术检查区分 `error` 与 `warning`。`error` 至少覆盖同轨重叠、共用端点、非法时间、越过权威时长、空文字、视频或来源缺失、画面字配对不完整及台词禁止格式；阻断逐集通过和发布。时长/间隔异常等 `warning` 可在填写理由后确认，不得仅靠颜色区分。
- “通过并下一集”先提交当前未保存修改，再由服务端在事务中检查、生成通过签名并定位下一未通过集。提交中按钮锁定；未知结果保留完整请求标识和唯一“恢复本次通过结果”，不能改为再次通过。
- “整剧预检”是一次同步、只读的 `GET /preflight`，不是后台任务。打开右侧抽屉后立即显示保持抽屉几何的 loading；成功后按集显示可通过、警告需理由、硬错误、来源失效和证据缺失；失败时保留服务端原因与 requestId，抽屉内只提供“重新读取预检”和“关闭”，人工重新读取期间防重复，再次失败焦点回唯一重读，成功焦点回结果标题。不存在预检进度轮询、百分比或取消命令。
- “一键通过可通过集”只处理最近一次成功预检由服务端返回的可通过集合。硬错误集始终需逐集处理；预检 loading、失败或防御性 `preflighting` 状态下不展示一键通过入口。
- 局部问题留在当前会话修正；整集或批量问题使用“发起返工”，明确选择集数、台词/画面字轨和原因。返工成功后旧会话只读保留；上游完成后只能从新来源创建新验收会话。
- 只有服务端 `ready_to_release` 才展示“前往交付确认”；员工不再单独执行或理解“冻结验收版本”。交付确认页列出会话修订、来源版本、通过/阻断集数和三类文件摘要；最终生成命令在后台原子固定不可变 `AcceptanceRelease` 与交付产品，浏览器不得推算资格或串联两个创建请求。

#### 5.8.4 状态、恢复与来源真实性

- 页面必须覆盖 `draft / preflighting / ready_to_release / released / stale / blocked` 会话状态，以及 `not_started / in_review / changes_pending / blocked / rework_required / passed` 逐集状态；前端只投影服务端事实。首版不会主动进入或启动 `preflighting`：若读取会话时真实返回该状态，只作为防御性只读映射显示“服务端正在核对验收结果”，并仅允许“重新读取预检”或关闭抽屉；不得据此虚构后台任务、进度、百分比、轮询或取消能力，也不得重复启动预检。
- 加载使用保持三栏几何的骨架；无会话时解释来源要求并只提供“新建验收会话”；无剧集、无台词、未知视频时长、媒体授权失败和媒体解码失败分别给出原因与唯一恢复动作。
- 自动保存反馈明确区分“保存中”“已保存”“有未提交修改”“保存失败”。失败必须保留本地未提交内容、失败原因、请求标识和唯一人工恢复；恢复中禁用重复提交，再次失败焦点回恢复按钮，成功焦点回原编辑上下文。
- `stale` 顶部显示被替换的来源与新来源摘要，整页写动作锁定，唯一主动作变为“从新来源新建会话”；`blocked` 显示服务端阻塞原因和对应恢复入口；已形成交付产品的会话只读展示后台验收快照和对应产品详情入口。
- 刷新后必须恢复权威会话、当前来源、逐集状态、Cue 工作副本、问题与已保存视频选择；不得偷偷改回最新来源，也不得把浏览器缓存当作验收完成事实。
- 项目中心是跨项目选择的唯一入口；页面项目上下文必须提供“返回项目中心 / 切换项目”。进入任意 active 云端项目后读取该项目会话列表，优先恢复唯一可写会话，并可切换到 released/stale 历史会话只读查看。尚未导入云端的本地旧项目只能先走项目创建、素材确认与上传，S5 不直接读取员工电脑上的本地项目库。
- 无可写会话或用户从新来源继续时，“新建验收会话”必须先展示并固定明确的 S3 台词 Release 与可选 S4 画面字 Release。默认可以指向最新有效来源，但用户能选择已有版本或不绑定画面字；创建后的会话刷新时仍读取冻结来源，不以浏览器当前最新值替换。
- 代理媒体首轮如未实现，忠实显示“流畅/高清代理未生成”，并使用当前可播放 Asset；不得伪造 360p/1080p 可用。S5-MEDIA 后续接入后仍保持当前集优先、失败回退和不改原 Asset。

#### 5.8.5 视觉、响应式与可访问性

- 延续冷灰画布、白色工作面与全局 `#4458D8` 主强调，具体角色值以 5.8.0 为准。生产实现只有明亮工作面，不提供 B/C、深色或用户自定义配色分支。
- 字幕正文 `13px/1.45`，元数据与时间码 `12px` 并使用等宽数字特性；控件最小高度 `32px`，主操作 `38px`。编辑工作台减少大圆角容器，主要分区用边界线建立层级；播放器与时间轴的默认纵向分配保持 `54% / 46%`，用户可缩放时间轴内容，但不得把布局模式改成运行时偏好。
- `1440×900` 使用展开 AppShell `204px`：主内容可用宽度约 `1236px`，工作面左/右栏固定 `220px / 326px`，中央吸收约 `690px`；`1024×768` 默认使用收起 AppShell `72px`：左/右栏收为 `182px / 270px`，中央吸收约 `500px`。时间轴只在自身区域横向滚动，根页面宽度必须等于视口；低于当前承诺的电脑屏幕宽度时，右栏才变为由明确按钮打开的覆盖式抽屉，不能直接隐藏编辑能力。
- Rev 4 仓库外方案 A 定向证据在不含 AppShell 侧栏的 `1440×900` 画布实测为 `220 / 894 / 326`、播放器/时间轴 `410 / 349`，在 `1024×768` 画布实测为 `182 / 572 / 270`、播放器/时间轴 `339 / 288`，两者根 `scrollWidth=clientWidth`；1024 时间轴仅自身横滚。正式前端须在真实 AppShell 内按上一条公式复验，不得把证据原型中央宽度硬编码进生产 CSS。
- 所有可操作元素有清晰 `:focus-visible`；轨道块可通过键盘选择和定位。抽屉/对话框形成焦点闭环，`Escape` 仅在非提交中关闭，关闭后回触发点。错误出现不得无条件抢走活动对话框中的焦点。

唯一生产交接与完整验收矩阵见 `docs/ui/M3-subtitle-acceptance.md`。Rev 4 方案 A 最终证据为仓库外 `ui-m3-05-rev3-customizer/A-rev4-1440.png` 与 `A-rev4-1024.png`；同目录 `index.html?final=a` 是七条功能等价路径的唯一可操作交接原型。Rev 3 的 `A-final-*` 只保留此前视觉冻结证据，候选切换页及其中 B/C 只解释选择历史，不构成生产入口。所有匿名示例仅用于解释布局与交互，不构成后端字段、数量或成功状态。

### 5.9 交付产品库

交付采用“完整确认页 → 生成状态与恢复 → 生成成功转场 → 产品详情 / 全局产品库”单一连续结构，权威流程与数据边界见 `docs/contracts/M3-delivery-product-workflow.md` 和 `INT-08`。员工不返回本地软件，也不在字幕验收页直接导出；员工界面统一说明“生成后文件不可修改；需修改请创建修订”，不暴露 freeze、AcceptanceRelease、通过签名等后台术语。`/deliveries` 是全局一级模块，项目二级导航不得复制第二套产品列表。

#### 5.9.1 完整交付确认页

- 路由为 `/projects/:projectId/deliveries/confirm?sessionId=...`，是独立完整页面，不使用侧抽屉。顶部保留项目上下文和“返回继续修改”，唯一主操作为“生成交付产品”。
- 页面在 AppShell 内使用最大约 `1160px` 的居中工作面，按“产品身份与负责人 → 来源版本 → 三类文件摘要与逐集清单 → 生成确认”纵向组织。一个页面只保留一个标题；项目上下文条同时显示会话修订、通过集数、负责人和返回路径，不再重复大标题或营销说明。
- 产品身份区在常规桌面为双列：产品名预览、负责人、可编辑备注、通过/阻断集数。产品名由服务端预览为 `剧名-Vn`，负责人读取项目事实；无账号阶段不得伪造成员管理器。来源区展示验收会话修订、S3/S4 Release、视频清单、术语与模板版本及有效状态。
- 三类摘要分别显示台词 SRT 的集数/Cue 数，画面字 SRT 的集数/非空集数/Cue 数，以及术语 XLSX 的术语数/模板版本/五列表头。确认页只核对，不编辑字幕、画面字或术语正文。
- 摘要下方使用逐集配对表，每一行同时出现该集台词 SRT、画面字 SRT 和验收事实。每集始终生成台词和画面字两个 SRT；某集或整剧没有画面字时，仍生成 UTF-8 BOM 合法空 SRT，并以琥珀语义标签明确写“空轨文件 / 0 条”，不能表现为遗漏、禁用或生成失败。
- 页面说明使用“确认来源版本和文件清单”，来源区只展示员工可识别的版本、会话和有效状态，不显示通过签名。底部粘性操作条只保留次操作“返回继续修改”和唯一主操作“生成交付产品”，并说明“生成后文件不可修改；需修改请创建修订”。生成前使用受保护确认对话框，初始焦点落在“安全返回”；焦点循环必须按当前动作索引显式前进/后退，确保两个动作间 `Tab / Shift+Tab` 双向闭环，非提交 `Escape` 关闭并回到原触发按钮。

#### 5.9.2 生成状态与成功转场

- 生成命令预生成稳定 `deliveryId` 并使用完整请求体与幂等键；提交中禁止重复操作，网络未知只查询同一 `deliveryId`，不得创建第二个版本。
- 生成状态使用最大约 `720px` 的居中工作面。`preparing` 只显示服务端真实阶段的有序清单，不伪造百分比、预计时间或客户端进度；员工可见阶段写“确认验收内容与来源版本”，不显示 freeze 或 AcceptanceRelease。提交后不提供取消创建，避免把尚不存在的后台能力画进页面。
- 网络未知保留同一 `deliveryId`，唯一动作“查询本次生成结果”只读取该产品。`generation_failed` 保留确认内容、具体失败原因、requestId 与同一 `deliveryId`；生成问题的唯一恢复为“恢复本次生成”，继续同一产品和文件清单。来源失效则明确返回验收，不用重试掩盖失效。
- 成功后进入完整成功状态，初始焦点落在成功标题，展示产品名与三类文件完成事实。主按钮为“立即进入产品详情”，同时提供可取消的短暂自动进入倒计时；倒计时更新只用 `aria-live` 文本或等价可读反馈，不得改变当前键盘焦点。取消后用户仍可留在成功页自行进入。

#### 5.9.3 全局交付产品库

- `/deliveries` 是与项目中心、任务中心同级的全局模块，采用成熟数据表，不使用卡片墙。确认、生成中、未知、生成失败、生成成功、产品库和产品详情整条 S7 路径都高亮全局“交付产品库”，并为该导航项设置 `aria-current="page"`；项目面包屑和“返回继续修改”只表达来源与返回路径，不改变全局模块归属。字段固定为产品名称、项目、版本、状态、文件摘要、负责人、备注摘要、创建时间、更新时间和操作。
- 列表工作面在 AppShell 内尽量使用全部可用宽度；工具条依次为搜索、状态筛选、创建/更新时间排序和“查询”。搜索产品名/项目名/负责人/备注，按状态筛选，按创建时间或更新时间排序，全部由服务端分页。分页显示总数和当前稳定范围；行级主操作只有“查看详情”，下载、修改备注和回收等低频动作进入详情或三点菜单。
- 列表必须覆盖保持表格几何的 `loading`、无匹配结果的 `empty`、保留请求标识与唯一重新读取的 `error`，以及 `preparing / ready / generation_failed / recycled` 状态。状态使用文字、圆点和语义色共同表达，不允许只靠颜色。
- `updatedAt` 只因负责人、备注或未来外部上传状态等元数据变化而更新，不能暗示不可变文件已被改写。文件内容变更必须通过“创建修订”形成 `剧名-V2`。
- `/deliveries/:deliveryId` 在 1440 使用“文件与事件主栏 + 产品信息侧栏”，展示产品身份、生成状态、三类文件树、单独下载/下载全部、来源版本、负责人、备注、创建/更新时间、requestId 和历史事件。文件树按台词字幕、画面字字幕、术语表三组展开；空画面字文件继续逐行标记“空轨 / 0 条”。历史事件使用“确认验收内容与来源”，不显示 AcceptanceRelease。产品内容只读，不在产品库重新编辑或运行识别；“创建修订”明确返回验收流程并保留旧产品。
- 首版不自动上传甲方总库、不增加管理员批准门、不实现跨产品批量回收；这些能力待管理员站和外部接口另立任务。

#### 5.9.4 视觉、响应式与唯一交接

- 延续 AppShell、冷灰画布、白色工作面、`#4458D8` 主强调、`#3549BD` 悬停、`#E7E7EE` 边界、`#20202A` 主文字和 `#747481` 次文字。主要区域以边界和紧凑表格建立层级；只有对话框使用明显阴影。不得改成卡片墙、营销页、渐变、装饰主题或管理员配置墙。
- `1440×900` 使用展开 AppShell `204px`，确认/详情主内容最大约 `1160px`；产品库使用约 `1236px` 主区。`1024×768` 使用收起 AppShell `72px`：主区约 `952px`，确认页身份仍保持可读双列、来源改为单列；详情主/侧栏约 `589px / 303px`。产品库表格在自身容器横向滚动，实测容器/表格约 `914px / 1040px`，根页面不得横溢。
- 所有可操作元素使用靛蓝可见焦点；确认对话框、失败恢复和自动转场遵循 5.9.1/5.9.2 的焦点门。SVG 图标使用同一线宽，滚动条与数字排版纳入令牌；不得以 Unicode 符号代替图标系统。
- UI-M3-07 Rev 2 唯一可操作交接原型为仓库外 `ui-m3-07-delivery-product/index.html`；页面右上“原型页面切换”和状态演示按钮只用于验收，不进入生产。1440/1024 最终截图、关键状态与焦点证据见 `docs/ui/M3-delivery-product-acceptance.md`。匿名产品名、数量、稳定短 ID 和 requestId 只解释布局与恢复；同页短 ID 必须逐行唯一，均不构成共享契约或真实业务事实。

### 5.10 设置页

用于模型、存储、队列、规则、公司稿/ASR 默认基准和未来权限设置。设置按业务域分组，不允许把所有配置堆进一个超长表单。

普通员工设置页不提供“学习库”入口。未来管理员维护页只展示去重候选、证据统计、批准状态、回归结果和发布版本，不直接暴露其他项目的完整字幕正文；当前无账号阶段不注册该路由，也不提供普通业务读取接口。

### 5.11 系统控制台 ControlShell

系统控制台是独立管理员产品，不复用或嵌入员工 `AppShell`。唯一视觉方向为成熟企业控制台“日间控制舱”：冷灰白画布、深石墨蓝侧栏与系统运行带、`#4458D8` 主要动作；科技感由真实运行状态、稳定身份和精确数据建立，不使用霓虹、玻璃拟态、粒子、营销大屏、卡片墙或第二套运行时主题。

#### 5.11.1 ControlShell 与全局信息架构

- 侧栏一级入口固定为：系统总览；引擎与 API；路由与调度；服务器与运行环境；策略与学习；预算与限额；日志与质量；密钥与安全；变更与审计。当前项同时用背景、图标、文字和 `aria-current="page"` 表达。
- 顶栏固定承载环境切换、系统整体状态、立即刷新与最近刷新时间、自动刷新、待发布变化、全局搜索、告警和管理员身份；这些对象未来均读取服务端事实，浏览器不推算整体健康、费用或待发布数量。
- `1440×900` 默认展开 `216px` 侧栏并可收至 `72px`；`1024×768` 默认 `72px`，可临时覆盖展开至 `216px`，不挤压主工作面。两种视口根页面不得横向溢出；宽表只在自己的容器内滚动。
- 全局只允许一个“示例数据”标识覆盖原型中的全部匿名值。生产构建不得带入示例数值、示例成功状态、示例 requestId、伪造监控曲线或浏览器常量。

#### 5.11.2 系统总览

- 管理员首页第一屏只回答四件事：现在是否正常、最近处理多少、当前失败或需处理的原因、唯一下一步。阅读顺序固定为“页面标题与最近读取 → 单一总状态/一句解释/唯一下一步 → 一行处理量摘要 → 最多 3 条当前需处理原因”。资源池、趋势、配置版本、预算、服务端窗口、requestId、provider/adapter/model/deployment 等技术身份不再主导首页第一屏。
- `/api/system-control/overview` 的 `overallStatus` 是总状态的唯一权威，浏览器不得再按四个资源池、失败数量或内部状态名二次汇总。首次读取失败或已有事实变 stale 时，首页统一显示“状态暂时无法确认”和唯一“重新读取”；保留旧事实也必须标明已过期。响应中的 `freshness=unknown`、缺失趋势、费用待对账或遥测未知均显示“未知/待对账”，不得补零。

| 权威状态 | 首页总状态 | 一句解释 | 唯一下一步 |
| --- | --- | --- | --- |
| `healthy` | 系统运行正常 | 当前处理链路可用，没有需要管理员介入的系统问题。 | 无需处理 |
| `degraded` | 有事项需要处理 | 当前工作仍可继续，但存在服务端确认的待处理事项。 | 处理当前问题 |
| `failed` | 当前处理受阻 | 当前处理链路存在已确认故障，至少一项工作无法继续。 | 处理当前问题 |
| `empty` | 当前没有处理任务 | 当前没有正在执行或等待系统处理的任务。 | 等待新任务 |
| `unknown` | 状态暂时无法确认 | 服务端缺少判断整体状态所需的当前事实。 | 重新读取 |
| `not_configured` | 关键能力尚未接入 | 当前工作所必需的处理能力尚未完成配置。 | 查看接入详情 |

- 服务端生成 `overallStatus` 时只使用当前、仍可行动的权威事实：`review_pending` 属于“待人工处理”，不计入运行中或系统失败；已经结束的 `localPolicyBlocked`、历史失败和历史过期租约只进入日志/趋势，不改变当前总状态；没有任务的空闲 Worker 是正常空闲，不是失败；可选资源池未配置、遥测未知或费用待对账也不能单独把整体判为失败。`failed` 仅用于当前处理链路已确认受阻，`degraded` 仅用于当前仍可恢复或需人工处理的事项。若 `failed/degraded` 与当前原因列表矛盾，首页降为“状态暂时无法确认”，不得由浏览器猜原因。
- 一行处理量摘要固定使用 overview 现有指标表达“最近 24 小时处理 {processedProjectCount} 个项目 / {processedEpisodeCount} 集 · 正在处理 {runningCount} · 等待系统处理 {queuedCount}”。只有 `freshness=fresh/empty` 才显示数值；unknown/stale 显示“未知”。现有 `anomalies` 没有总数合同，不得用数组长度冒充全量；`review_pending` 在没有专用权威字段前也不得塞入 `runningCount`、`queuedCount` 或失败数，完整工作量进入既有任务详情。
- 当前原因只消费服务端返回的当前 `anomalies`，按严重度和时间取最多 3 条，并统一为业务中文：“当前任务处理失败 / 处理结果需要确认 / 当前任务等待恢复”；不在首页显示资源池内部名、reasonCode、requestId 或技术身份。历史、已恢复和仅用于 24 小时统计的记录不进入该列表；没有当前原因时显示“当前没有需要管理员处理的问题”。
- 技术能力不删除，只做渐进披露：引擎和版本进入“引擎与 API/路由与调度/变更与审计”，资源池与 Worker 进入“服务器与运行环境”，趋势、历史失败、requestId 与执行身份进入“日志与质量”，费用进入“预算与限额”。首页的唯一下一步按当前状态深链上述既有入口，不新建页面、控制面、第二设计系统或浏览器状态源。
- 单一总状态容器使用 `role=status`，总状态、解释和下一步形成同一焦点语境；当前原因使用一个标题与紧凑列表，不为每条原因制造竞争主按钮。状态同时使用文字与图形，不只靠颜色；`1440×900` 与 `1024×768` 的第一屏都必须在无需横向滚动的情况下完整看到总状态、处理量和至少首条原因。

#### 5.11.3 详情方向与数据边界

- **引擎与 API**：登记供应商适配器、模型、语言和能力，展示连接状态、当前角色、配置版本和最近测试。连接测试、匿名样本测试和生产发布是三个不同动作；不回显 Secret，不把测试成功自动视为发布成功。
- **路由与调度**：按 ASR API、OCR API、OCR 自建 Worker、交付生成分别管理不可变路由/限额版本和 active 指针。暂停只阻止领取新任务，不终止运行中的 Attempt，不删除队列，不在未知结果时自动重复付费请求。
- **日志与质量 / 用量**：按项目、任务、领域、日期、状态和 requestId 查询 Attempt/Usage 与最终对账事实；允许进入领域详情，首版不执行文件导出，也不保存原始视频、完整字幕、提示词、热词、明文密钥或供应商原始载荷。
- **服务器与运行环境**：展示环境、应用、Worker、数据库、对象存储和受控遥测摘要；本页只读诊断并深链唯一控制所有者，不复制暂停、并发、扩缩容或恢复命令。首版不提供裸 SSH、任意命令、删除服务器、购买资源或防火墙配置。
- **高风险配置发布**：唯一链路为“草稿 → 测试 → 影响检查 → 确认发布 → 可回滚”。版本不可变，成功前旧 active 指针持续生效；提交中禁止重复命令，失败保留原因/requestId，未知结果只查询同一幂等发布命令，回滚形成新审计事实而不删除失败版本。
- **策略与学习、预算与限额、密钥与安全、变更与审计** 保持一级正式页面，不得从目标范围删除。策略只消费 INT-14 的不可变人工事件并经离线评测/批准/发布；预算区分提醒与硬阻断；密钥只展示引用及可用状态；审计只追加不覆盖。
- 设计交接与验收文档必须逐页列出“未来真实数据源 / 允许操作 / 非目标”，供规划、前后端和测试核对边界；这些是生产不可见的工程注释，不得作为管理员页面说明卡、页尾备注或帮助文案渲染。INT-11 负责 active 配置到 Worker 的读取边界，INT-12 提供 Attempt/Usage/运行环境摘要，INT-14 提供人工事件到策略学习的证据；所有权威数字、状态和命令来自 PostgreSQL、监控摘要或领域命令。

#### 5.11.4 状态、刷新与安全操作

- 页面统一覆盖 `loading / empty / error / unknown / stale / disabled`。错误保留原因、requestId 与唯一重新读取；未知不按零计算；过期数据不得用于暂停或发布；未启用使用文字和中性状态标识，不能只靠颜色。
- 自动刷新关闭后保留最后一次服务端摘要并明确标记；执行任何管理动作前仍需重新校验版本。资源池暂停必须显示“不接收新任务”和唯一恢复入口，运行中任务与历史事实不变。
- 费用未知统一显示“待对账”，不填充推算金额。异常恢复与配置发布的提交中、失败和未知结果都必须防重复；唯一恢复动作出现后获得安全焦点。
- 高风险对话框打开后初始焦点落在“安全返回”，`Tab / Shift+Tab` 在安全返回与确认动作间双向闭环；非提交中 `Escape` 或遮罩关闭并回原触发点。提交中不可关闭或重复激活；成功/未知/失败焦点分别落到明确标题或唯一下一动作。

唯一可操作设计交接为仓库外 `ui-system-01-control-shell/index.html`，验收矩阵和证据见 `docs/ui/SYSTEM-control-shell-acceptance.md`。最终预览不显示逐页工程注释卡；原型只定义信息结构、交互语义、响应式和未来数据边界，不构成管理 API、生产常量或已实现能力。

### 5.12 引擎/API 与安全配置发布

UI-SYSTEM-03 Rev 2 在 5.11 的同一 `ControlShell` 内冻结 `/engines`、`/routing`和 `/changes`。Rev 1 的“日间控制舱”视觉、双视口响应式、宽表内滚、发布/回滚焦点链和发布未知恢复均保持不变；本次只收口功能等价交互与数据所有权。不建立 B/C 方向、运行时主题或第二套管理壳，不复用、嵌入或混合员工 `AppShell`。该切片只定义本地零网络控制平面的信息结构、状态、交互、安全边界与工程交接，不代表真实供应商、网络、密钥、付费调用或部署已接入。

#### 5.12.1 `/engines` 引擎部署与连接测试

- 列表只投影服务端真实 `EngineDeployment`，并允许按“中文语音识别 / 画面字识别”能力和部署状态筛选。`enabled / disabled` 在界面中固定投影为“可用于草稿 / 已停用”；主部署、备用部署与无部署空位不属于部署身份，只能在 `/routing` 表达。桌面紧凑表格展示稳定部署身份、能力、执行方式、状态、版本与最近连接测试；管理员顶层不显示 `configDigest`、`adapterKey` 等实现名。
- 部署页可只读显示“当前使用位置”，但数据只来自当前生效路由；没有 Wave B 路由事实时显示“尚未分配”。“无部署”只允许作为空列表或路由槽位，不创建带部署 ID 的占位记录。“执行组件未注册”只用于后端 Registry 与数据库部署事实发生漂移后的阻断状态。
- “登记部署”打开同壳受保护表单：管理员填写 capability、executionKind、displayName、后端 Registry 可选执行组件、model/language，以及受控 region/endpointReference；provider、能力快照等 Registry 派生字段只由服务端确定。客户端预生成稳定 deploymentId/versionId 作为隐藏命令身份，不呈现为可编辑字段。
- 版本详情是不可变的只读事实，包含模型/语言摘要、能力快照、凭据引用脱敏摘要、不可变版本历史和有界最近测试历史；`1024` 用约 `430px` 右侧抽屉承载详情，不在宽表中删字段或转成卡片墙。详情必须提供“创建新版本”，但不允许就地修改旧版本。
- 连接测试与生产发布在视觉和语义上完全分离。首版只能运行后端已登记的零网络 probe；“零网络测试通过”只证明本地执行路径通过，不得投影为供应商可用或自动发布成功。
- 未受权的真实供应商、网络能力或付费连接统一显示“尚未接入”，不用假成功、模拟延迟或匿名厂商卡表现为已就绪。
- 最近测试由服务端按状态筛选、完成时间排序和稳定分页；刷新后从 PostgreSQL 重新发现。`queued / running` 禁止重复创建；未知结果只读取同一 `testRunId`；失败在修正前置条件后只允许“创建新的零网络测试”并预生成新的 `testRunId`，不得写成重跑同一次。完整覆盖无部署、凭据未绑定、执行组件未注册、测试排队中/运行中/失败/结果未知/成功（仅零网络），以及数据 loading/empty/error/stale/历史只读。
- 登记部署和创建版本均覆盖提交中锁定、确定失败、响应未知、版本冲突与成功。未知只查询同一预生成 deploymentId/versionId，不重新发送创建；失败/未知/冲突显示唯一恢复动作并获得焦点，成功标题获得焦点。登记部署中的模型/语言只允许在选定 Registry 执行组件后作为只读派生预览，浏览器不得编辑、提交或覆盖；生产请求严格服从共享 create body，provider/model/language/capabilities 均由服务端 Registry 派生。

#### 5.12.2 `/routing` 业务环节、资源池与容量草稿

- 页面先按“中文语音识别”（`asr`）与“画面字识别”（`screen_text`）分组；资源池固定拆为 ASR API、OCR API 和 OCR 自建 Worker，OCR API 与 OCR 自建 Worker 不共用容量、主备或排队状态。
- 每池显示主部署、可选备用部署和明确的“无部署”空位。ASR 备用位不得因当前无备用而被删除；自动备用只能处理明确未受理、零付费副作用且契约允许安全降级的失败，未知或需对账不自动切换。
- 容量首版只展示“同时处理上限”、“单项目同时处理上限”和“等待队列上限”，分别是 `maxConcurrentJobs / perProjectMax / queueLimit` 的中文投影。管理员页面不直接显示这三个实现字段名。
- “当前生效”与“草稿”必须并列展示，让管理员同时核对主备、容量、版本和差异；保存草稿不改变当前生效。预算、费率/速率限制、计费阈值与服务器/Worker 扩缩容均不进入本切片。
- “新建路由草稿”进入真实编辑态：按 `asr / screen_text` 与 ASR API、OCR API、OCR 自建 Worker 分池，主备只选择精确的、可用于草稿的 deploymentVersion，备用位保留“无部署”。三项容量均提供输入范围与关联校验；客户端预生成稳定 draftId 作为隐藏身份。
- 保存生成不可变草稿且不移动当前生效指针。确定失败保留输入和原因；网络未知只查询同一 draftId；部署版本或成员发生冲突时唯一恢复为刷新服务端可选版本；保存成功标题获焦并进入影响检查。未知、冲突和恢复均不得重发第二个草稿身份。

#### 5.12.3 `/changes` 影响检查、发布与回滚

- 不可变配置版本的唯一状态链为 `draft → testing → impact_checked → approved → active / retired`；管理员顶层只显示“草稿→测试中→影响已检查→已批准→当前生效/已退役”的中文投影。发布只能从已批准且版本未变的同一发布身份进入。
- 影响检查必须展示受影响业务环节、活动任务、需对账任务、未通过连接测试与容量变化。任一阻断存在时不展示发布主操作，唯一恢复是“返回修正草稿”并获得焦点；阻断文案不得表现为普通警告或可强制绕过。
- 确认、提交中、失败、未知、成功和回滚是六个不同状态。提交中锁定对话框和重复命令；失败保留原因和诊断身份；未知只提供“查询本次发布结果”并只读取同一发布身份，不创建第二命令。失败和未知时旧“当前生效”始终不变；成功后焦点落在成功标题。
- 回滚是一次新的、可审计的发布，指向历史已批准版本；不删除失败版本、不改写历史事件。发布成功只影响之后新建任务：新建 Job/Attempt 固定实际部署版本与路由版本，运行中、已完成、需对账及所有历史任务不改写、不自动重试。
- 发布、回滚、失败和未知查询都追加审计事件；审计只追加不覆盖。`requestId` 仅在诊断区、错误/未知恢复和审计详情中显示，不进入顶栏搜索文案、页面标题、主表第一层或操作按钮。

#### 5.12.4 Secret、可执行边界与管理员表达

- Secret 只作为服务器端已存在的引用绑定和脱敏可用状态出现；界面不提供明文输入、粘贴、回显、复制或下载，也不返回引用原文、环境变量值、令牌、连接串或供应商私参。
- 创建新版本时 Secret 选择固定为四种业务语义：“不需要”“继承上一版本”“替换为服务器已有别名”“明确清除”。当“密钥与安全”的服务端数据源尚未实现时，“替换”必须禁用并提供“前往密钥与安全”；不得用任意 `referenceId` 文本框、匿名假选项或浏览器本地值代替。
- 不提供任意 URL、任意 Header、脚本、自定义请求体或任意命令；数据库不能登记任意可执行代码，执行能力只能来自后端启动时已注册的 Adapter Registry。
- 稳定部署/版本/测试/发布短身份可用于定位，但管理员顶层文案使用“当前生效、执行记录、零网络测试、影响已检查”等中文业务投影，不把 `active`、`Attempt`、`configDigest`、`adapterKey` 等实现名作为主界面术语。

#### 5.12.5 布局、宽表与受保护焦点

- `1440×900` 使用展开 `216px` 侧栏，`/engines`和 `/routing` 保留详细表格与并列事实，`/changes` 同屏展示阶段、差异、影响与当前生效；页面根不得横向溢出。
- `1024×768` 默认使用 `72px` 侧栏；临时展开至 `216px` 时只覆盖主工作面，不挤压或重排主区。引擎版本详情使用约 `430px` 右侧抽屉；最小宽度 `1080px` 的引擎/路由表格只在自身容器内 `overflow-x:auto`，页面根宽度仍等于视口。
- 发布和回滚对话框打开后，初始焦点落在“安全返回”；`Tab / Shift+Tab` 在安全返回与确认动作间双向闭环。非提交中 `Escape` 可关闭并将焦点返回原触发点。
- 提交中对话框设置 `aria-busy="true"`，安全返回和确认都锁定，焦点继续保留在受保护对话框内，`Tab` 不逃出，`Escape` 不关闭。失败/未知只显示与当前状态对应的唯一恢复动作并获得焦点；成功标题获得焦点，不自动跳走。
- 登记部署、创建新版本和路由草稿编辑复用同一受保护焦点规则；表单中的可用控件与底部动作组成显式正反向焦点循环，非提交中 `Escape` 返回原触发点。`1024` 对话框保持在剩余主工作面内，版本/测试抽屉与宽表横滚互不扩大页面根宽。

#### 5.12.6 逐页工程交接与职责映射

| 页面 | 未来真实数据源 | 允许操作 | 非目标 |
| --- | --- | --- | --- |
| `/engines` | PostgreSQL 真实 `EngineDeployment`/不可变版本、Adapter Registry 能力快照、Secret 引用元数据、持久化连接测试执行记录 | 服务端读取与筛选、登记部署、查看版本/测试历史、创建不可变版本、选择受控 Secret 语义、创建新零网络测试、查询同一测试或创建命令 | 路由主备/空位所有权，真实供应商/SDK/网络/付费，明文 Secret，任意 URL/Header/脚本/引用 ID，测试自动发布 |
| `/routing` | PostgreSQL 不可变路由/容量版本、唯一当前生效指针、精确部署版本可用状态与排队/领取事实 | 按业务环节和资源池查看当前生效/草稿，选择精确主备版本或无部署，校验三项容量，保存同一身份的不可变草稿，进入影响检查 | 创建伪部署，预算、费率/速率、服务器扩缩容，终止运行任务，未知/需对账自动切换或重试 |
| `/changes` | PostgreSQL 配置版本、测试执行记录、影响检查、幂等发布/回滚命令、当前生效指针与只追加审计事件 | 测试、影响检查、批准、确认发布、查询同一未知发布、指向历史已批准版本回滚、查看审计 | 浏览器判成功或持有当前生效，失败/未知改指针，删除失败版本/审计，改写历史任务 |

上表仅是规划、前后端和测试使用的工程交接注释，在生产 UI 与最终预览中必须完全不可见。职责矩阵唯一对应行为“系统控制台引擎/API 与安全配置发布”：`INT-11` 负责当前生效配置到 ASR/OCR Worker 与新任务固定版本；`INT-12` 将连接测试、Job/Attempt/Usage 与运行摘要只读投影到控制台；`INT-13` 继续限定员工站、控制台与 Worker 的不同权限域，真实 Cloudflare Access/JWT 和部署仍属后续授权门。

UI-SYSTEM-03 Rev 2 唯一可操作交接继续使用仓库外 `C:\Users\ComradeGu\.codex\visualizations\2026\08\15\01a00445-14ec-77c0-9fda-595190220af9\ui-system-01-control-shell\index.html`。Rev 1 的十张视觉/响应式/发布证据冻结在 `evidence/ui-system-03-rev1/`；本轮变化场景证据位于 `evidence/ui-system-03-rev2/`，完整结论见 `docs/ui/SYSTEM-engine-control-acceptance.md` §12。原型场景入口、匿名数值和成功状态只用于设计验收，生产不得保留。

### 5.13 预算、限额与付费调用安全门

UI-SYSTEM-04 Rev 1 在 5.11–5.12 唯一 `ControlShell` 内冻结 `/budgets`。页面继续使用“日间控制舱”的冷灰白画布、深石墨蓝侧栏与 `#4458D8` 主动作，不建立第二视觉方向、主题切换或员工入口。本切片只定义预算安全门的信息、状态与受保护交互，不代表真实供应商、Secret、外部网络、付费调用、云资源或部署已经接入。

#### 5.13.1 总览与账本

- 页首必须同时显示目标环境、当前生效策略版本、服务端数据新鲜度和 PostgreSQL 权威金额来源；缺少当前生效策略时明确阻断新的付费请求，不以本地默认阈值继续。
- 每个资源池与币种独立成行，首版资源池仅为 ASR API 与 OCR API。今日、本月均分列“已结算 / 预留 / 待对账 / 占用合计”，金额使用服务端精确十进制字符串；不做汇率换算，不跨币种相加，不把未知费用补零。
- 提醒阈值命中只提示并允许继续新请求；硬阻断只阻断新的付费请求，不终止运行中请求，也不释放已预留或待对账占用。费用结果未知时继续保留预留并标记“待对账”，直到权威对账结果完成。
- 缺少币种规则时对应行保留币种与阻断原因，金额显示 `—`，唯一安全下一步为补充策略草稿；不得借用其他币种规则或前端推算。并发、每项目并发和队列容量只链接 `/routing`，预算页不复制输入或状态所有权。

#### 5.13.2 不可变策略版本与高风险发布

- 策略链固定为“不可变草稿 → 历史回放 → 影响检查 → 批准 → 当前生效 / 已退役”。保存草稿、回放通过、影响检查通过和批准均不改变当前生效指针；只有发布成功后才切换。
- 草稿按资源池与币种分别编辑日提醒、日硬阻断、月提醒、月硬阻断。提醒必须严格小于对应硬阻断；金额格式、精度、完整币种覆盖与来源版本都由服务端复验。
- 历史回放使用固定服务端快照，显示测试中/失败/未知/成功，不伪造百分比。影响检查必须列出新旧阈值、当前在途预留、将新增提醒、将阻断的新请求和运行中请求保持不变；硬阻断或版本冲突不能被确认弹窗绕过。
- 高风险发布确认清楚对照旧/新阈值、在途占用数量等权威摘要、将阻断的新请求数及“旧 active 在成功前保持”。提交中锁定重复操作；确定失败保留原因与 `requestId`；响应未知只查询同一 `releaseCommandId`，不重新发布。失败或未知时旧策略继续生效，成功标题获焦后才显示新版本。
- 回滚是指向历史已批准版本的一次新发布命令，走同一影响检查、确认、提交中、失败/未知恢复与审计链；不得改写旧版本或历史用量。

#### 5.13.3 读取、状态与焦点

- 读取覆盖 loading、empty、首次 error、再次失败、恢复成功、stale、无 active、缺币种、提醒、硬阻断和待对账。已有事实刷新失败时保留旧账本并标记 stale；错误区只提供一个“重新读取”，再次失败仍保持唯一动作，恢复后清除 stale 并将焦点落到页面标题。
- 草稿保存、回放、影响检查、发布和回滚都预生成稳定命令/版本身份。确定失败清除本次写意图并重新读取权威事实；未知只读取同一身份，不能同时出现通用重试、第二创建动作或浏览器 fallback。
- 高风险对话框打开后初始焦点为“安全返回”，`Tab / Shift+Tab` 双向闭环；非提交中 `Escape`/遮罩关闭并回原触发点。提交中对话框设置 `aria-busy`，锁定 Tab 逃逸、Escape、遮罩和重复提交；失败/未知聚焦唯一恢复动作，成功聚焦明确标题。
- 状态不能只靠颜色：标题、标签、原因、金额保留规则与唯一下一步共同表达。`requestId` 只进入错误、未知恢复、审计或诊断详情，不进入顶层正常状态。

#### 5.13.4 响应式与工程交接

- `1440×900` 保留展开 `216px` 侧栏、紧凑账本、阈值表与版本链并列；`1024×768` 默认 `72px` 侧栏，展开至 `216px` 只覆盖主工作面、不挤主区。账本最小宽度 `1480px`、阈值表 `980px`，仅自身容器 `overflow-x:auto`；页面根始终无横向溢出，不删字段或逐字折行。
- 版本历史使用同壳右侧抽屉；草稿与发布对话框在 1024 剩余主工作面内保持可操作，内部宽表自行横滚。所有原型匿名数字只受全局唯一“示例数据”标识约束，生产禁止硬编码或当后端事实。

| 页面 | 未来真实数据源 | 允许操作 | 非目标 |
| --- | --- | --- | --- |
| `/budgets` 总览 | PostgreSQL 用量、预留、待对账台账，环境、数据库时间、新鲜度、不可变策略版本与唯一 active 指针 | 服务端读取/筛选账本和当前阈值，重新读取失败状态，查看版本历史，进入新草稿 | 浏览器汇总权威金额、跨币种换算、未知补零、复制路由并发/队列、匿名数字进入生产 |
| `/budgets` 草稿与回放 | PostgreSQL 不可变预算版本、固定历史快照、服务端校验与影响检查 | 编辑四类阈值，保存同一预生成版本身份，查询未知结果，运行历史回放，修正冲突 | 改写 active、客户端判定通过、绕过缺币种/版本冲突、伪造回放百分比 |
| `/budgets` 发布与回滚 | PostgreSQL 发布/回滚命令、影响摘要、active 指针、只追加审计与稳定 `requestId` | 批准、确认发布、查询同一未知命令、对历史已批准版本发起回滚 | 失败/未知切换 active，终止运行中请求，释放未知占用，重发第二命令，真实付费/网络/Secret |

上表是规划、前后端和测试使用的生产不可见工程交接注释，不渲染在最终原型或生产管理员页面。职责归属为“系统控制台预算、限额与付费调用安全门”：`INT-11` 只把成功发布的 active 策略提供给 Worker 新请求；`INT-12` 投影 Usage/Attempt、预留、待对账和运行事实；`INT-13` 继续隔离管理员权限；`INT-15` 约束付费副作用前的原子预留、数据库时间与未知结果保留。唯一设计验收见 `docs/ui/SYSTEM-budget-guardrails-acceptance.md`。

### 5.14 密钥与安全引用及供应商接入准备

UI-SYSTEM-05 在 5.11–5.13 唯一 `ControlShell` 内冻结 `/security`。本页面管理的是服务器端安全引用、不可变引用版本、零网络验证和使用关系，不是 Secret 录入器；浏览器、管理 API、日志和审计都不得接收、回显、复制或下载 Secret 值、环境变量值、内部句柄、连接串或请求头。

#### 5.14.1 首屏判断顺序与安全引用表

- 页面首屏顺序固定为“标题与主动作 → 安全接入就绪带 → 读取/命令状态 → 安全引用或服务器候选表”。就绪带只展示当前环境、服务端候选发现、员工站/控制台 Access 就绪摘要、真实供应商连接状态和数据新鲜度；未配置或未知时如实显示，不以本地默认值冒充成功。
- 安全引用主表由服务端完成搜索、状态/能力/来源筛选、更新时间排序与稳定分页。列固定为业务别名、用途、来源类别、状态、当前不可变版本、绑定部署数、最后验证、轮换提示和查看详情；空页仍返回真实总数。
- “发现服务器候选”切换到独立候选表。候选只显示服务器给出的业务别名、环境、能力、来源类别、脱敏摘要、最近发现和登记状态；没有候选时唯一下一步为在服务器安全存储中配置，不出现 Secret 文本框或任意引用 ID 输入。
- 引用状态使用“可用于新版本 / 验证失败 / 结果未知 / 待轮换 / 已撤销”等管理员业务文案。`rotation_due` 只是一项轮换提醒，不自动换绑、不修改旧部署版本，也不阻断仍然有效的既有绑定。

#### 5.14.2 详情、验证与跨模块绑定

- 右侧详情抽屉展示当前状态、不可变版本、脱敏摘要、当前生效路由及部署使用位置、最近验证、只追加审计和轮换/撤销入口。详情读取失败只影响该区域，不清空已加载主表；关闭后焦点回到原表格触发点。
- 零网络验证状态固定为等待、运行中、失败、结果未知和通过。等待/运行中只能刷新同一执行记录；unknown 只查询同一 `validationRunId`；明确失败后可以显式创建新的验证，但不能自动重复执行。只有成功验证且未撤销的引用版本可以用于新的引擎版本。
- `/engines` 创建不可变部署版本时，“替换为服务器已有别名”只读取 `/security` 已验证且环境、能力、来源匹配的引用版本。选择后固定的是不可变引用版本身份；轮换不会改写旧引擎版本，管理员必须创建新引擎版本并重新经过测试、影响检查和发布。
- 安全引用可用不等于允许真实连接或付费调用；真实供应商连接仍需已发布路由、预算安全门和单独的网络/付费授权共同通过。

#### 5.14.3 登记、轮换、撤销与失败恢复

- 登记和轮换复用同一受保护工作流。登记只从服务端候选创建稳定引用及首个不可变版本；轮换只追加新引用版本并保留旧版本、历史任务和审计。两者均预生成稳定资源/命令身份，提交结果未知时只查询同一身份。
- 登记或轮换确定失败时，弹窗保留人工输入，显示稳定原因与 `requestId`，唯一恢复动作是刷新服务器候选；刷新后由管理员重新核对并显式提交新命令，不复用已确定失败的写意图。
- 撤销前必须展示当前生效路由、非终态任务和受影响部署。存在当前路由或非终态任务时事务前后均稳定阻断，并把管理员带到使用位置；不得用确认框绕过。
- 撤销确认打开后初始焦点为“安全返回”，`Tab / Shift+Tab` 在安全返回和确认撤销间双向闭环。提交中 `aria-busy=true`，锁定 Escape、遮罩、重复提交和焦点逃逸。确定失败显示引用仍保持原状态、真实原因、`requestId` 和唯一重新读取；unknown 只查询同一 `revokeCommandId`；成功标题获焦，历史版本和使用记录不删除。
- 主表、候选、详情和工作流均覆盖 loading、empty、首次 error、再次失败、恢复成功、stale、Provider unknown 与 Access 未配置。已有事实刷新失败时保留 stale 只读快照，全部登记、验证、轮换和撤销入口统一锁定；恢复成功后清除 stale 并聚焦页面标题。

#### 5.14.4 响应式、权限与工程交接

- `1440×900` 使用展开 `216px` 侧栏、四列安全就绪带和完整引用宽表；`1024×768` 默认 `72px` 侧栏，展开到 `216px` 只覆盖主区、不挤压正文。安全引用表最小宽度 `1180px`、候选表 `1060px`，只在自身容器横向滚动；详情抽屉约 `440–470px`，页面根始终无横向溢出。
- `/security` 只属于管理员 ControlShell。员工 AppShell、员工路由和员工构建不得出现入口、管理 API 或安全引用事实。首版只消费可注入的 Access 就绪摘要，不提供邮箱成员管理，也不把隐藏网址当权限。

| 页面/区域 | 未来真实数据源 | 允许操作 | 非目标 |
| --- | --- | --- | --- |
| `/security` 引用与候选 | Secret Provider 有界候选摘要；PostgreSQL 稳定引用、不可变版本、验证状态、绑定部署计数与轮换到期事实 | 服务端搜索/筛选/排序/分页、发现候选、登记引用、重新读取 | 明文 Secret、任意引用/URL/Header/脚本、浏览器推算可用性、默认业务 bootstrap |
| 引用详情与验证 | PostgreSQL 引用版本、引擎/路由使用位置、验证 Run/Attempt 与只追加审计 | 查看不可变历史、查询同一验证、显式创建新验证、进入轮换或撤销保护 | 修改旧版本、自动重跑、把零网络成功冒充真实供应商成功、泄露内部 Provider 句柄 |
| 登记/轮换/撤销 | 稳定命令、规范化请求摘要、事务内引用检查、`requestId` 与命令结果 | 查询未知命令、刷新候选、创建轮换版本、受保护撤销 | 响应未知重发写命令、撤销历史、自动换绑引擎、真实网络/付费/云资源/部署 |

上表是规划、前后端和测试使用的生产不可见交接注释。职责归属为“系统控制台密钥与安全引用”，衔接 `INT-11/13/15/16`。正式原型沿用仓库外唯一 `ui-system-01-control-shell/index.html`；匿名场景切换器和示例值只用于设计验收，不进入生产。

### 5.15 服务器、Worker 与运行日志

UI-SYSTEM-06 在 5.11 唯一 `ControlShell` 内冻结 `/servers` 与 `/logs`。两个页面共同负责“发现异常 → 确认影响 → 定位执行记录 → 深链唯一控制所有者”，但不拥有路由、引擎、预算、任务取消/重试或服务器控制状态。

#### 5.15.1 `/servers` 运行环境

- 首屏顺序固定为“标题与只读动作 → 深色运行面摘要 → 运行资源清单 → 三步诊断说明”。运行面摘要并列应用服务、后台 Worker、业务数据库、对象存储和需人工处理数量；Provider 未配置时显示“遥测未配置”，不能把未知补成 0 或“正常”。
- 运行资源表按类型、作用域、健康结论、运行/排队、新鲜度和绑定版本展示服务端事实。资源名称与稳定 ID同时存在；健康状态使用“正常 / 需关注 / 遥测未配置”，不使用模糊的在线绿点代替事实。
- 资源详情使用右侧只读抽屉，展示权威说明、逐项观测指标和最近关联记录。底部动作只允许返回列表、查看执行记录或前往已有系统总览/引擎/路由页面；不得提供暂停、恢复、重启、扩缩容、终端、购买或删除。
- PostgreSQL 领域事实优先于基础设施遥测。CPU/GPU/内存/磁盘/容量只有受控 `RuntimeTelemetryProvider` 返回时才展示数值；无 Provider、超时或部分缺失均逐字段表达 unknown/not_configured。

#### 5.15.2 `/logs` 执行记录

- 顶部摘要显示 24 小时执行记录、失败、需对账、人民币已结算和最早未处理时长；人民币是主显示，外币只允许进入详情审计摘要，未知金额显示“待对账”。
- 列表按项目/任务/requestId 搜索，按领域、状态和日期筛选；服务端稳定分页。表格列为时间、领域/对象、阶段、状态、运行资源、耗时/用量、人民币金额、requestId 和查看详情，宽表只在自身容器横向滚动。
- 详情抽屉显示当前状态、阶段、运行资源、requestId、耗时/用量、人民币金额、诊断结论和时间线。时间线来自领域 Job/Attempt/Usage/事件，不显示 SQL、连接串、对象键、Secret、完整字幕/热词/提示词或供应商原始载荷。
- 每条记录只提供与事实对应的深链：引擎/API、路由/调度、预算/限额或领域任务。页面不提供“重试全部”“自动修复”“清空日志”或前端推算的恢复动作。

#### 5.15.3 状态、焦点与响应式

- 两页必须覆盖 loading、empty、error、再次失败、stale、部分遥测未知和恢复成功。PostgreSQL 读取失败显示真实 requestId 和唯一原查询重读；恢复后聚焦页面标题，详情打开/关闭后焦点在详情标题/原行触发点之间闭环。
- 自动刷新只重读当前查询；关闭后切换筛选、详情或标签不得产生后台请求。查询身份变化必须清除失效详情，迟到响应不得覆盖新身份。
- 1440 保持完整侧栏和宽屏密度；1024 使用 72px 收起侧栏、正文宽度不被覆盖展开挤压。运行摘要改为两至三列，诊断说明下置；资源表和日志表只在 `.table-wrap` 内横滚，根页面无横溢。
- 原型的“示例数据”标识与状态切换器只用于验收，不进入生产。生产所有数字、状态和深链都必须来自 PostgreSQL、受控遥测摘要或共享契约。

#### 5.15.4 生产不可见交接矩阵

| 页面 | 真实数据源 | 允许操作 | 明确非目标 |
| --- | --- | --- | --- |
| `/servers` | RuntimeTelemetryProvider 受控资源目录；领域 Job/Attempt/Usage/lease 有界聚合 | 搜索/筛选/分页、查看资源详情、重新读取、深链日志/引擎/路由/总览 | 暂停/恢复、并发控制、重启、扩缩容、SSH/终端、云资源写入 |
| `/logs` | 各领域 PostgreSQL Job/Attempt/Usage/事件与 system-control 不可变版本事实 | 搜索/筛选/排序/分页、查看详情、原查询重读、深链唯一控制所有者 | 第二日志库、浏览器全文缓存、文件导出、自动重试/修复、Secret/正文/原始载荷 |

本节职责归属“系统控制台服务器与运行环境”和“系统控制台日志与质量”，衔接 `INT-11/12/13/15/17`。正式实现必须消费 SYSTEM-06 共享契约，不得照搬原型匿名数据。

### 5.16 策略与学习中心

UI-SYSTEM-07 沿用 5.11 唯一 `ControlShell`，不另建 AI 驾驶舱或学习系统外壳。页面采用 Operate 模式：管理员先判断当前纳管范围和数据新鲜度，再进入具体资产或事件证据；科技感来自不可变版本、状态链和精确差异，不来自霓虹、模型动画或聊天框。

#### 5.16.1 页面结构与 Wave 边界

- 左侧一级导航“策略与学习”唯一高亮，正式路由为 `/strategy`。页内使用紧凑标签依次承载“总览、本地规则、AI 筛选、提示词模板、热词与风险词、操作日志”；“学习候选、评测与发布”可见但标记“后续波次”，没有可执行按钮、匿名数量或伪进度。
- 总览首屏顺序固定为“标题与数据新鲜度 → 深色纳管摘要带 → 五类资产状态 → 最近人工事件 → 安全链说明”。摘要带只显示服务端事实：已纳管资产、草稿版本、近 24 小时安全事件和异常读取；空库时全部显示“尚未纳管/暂无事件”，不得补匿名示例。
- 五类资产固定为本地规则包、AI 筛选策略、提示词模板、热词投影、风险词库。Wave A 只允许查看、登记资产和创建不可变草稿版本；页面不出现 active、发布、回滚、小流量、优化运行或训练按钮。
- 本地规则、AI 筛选、提示词和词库共享一套服务端搜索/状态筛选/排序/分页表格，但每类使用自己的严格字段摘要。不能用任意 JSON 编辑器替代领域表单，也不能让浏览器保存“当前规则”。

#### 5.16.2 资产与事件证据

- 资产表最少展示名称/用途、类型、适用模块、最新草稿版本、版本摘要、更新时间和“查看”。稳定 ID 只在详情/复制动作中出现，不用长 UUID 占据主列。
- 详情使用右侧只读抽屉：资产身份、不可变版本时间线、结构化载荷摘要、前后 digest、创建者类别和审计入口。创建新版本从详情显式发起，提交成功后仍返回同一资产并聚焦新版本标题。
- 操作日志只显示安全事件投影：模块、项目短身份、集数/轨道、动作类别、来源版本、时间、变更字段、前后摘要和证据计数。默认不展示 `before_state/after_state` 正文、完整字幕、音视频、提示词/热词全文、对象键、Secret 或供应商载荷。
- 事件详情说明“该事件是学习证据，不会自动成为规则”。撤销与恢复通过关联事件和中性状态标签呈现；被撤销修改不能显示为稳定偏好或已采用规则。

#### 5.16.3 新建草稿、错误与焦点

- “登记策略资产”与“创建新草稿版本”使用受保护 Modal。初始焦点在安全返回，`Tab / Shift+Tab` 在安全返回和主操作间双向闭环；提交中聚焦可编程 dialog，锁定 Escape、遮罩和重复提交。
- 确定失败保留表单，显示稳定原因、真实 `requestId` 和唯一重新读取；网络未知只查询同一 `artifactId/versionId`，不得生成第二资源。成功聚焦资产或新版本标题。
- 主读取覆盖 loading、empty、首次 error、再次失败、stale、权限不足和恢复。已有事实刷新失败时保留 stale 只读表格，全部写入口锁定；错误摘要是页面唯一恢复入口，恢复后聚焦 H1。
- 查询身份变化清除失效抽屉、局部选择和分页；迟到响应不得覆盖新筛选。Wave A 不提供批量发布或全选写入，避免把浏览行为误作规则变更。

#### 5.16.4 AI、风险词与费用表达

- AI 筛选页只管理“关闭 / 本地规则后复核 / 抽样复核”的草稿策略，以及将来引用的引擎部署身份；没有真实部署或授权时显示“尚未接入”，不显示模型营销文案或发送测试按钮。
- 风险词只形成提示/人工复核事实，不能表达为自动删除、自动屏蔽或交付硬门；热词投影与风险词库在同一页分段但使用不同语义标签。
- 任何未来优化或 AI 成本只用人民币 CNY 主显示；未知与待对账保留原状态，不显示为 0。Wave A 没有付费动作，也不出现预算输入。

#### 5.16.5 响应式与生产交接

- `1440×900` 保持 216px 展开侧栏、单行页内标签、五列纳管摘要和双栏“资产状态 / 最近事件”；`1024×768` 默认 72px 侧栏，展开只覆盖主区，正文宽度不变。纳管摘要改为两列，内容区改单栏；宽表最小 1080px，只在自身 `.table-wrap` 内横滚。
- 详情抽屉在 1440 为 460px、1024 不超过可用主区的 52%；Modal 最大 620px。页面根 `scrollWidth` 必须等于 `clientWidth`，标签过多时标签栏自身横滚，不把核心入口隐藏到“更多”。
- 复用现有 `ErrorBlock`、服务端表格/分页、只读抽屉和公共 Modal。原型的匿名数据、状态切换器和“设计预览”标识不进入生产。

| 页面/区域 | 未来真实数据源 | Wave A 允许操作 | 明确非目标 |
| --- | --- | --- | --- |
| 总览与五类资产 | PostgreSQL StrategyArtifact/不可变 draft version | 服务端检索、筛选、分页、登记资产、创建新草稿版本 | active/发布/回滚、任意 JSON、浏览器本地规则、匿名生产数据 |
| 操作日志 | 术语、前置审改、画面字、字幕验收既有不可变事件的安全投影 | 检索、筛选、分页、查看安全证据与撤销关系 | 完整正文、音视频、Secret、供应商载荷、编辑/删除事件 |
| 学习候选与评测 | Wave B/C 后续 PostgreSQL 运行与评测事实 | 只显示后续波次说明和安全链 | 假候选、假分数、自动学习上线、真实 AI/训练/付费 |

#### 5.16.6 Wave B 信息架构与主流程

- Wave B 仍在同一 `/strategy` 与同一标签栏内展开。原“学习候选、评测与发布”后置入口冻结为“优化运行 / 学习候选 / 离线评测 / 批准与发布”；前三项在 Wave B 可执行，“批准与发布”继续标记 `Wave C` 且 `aria-disabled=true`。不得把发布动作混入评测结果页。
- 主流程只有一条：管理员创建零网络优化运行 → 等待服务端完成或处理失败/unknown → 逐条审查候选 → 编辑、驳回或送入离线评测 → 阅读基线/候选结果并返回候选继续处理。运行完成、候选送评测或评测成功都不创建策略版本、不移动 active 指针、不改变员工站规则。
- 优化运行列表显示稳定运行短身份、模块、项目/时间范围、固定输入版本、服务端状态、事件/候选计数、CNY 主金额和操作。创建 Modal 明示“确定性本地分析器、零网络、零 AI、零付费”，字段为模块、项目范围、时间范围、最小证据量和固定输入版本；浏览器不计算快照摘要、预算或候选数量。
- 运行服务端状态采用 `queued / running / succeeded / failed / unknown` 的展示映射“排队中 / 运行中 / 已完成 / 失败 / 结果未知”。`failed` 允许在详情中基于原运行事实显式创建新运行；`unknown` 只能查询同一 `optimizationRunId`，禁止自动重发创建命令。两份 Wave B contracts/迁移当前是后端未验证草案，正式前端必须等待 BACK-SYSTEM-07B 冻结共享契约，UI 不据此宣称 API 已完成。

#### 5.16.7 候选审查、编辑与驳回

- 候选列表以单条高密度审查行为为核心，展示类别、标题、目标模块、来源运行、基线版本、支持/反对/未知数量、潜在误报说明、当前修订和服务端状态。完整证据正文不进入列表；详情抽屉只展示脱敏摘要和有界证据引用。
- 候选状态展示映射冻结为“待审查 / 已编辑 / 已驳回 / 待送评测”。允许动作只有查看、编辑建议、驳回、恢复已驳回候选、送入离线评测。编辑产生服务端候选新修订，不覆盖历史建议；驳回是可审计决定而非删除；恢复只追加恢复事件。
- 编辑使用 Modal，初始焦点在“安全返回”；正文使用候选类别对应的严格领域字段，不提供任意 JSON。驳回使用短确认 Modal，必须填写结构化原因类别，可选短备注；送评测确认显示候选数量、candidate digest、固定回归集与“不会批准/发布”。三者提交中均锁定 Escape、遮罩和重复提交。
- 候选决定的网络 unknown 只查询同一 `candidateId + commandId`；确定性冲突关闭当前意图、刷新权威候选并聚焦冲突摘要，下一次显式动作使用新命令身份。查询身份变化清除失效详情/选中项；Wave B 不提供跨页全选或批量批准。

#### 5.16.8 离线评测结果与失败恢复

- 评测结果顶部按“评测身份 / 结论 / 人工处理量变化 / CNY 费用”显示服务端事实；主体以同一固定数据集对比基线与候选的误报、漏报、人工处理量、耗时和 unknown。每项同时显示绝对值与方向，不只靠绿/红颜色；费用未知或待对账保持原状态，不补 `¥0.00`。
- 评测状态同样映射 `queued / running / succeeded / failed / unknown`。成功只表示离线计算完成，默认结论文案为“需人工判断”；页面唯一后续动作是查看固定输入摘要、返回候选继续编辑/驳回或保留证据。Wave B 没有“通过评测”“批准”“发布”“小流量”按钮。
- 读取失败分首次失败与 stale：首次失败显示真实 `requestId` 和唯一原查询重读；已有事实刷新失败保留 stale 只读结果并锁定所有写动作。再次失败仍保留同一恢复块；恢复后聚焦页面 H1。异步命令 unknown 显示稳定运行/评测身份、最后已知阶段、真实 `requestId` 和唯一“查询同一运行”，不得出现“重试运行”。
- 状态轮询只在当前可见且查询身份未变化时读取同一运行；切换标签、筛选、详情或页面离开后停止。迟到响应不得覆盖新身份。`failed` 与 `unknown` 不能展示候选数、指标或费用为 0；部分指标缺失逐字段显示“未知”。

#### 5.16.9 Wave B 响应式、焦点与生产交接

- `1440×900` 保持 216px 侧栏、标签栏单行自身横滚、优化运行宽表仅 `.table-wrap` 横滚；候选采用单列审查行，评测四项摘要单行、五项指标单行。`1024×768` 保持 72px 侧栏覆盖展开且正文宽度不变；评测摘要/指标改两列，候选操作换行，页面根无横向溢出。
- 运行/候选详情抽屉打开聚焦标题，Escape/关闭返回原行触发点；创建运行、编辑、驳回、送评测 Modal 初始聚焦安全返回，正反向 Tab 闭环，pending 时 dialog 获焦。成功后创建运行聚焦对应运行标题，候选决定聚焦更新后的候选标题，创建评测聚焦评测标题；恢复读取聚焦 H1。
- 正式实现复用既有 `ErrorBlock`、服务端表格/分页、只读抽屉和公共 Modal。状态、计数、digest、CNY、requestId 与恢复动作必须来自 BACK-SYSTEM-07B 最终共享契约和 PostgreSQL 权威事实；不得从本原型匿名数据、未验证草案或浏览器推算生产状态。

| Wave B 区域 | 未来真实数据源 | 允许操作 | 明确非目标 |
| --- | --- | --- | --- |
| 优化运行 | PostgreSQL OptimizationRun、固定事件/版本摘要、确定性零网络 Worker | 创建运行、服务端检索/分页、读取同一运行、失败后显式新建 | 真实 AI/网络/付费、自动重发 unknown、训练、修改员工规则 |
| 学习候选 | PostgreSQL Candidate + 只追加决定/修订事件 | 审查、严格字段编辑、驳回/恢复、送离线评测 | 删除历史、任意 JSON、自动采用、跨页批量批准 |
| 离线评测 | PostgreSQL EvaluationRun、固定回归集与服务端指标 | 创建/读取评测、比较基线、返回候选 | 通过即批准、发布/回滚、小流量、active 指针 |

本节职责归属“系统控制台策略与学习”，衔接 `INT-11/14/15/17`。正式实现必须先消费 SYSTEM-07A 共享契约；Wave A 不修改员工站既有规则或任何生产 active 指针。

### 5.17 SYSTEM-07C 前置审改本地规则 Wave C

本节沿用唯一 ControlShell `/strategy` 的 Operate 模式，不另建发布中心、规则编辑器或视觉主题；也是该路由在 Wave C 的专属覆盖合同。页面第一视线必须回答四件事：当前是否存在唯一当前生效版本、当前版本身份、旧会话门禁、下一项人工动作。Wave B 的优化运行、候选与离线评测继续保留；Wave C 只为 `pre_review_local_rule_pack_v1` 增加受控生效与人工恢复，不把其他策略资产一并开放发布。

#### 5.17.1 信息架构与主链

- 一级导航仍只高亮“策略与学习”。页内标签使用既有紧凑标签：“总览 / 本地规则 / 优化运行 / 学习候选 / 离线评测 / 操作日志”；进入“本地规则”后，局部步骤导航固定为“基线与版本 / 影子验证 / 批准与发布 / 历史与恢复”，不得复制第二套侧栏。
- 主链唯一为：未纳管 → 基线预览 → 确认导入 → 对系统基线直接执行固定快照影子影响验证 → 人工批准系统基线 → 首次发布 → 查看历史 → 比较并人工恢复。导入后可并列选择“验证系统基线”或“基于基线创建自定义草稿”；自定义草稿不是基线首发的必经复制步骤。任何完成态都不自动触发下一项写命令。
- 顶部“生效状态带”固定展示当前生效状态、版本短身份、来源、发布时间、未完成旧会话和数据新鲜度。无当前生效版本时使用阻断语义“尚无可执行版本，新会话已阻断”，不得显示默认值已在运行；存在当前生效版本时必须显示稳定版本身份，不能只显示“已生效”。
- Wave C 不提供百分比流量、自动批准、自动发布、定时发布、批量恢复或员工站规则切换。真实 AI、外部网络、Secret、付费和云资源不进入页面，也不把原型示例值当生产常量。

#### 5.17.2 系统初始基线导入

- 未纳管空状态唯一主操作为“预览系统初始基线”。只读 Drawer 展示基线来源、结构化载荷、内容摘要、安全不变量和“导入不等于发布”；关闭后焦点返回原触发按钮。
- “确认导入”打开公共 Modal，标题必须包含“导入系统初始基线”，生产可见正文固定说明：将创建“来源：系统初始基线”的不可变数据库版本；该版本受保护、不可更新、不可删除，并长期保留供查询、比较和人工恢复。工程交接身份仍为 `origin=system_baseline`，不得把该原始字段串显示给用户。安全初始焦点为“返回预览”。
- 导入成功只创建历史版本，不移动当前生效指针，也不解除新会话阻断；成功后聚焦该基线版本标题，并列显示“验证系统基线”和次级操作“基于基线创建自定义草稿”。受保护基线本身可以经过相同影子影响验证、人工批准和首次发布安全门，无需复制同内容草稿。硬编码/default/static/nullable-version 运行旁路的删除不得在 UI 中表现为基线内容或历史被删除。
- 重复导入由服务端身份约束为同一系统基线事实；UI 不生成“基线副本”，也不允许改名、覆盖、归档或删除。历史行永久显示三个并列文本标签：“系统初始基线 / 受保护 / 不可删除”，状态不能只靠图标或颜色。

#### 5.17.3 严格字段草稿

本模块使用领域表单，不出现自由文本配置、任意 JSON、正则、脚本、公式或代码编辑器。每次编辑都创建新的不可变草稿版本，旧版本和内容摘要不覆盖。

| 分组 | 字段 | 控件与显示 | 硬约束 |
| --- | --- | --- | --- |
| 时间对齐 | `alignmentNearbyGapMs` | 带 `ms` 后缀的整数输入；基线为 `350` | 只按共享契约给出的范围与步进校验；浏览器不得引入额外默认值 |
| 时间对齐 | `alignmentSimilarityThreshold` | 固定六位小数输入；基线为 `0.750000` | 保存、比较和历史均保留六位精度 |
| 字幕长度 | `maxCharacters` | 整数输入；基线为 `28` | 只按共享契约范围校验，不接受表达式 |
| 格式开关 | 句末标点 | Switch + 明确“开/关”文本 | 必须与其余开关分别存储、分别比较 |
| 格式开关 | markup | Switch + 明确“开/关”文本 | 不提供自定义标签或替换规则 |
| 格式开关 | 括号 | Switch + 明确“开/关”文本 | 不提供自定义括号或正则 |
| 格式开关 | 多人半角短横线 | Switch + 明确“开/关”文本 | 不提供脚本或格式模板 |

- `one_to_one`、ASR 质量、阻断格式、术语冲突统一置于“只读安全不变量”卡片，只显示服务端结论和说明，不显示表单控件、解锁按钮或“高级设置”。
- 创建草稿 Modal 打开后初始焦点为“安全返回”；确定性字段错误聚焦首个无效字段并关联行内说明，服务端失败聚焦错误摘要。保存成功聚焦新版本标题，后续操作仍需管理员显式发起影子验证。

#### 5.17.4 固定快照零网络影子验证

- 创建影子验证必须选择一个不可变候选版本和一个服务端固定输入快照；确认区展示两者稳定短身份、内容摘要、覆盖范围与“零网络、零 AI、零付费、不会移动当前生效指针”。浏览器不得重建快照、推算覆盖范围或生成匿名指标。
- 验证状态展示为“排队中 / 运行中 / 已完成 / 失败 / 结果未知”。结果页按结构化字段展示当前生效值 → 候选值、影响会话数、阻断数、逐项不变量结论和 requestId；部分缺失逐字段显示“未知”，不补 0，也不把变化方向渲染成发布建议。
- 验证完成只形成不可变证据。人工批准入口必须等待服务端门禁通过；验证对象既可以是受保护系统基线，也可以是自定义草稿。验证失败允许管理员返回版本详情并显式创建新验证，unknown 只查询同一 `impactRunId`，不得自动创建第二次验证。

#### 5.17.5 人工批准、发布与门禁

- 批准与发布为两个独立命令。批准 Modal 展示版本身份、内容摘要、影子验证身份与不变量结论，安全初始焦点为“返回核对”；批准成功只把版本变为“已批准、待发布”，不改变当前生效版本。
- 发布确认必须显示“当前生效版本 → 待发布版本”、只影响发布后新建会话、旧会话继续绑定原版本、稳定 `releaseCommandId` 将用于结果恢复。没有当前生效版本的首次发布，当前侧明确显示“无当前生效版本”。
- 首次发布前，只要服务端返回任一未完成且未绑定策略版本的旧会话，发布按钮硬禁用。阻断区标题固定为“未完成旧会话”，只显示服务端精确数量、最新检查时间、requestId，以及唯一恢复说明“请先按现有工作流完成这些旧会话，再重新读取发布条件”；不得提供绕过、临时绑定或浏览器推算。
- 发布确定失败清除本次写意图并刷新权威版本与门禁事实；管理员复核后才能创建新命令。POST 结果 unknown 时保留同一 `releaseCommandId` 恢复块，只允许“查询此发布/恢复命令”，不得重发 POST。失败、unknown、再次查询失败都保持原当前生效版本。
- 发布成功只移动 PostgreSQL 唯一当前生效指针，追加发布事件并聚焦新的当前生效版本标题。旧版本、系统基线、影子验证、批准和发布事件全部保留，不覆写历史。

#### 5.17.6 历史、比较与人工恢复

- 历史表固定列为版本/来源、状态、结构化字段摘要、内容摘要、确认人、发布时间、发布/回滚事件和操作。服务端检索、筛选、排序与分页保持当前查询身份；表格不展开完整事件正文。
- 基线来源显示 `system_baseline` 的用户文案“系统初始基线”；人工版本显示明确来源。完整详情展示七个可配置字段、安全不变量、内容摘要、确认人、发布时间和发布/恢复事件链，但不显示 Secret、提示词/热词、供应商载荷或隐藏推理。
- “比较”Drawer 使用逐字段双栏：当前生效版本 → 目标版本；未变化字段仍可折叠查看，开关同时显示文字“开/关”。从 Drawer 发起“恢复到此版本”时，目标只能是系统初始基线或已批准历史版本。
- 恢复 Modal 必须再次显示当前生效版本 → 目标版本、两个内容摘要、只影响新会话、历史不改写。主操作文案为“创建恢复命令”，不能写成“覆盖历史”或“撤销发布”；安全初始焦点为“返回比较”。恢复目标只能是已通过同一影子影响验证 + 人工批准安全门的系统基线，或已批准历史版本。
- 发布和恢复共用一种稳定发布命令：`releaseCommandId + action=publish|rollback`，并共用同一个 release-command GET。恢复成功只移动唯一 active pointer、追加审计事件，既不修改目标历史版本，也不删除当前版本。确定冲突清除意图并刷新权威 active；unknown 只查询同一 `releaseCommandId`，不得重发或调用第二恢复端点。成功后聚焦新的 active 标题，并在历史中追加独立恢复事件。

| 当前事实 | 允许主动作 | 必须锁定或隐藏 |
| --- | --- | --- |
| 未纳管、无当前生效版本 | 预览并导入系统初始基线 | 草稿、验证、批准、发布、恢复 |
| 基线已导入、无当前生效版本 | 验证系统基线；基于基线创建自定义草稿 | 未经影子影响验证/批准直接发布、删除/修改基线 |
| 系统基线或草稿待验证 | 创建固定快照影子影响验证 | 批准、发布、恢复 |
| 验证通过、待批准 | 人工批准；返回创建新草稿 | 发布、恢复 |
| 已批准、待发布 | 发布确认；比较版本 | 修改已批准版本、自动发布 |
| 发布 unknown | 查询同一 `releaseCommandId` | 所有写入口、重发发布 |
| 有当前生效版本、历史目标可恢复 | 比较；创建恢复命令 | 覆盖历史、恢复未批准版本 |
| 任一读取 stale/详情 loading-error-refreshing/mutation pending | 唯一重读或查询同一命令 | 导入、草稿、验证、批准、发布、恢复全部写入口 |

#### 5.17.7 异步恢复、焦点与查询身份

- 初始 loading、empty、首次 error、再次失败、恢复成功、已有事实 stale、权限不足和 partial unknown 都有独立呈现。首次 error 只显示真实 requestId 与唯一原查询重读；stale 保留只读快照并明确数据时间，所有写入口统一锁定。
- mutation pending 时 Modal/Dialog 设置 `aria-busy=true` 并获得可编程焦点；锁定 Escape、遮罩关闭、右上角关闭、重复提交和背景写入口。Tab/Shift+Tab 始终在当前 Modal 内闭环；非 pending 时 Escape 关闭并恢复原触发点。
- 确定失败保留必要核对上下文，聚焦错误摘要；unknown 聚焦同一命令恢复标题；查询再次失败仍保留稳定命令身份和唯一再次查询。成功时聚焦新版本或新的当前生效版本标题，不把焦点落到 toast。
- 切换标签、筛选、分页、版本、快照或命令即改变查询身份，必须清除旧详情、局部选择、草稿意图和恢复块。迟到响应只能被丢弃，不得覆盖新身份或重新开启写入口。Drawer 打开聚焦标题，关闭后恢复到原行触发点。

#### 5.17.8 响应式、权限与生产交接

- 本节覆盖 DESIGN 第 8 节的旧通用尺寸说明：`1440×900` 使用 216px 展开侧栏，状态带四列，版本时间线与详情按 5:7 分栏；`1024×768` 默认 72px 收起侧栏，展开到 216px 时只覆盖主区、不挤压正文，内容改单列。
- 历史表和字段比较表只在各自 `.table-wrap` 内横向滚动；标签栏只自身横滚。页面根必须满足 `scrollWidth === clientWidth`。Drawer 在 1440 为 460px，1024 不超过可用主区 52%；Modal 最大 620px。
- 页面同时以文字、图标和必要辅助说明表达状态，不只靠颜色。焦点可见，触控/点击目标与公共 ControlShell 一致；固定底部操作不得遮挡最后一行内容。
- `/strategy` 继续逐请求默认拒绝：读取使用既有 `system-control:strategy:read`，创建基线/草稿使用既有 `system-control:strategy:write`，创建或查询影子影响验证的工程身份仍为 `impactRunId`，权限使用既有 `system-control:strategy:evaluate`；Wave C 人工批准新增 `system-control:strategy:approve`，发布与恢复共用新增 `system-control:strategy:release`。这些 capability 与 API 身份只属于设计/工程交接，生产页面不得显示。员工 AppShell、员工路由、员工构建与员工 API 不得出现 Wave C 入口、当前生效版本管理事实或规则切换能力。

| 区域 | 未来权威数据源 | 允许操作 | 明确非目标 |
| --- | --- | --- | --- |
| 基线与版本 | PostgreSQL 不可变策略版本；代码基线清单仅供首次预览/灾难核对 | 预览/导入一次、直接验证/批准/首发系统基线、基于基线创建严格字段草稿、查看版本与内容摘要 | 强制复制同内容草稿、运行时硬编码兜底、修改/删除基线、任意 JSON/正则/代码 |
| 影子验证 | 固定输入快照、不可变版本、零网络 ShadowRun | 创建/读取同一验证、查看结构化差异与不变量 | 真实 AI/网络/付费、自动批准、百分比灰度 |
| 批准与发布 | Approval、稳定 ReleaseCommand、唯一 active pointer、旧会话门禁 | 人工批准、发布、同命令 unknown 查询 | 自动发布、unknown 重发、绕过未绑定旧会话 |
| 历史与恢复 | 不可变版本、发布/恢复事件、稳定 RestoreCommand | 检索/比较、恢复到系统基线或已批准历史版本 | 改写历史、删除事件、恢复未批准版本、员工站切换 |

本节职责仍归属“系统控制台策略与学习”，衔接 `INT-11/14/15/17`。正式实现必须等待 BACK-SYSTEM-07C 冻结共享契约；UI 文档和原型不代表 API、迁移或生产前端已经完成。

### 5.18 测试反馈与错误观测

UI-SYSTEM-08 沿用员工 `AppShell` 与管理员 `ControlShell` 两套既有站点外壳，不新建“测试模式”、任务清单或第三套观测门户。员工自由完成原工作，只有在需要时主动打开低强调“反馈问题”；管理员在唯一 `/feedback` 聚合服务端事实、查看安全上下文并以只追加事件推进处理。反馈关闭只表示当前处理结束，不删除报告、截图或历史；后续追加“重新打开”事件后回到待确认。

#### 5.18.1 两站入口与低干扰

- 员工站入口固定在 AppShell 顶栏工具位，与服务状态同组但使用中性描边、12px 文案和线性图标，不使用悬浮气泡、红点、动画或固定底栏。视频/时间轴等沉浸工作台使用同一顶栏/工具栏末端的紧凑按钮；入口不覆盖播放、时间轴、字幕、保存或提交动作。
- 页面已有 Modal/Drawer 时，反馈入口留在背景层且不可交互，不跨层悬浮。关闭业务浮层后才能显式打开反馈；反馈 Modal 自身不再叠第二个 Drawer。
- 管理员站在 ControlShell“追踪与安全”组新增唯一“反馈问题”，路由 `/feedback`。一级导航只高亮该项；员工站没有反馈管理列表、状态流转或跨项目查询入口。

#### 5.18.2 员工创建 Modal

- Modal 标题“反馈当前页面的问题”，安全初始焦点落“安全返回”。类型为六个中文单选项：无响应、显示不正确、状态不正确、操作缓慢、不清楚下一步、改进建议。描述只有一个必填多行输入，提示写清发生了什么与期望，不要求测试步骤或复现清单。
- “随反馈发送的安全信息”只读摘要固定展示页面区域、页面模板、应用版本、浏览器大类/版本、窗口尺寸、时区、稳定项目/任务短身份、最近安全 requestId 与性能摘要；禁止 DOM、输入内容、字幕/视频帧、文件名、对象键、Cookie/Secret、提示词、完整查询和请求正文。
- 截图默认无附件。提交者必须先点“选择一张截图”，在本地预览 PNG/JPEG/WebP 的名称替代文案、尺寸和大小，再勾选“我已确认截图中没有账号、聊天、字幕正文或其他敏感内容”；未二次确认时提交不携带截图。页面不自动截图、不录屏、不保存会话回放。
- 创建 pending 时 Modal `aria-busy=true`，锁定类型、描述、附件、Escape、遮罩、关闭和重复提交。确定失败聚焦错误摘要并允许同一表单显式再提交新命令；unknown 保留同一反馈身份，唯一动作“查询本次反馈结果”，不得重发创建。成功聚焦页面内低打扰成功状态“反馈已提交”，然后由用户关闭并回原入口。

#### 5.18.3 管理员 `/feedback`

- 首屏顺序固定为“标题/最近读取 → 四项服务端摘要 → 搜索筛选排序 → 紧凑数据表 → 分页”。四项摘要为待确认、处理中、待复测、最近关闭，均来自同一服务端查询范围的 total；未知显示“未知”，不由当前页计算。
- 服务端查询支持关键词、页面区域、类型、状态、项目、时间、排序、limit/offset。表格列固定为反馈、页面区域、类型、项目、状态、最近事件、更新时间、操作；最小宽度 `1080px`，只在表格容器横滚。
- 行“查看详情”打开公共 Drawer，初焦点为标题。详情按“问题描述 → 安全上下文 → requestId/稳定任务身份 → 用户确认截图 → 只追加历史”呈现；截图区只有安全缩略图、尺寸、大小、确认时间和“查看大图”，绝不显示对象键或存储地址。历史只显示有界事件摘要、操作者角色、时间和 requestId，不展开原始日志/请求体。
- 状态链为“新反馈 → 已确认 → 修复中 → 待复测 → 已关闭”；关闭后唯一写动作“重新打开”，追加事件并回到新反馈。状态动作使用公共 Modal，安全初始焦点“返回详情”，确认区显示当前→目标、备注与“历史不会被改写”。

#### 5.18.4 读取、命令与恢复

- loading 使用表格骨架；empty 解释当前查询没有反馈；首次 error 显示真实 requestId 与恰好一个“重新读取原查询”；已有事实刷新失败保留 stale 快照并锁全部写入口。再次失败仍只有同一重读，成功清 stale 并聚焦“反馈问题”标题。
- 创建反馈和追加状态事件均使用稳定反馈/事件身份与幂等键。POST 结果未知时只 GET 同一身份；查询再次失败保留唯一恢复动作。确定性冲突关闭 Modal、清旧意图、刷新权威详情/列表，聚焦唯一错误摘要；下一次显式动作使用新身份。
- 搜索、筛选、排序、分页或行身份变化会清旧 Drawer、恢复块和写意图；迟到响应不得覆盖当前查询或重开 Drawer。Drawer/Modal 非 pending 时支持 Escape、遮罩关闭、Tab/Shift+Tab 闭环并回原触发点。

#### 5.18.5 响应式、隐私与工程交接

- `1440×900`：员工 AppShell 保持 204px 侧栏，ControlShell 保持 216px 侧栏；反馈表完整显示。`1024×768`：两站默认 72px 收起侧栏，展开 216px 只覆盖、不挤正文，并有遮罩与焦点闭环；页面根无横向溢出，宽表只在自身容器横滚。Drawer 1440 约 560px，1024 不超过可用主区 58%；Modal 最大 640px。
- 状态同时使用文字、形状与颜色。全部员工可见文案为业务中文，不显示 capability、objectKey、Sentry、原始日志、请求体或工程状态名；requestId 与稳定短身份仅在确有诊断价值的安全位置出现。
- 本节职责归属“测试反馈与错误观测”，衔接 `INT-18`。未来正式实现必须消费共享反馈契约和 PostgreSQL 权威事实；匿名原型不构成 API，也不接真实 COS、Sentry、网络、Secret、付费或云资源。明确不做测试任务清单、强制步骤、会话录像、自动截图、第二日志库或营销卡片墙。

### 5.19 有序执行路由与安全兜底

UI-SYSTEM-09 Rev 2 在 5.11–5.12 的唯一管理员 `ControlShell` 内升级 `/engines`、`/routing`、`/changes` 与 `/logs`。页面继续使用冷灰白日间控制舱、深石墨蓝侧栏、`#4458D8` 单一主动作、紧凑宽表和公共受保护浮层，不建立第二管理壳、卡片式供应商市场或员工配置入口。本节只冻结有序目标与安全兜底的 UI/交互，不表示生产 API、FunASR、PaddleOCR、云厂商、Secret、网络、付费或服务器已经接入。

#### 5.19.1 唯一管理旅程

- 最短路径固定为 `/engines` 登记不可变部署版本、绑定服务器已有凭据别名并完成连接检查 → `/routing` 创建 1–8 个连续编号的有序目标草稿 → `/changes` 核对旧/新顺序、容量、预算和活动任务影响，人工批准并发布 → `/logs` 观察“执行记录 1 → 2 → 3”、CNY 与待对账 → 需要时回到 `/changes` 恢复到已批准历史版本。
- `/overview`、`/budgets`、`/servers` 继续作为只读深链，不在本节重做；员工 `AppShell`、员工导航、普通业务页面和员工 API 不增加部署、顺序、启停、费用策略或故障注入入口。
- 页面之间通过稳定路由版本、部署版本、任务/执行记录短身份和 requestId 关联；浏览器不选择下一个服务、不移动当前生效指针，也不根据错误文案推算是否可安全兜底。

#### 5.19.2 有序目标而非平等卡片

- `/routing` 的核心必须是一张连续纵向表：第一列同时显示 `1…8` 编号、连接线和“上移/下移”；其后依次显示不可变部署版本、业务角色、执行位置、三项容量、连接检查与行操作。不得把目标画成相同权重的供应商卡片、开关矩阵或并行泳道。
- 目标数量最少 1、最多 8；顺序必须从 1 连续递增，同一部署版本不得重复。新增、删除、上移、下移和容量编辑只改变浏览器草稿；“保存草稿”形成新的不可变路由版本，但不改变当前生效。
- 角色使用业务中文“首选 / 低价 / 高质量 / 应急”，但每一行都必须引用真实 `EngineDeploymentVersion`；执行位置只投影真实部署的“本地 / 云服务”。推荐画面字真实目标顺序为“本地画面字识别 → 低价画面字服务 → 高质量画面字服务”，推荐语音真实目标顺序为“本地/低成本语音识别 → 低价语音服务 → 高质量语音服务”；推荐是草稿模板，不是迁移常量或自动发布。
- 目标表之后单列“目标耗尽后的人工处理”：全部已启用真实目标均确定失败后，任务进入对应领域工作台人工处理。人工处理是最终业务出口，不是 RoutingTarget 或 EngineDeploymentVersion，不占 `1…8` 目标序号，不参与上移/下移、新增/删除、连接检查或引擎容量。
- 容量继续表达“同时处理上限 / 单项目同时处理上限 / 等待队列上限”。RoutingTarget 不复制第二个启停状态：是否可用只来自 `/engines` 的操作员启用/停用事实；被停用目标在新草稿中不可选择，系统不得偷偷重新启用。
- “当前生效”和“当前草稿”并列展示版本、目标数与差异；当前生效只读，草稿可编辑。已有当前生效读取失败时保留 stale 快照并锁住新增、排序、删除、容量编辑、影响检查和发布。

#### 5.19.3 `/engines` 操作员启停与连接检查

- 部署表新增“当前使用位置”，例如“画面字 · 第 1 位”；详情展示不可变版本、操作员状态、连接检查、执行位置与费用类型。部署身份不拥有“第几个目标”，使用位置只由当前生效路由只读投影。
- “启用/停用”是高风险命令。被当前生效路由引用时，停用动作稳定阻断并引导先发布替代路由；未引用部署才可二次确认停用。系统故障、健康变化或路由切换都不能自动改写操作员状态。
- 连接检查继续使用稳定测试身份。排队/运行中不允许重复创建；unknown 只显示“查询本次连接检查结果”；确定失败在修正前置条件后创建新的检查身份。连接检查成功只证明对应已登记路径可用，不代表质量达标或自动进入当前生效。
- 真实供应商和付费能力未授权时使用“尚未接入”；零网络检查必须明确写“零网络检查通过”，不得投影为真实云服务可用。

#### 5.19.4 safe failover 与 unknown 停止

- 页面在目标表下方并列固定两条只读规则：“安全进入下一目标”和“结果未知时立即停止”。前者必须写明只有服务端证明外部未受理、无付费副作用，且下一目标可用、容量/CNY 预算均准入时，才新建下一执行记录；后者必须写明已受理、响应丢失、超时或费用待对账时停在当前目标，只查询同一请求身份。
- 本地预检失败、目标停用、连接检查失效、明确未发送，以及经服务端安全分类的“明确拒绝且未受理”可以进入下一目标。401/403、Secret/配置错误不自动跨服务；5xx、断网、超时只有在服务端明确外部副作用不可能时才可前进。
- `unknown / reconciliation_required`、供应商成功但本地未知、已受理后响应丢失均禁止创建下一付费执行。生产文案不得出现“自动重试另一厂商”“备用服务已接管”等不能由权威事实证明的表达。
- 质量不达标首版不自动进入高质量目标；保存结果后只能由管理员显式发起高质量复核并重新经过预算门。全部已启用真实目标均确定失败后，领域状态才进入既有人工处理工作台；人工终点不是后台 Adapter、路由目标或自动成功。

#### 5.19.5 `/changes` 影响、批准、发布与恢复

- 影响页同时展示“当前 → 候选”的完整真实目标顺序差异、容量变化、连接检查、活动任务、待对账任务和 CNY 预算；目标数、连接检查数和顺序差异只统计真实 EngineDeploymentVersion。人工终点在真实目标列表之后单列为“全部目标耗尽后的处理”，不计入目标数量或检查数量。服务端阻断存在时隐藏发布主动作；不提供强制跳过或前端自行豁免。
- 人工批准与发布是两个独立事实。发布 Modal 展示当前版本 → 目标版本、只影响新任务、运行中任务继续固定旧路由、待对账保持原目标、云目标仍逐次经过预算准入；安全初始焦点落“安全返回”。
- 发布、恢复共用稳定发布/恢复命令身份。POST 只发一次；unknown 关闭 Modal 后在活动页显示唯一“查询本次发布结果/查询本次恢复结果”，只 GET 同一命令。失败/unknown 保持旧当前生效；成功后才移动唯一指针并聚焦页面标题。
- 恢复到已批准历史版本是新的受保护命令，显示当前值 → 目标值并说明“历史不改写，只影响新任务”。旧任务、旧执行记录、旧费用和发布/恢复事件永久保留。
- 确定冲突关闭或保留在当前活动 Modal 的唯一安全错误区，清除旧写意图并刷新服务端权威事实；下一次显式动作使用新稳定身份。不得在遮罩后显示可交互错误或把焦点落到 BODY。

#### 5.19.6 `/logs` 执行链

- 每个任务按固定路由版本展示横向“执行记录 1 → 2 → 3”链，每张执行记录固定目标顺序、部署版本、结果、费用、requestId 与安全前进原因；这是时间顺序，不是平等结果卡。
- 确定安全失败使用红色状态和明确“已证明未产生外部副作用”；成功使用绿色；unknown/待对账使用琥珀色并使后继记录显示“未开始”。状态必须同时有文字和形状，不只靠颜色。
- 自动前进时在链下写明“上一执行明确未发送，且下一目标通过容量和预算准入”；安全停止时写明“只查询同一请求身份，不创建下一付费执行”。CNY 0 只用于明确无外部费用的本地事实；云 unknown 显示“待对账 + 预留金额”，不得显示 0。
- 日志只显示有界业务摘要、稳定短身份和 requestId，不展示字幕正文、视频帧、providerRequestId 原值、Secret、URL/Header、objectKey、原始请求/响应或模型隐藏推理。工程身份仅在契约交接中存在。

#### 5.19.7 读取状态、焦点与双视口

- 四页统一覆盖 normal/loading/empty/首次 error/stale/再次失败/恢复成功。loading 使用表格骨架；empty 解释必须先完成的管理员动作；error 带真实 requestId 与恰好一个原查询重读；stale 保留旧事实并锁全部写入口；恢复成功清除 stale 并聚焦当前页 H1。
- 排序、删除、操作员停用、发布与恢复使用公共受保护 Modal。打开后“安全返回”获焦，`Tab / Shift+Tab` 双向闭环；非 pending 时 Escape/遮罩关闭并回原触发点；pending 时 `aria-busy=true`，关闭、Escape、遮罩、重复提交和字段全部锁定，焦点留在活动 Modal。
- unknown 的唯一查询动作、确定冲突错误、再次读取失败 ErrorBlock 都必须在最上层活动区域可见、鼠标 hit-test 可达并获得安全焦点；成功聚焦 H1。查询身份、业务环节、排序、分页或选中版本变化时清旧恢复/写意图，迟到响应不得覆盖或重开浮层。
- `1440×900` 保持 216px 展开侧栏与详细宽表；`1024×768` 默认 72px 收起侧栏，展开 216px 使用 fixed 覆盖与遮罩，正文仍保持 `x=72 / width=952`，不挤压重排。页面根必须 `scrollWidth === clientWidth`；目标表、日志链和历史表只在各自容器横滚。

#### 5.19.8 后续复用与工程边界

- 术语候选与需要 OCR/AI 的图片/PDF 表格识别以后可以复用“有序目标表 → 影响检查 → 执行链”信息架构，但本节不创建其生产页面、接口或状态。普通 XLSX/CSV 继续本地确定性解析，不进入有序路由。
- 术语/表格模型输出只能形成候选或草稿，不能直接覆盖不可变 `TermVersion`、画面字 Release、交付 XLSX 或原始文件。原型中的术语/表格注释不得被实现为当前已支持的选项。
- 本节职责对应“系统控制台有序执行路由与安全兜底”，主衔接为 `INT-11`；`INT-12/15/16/17` 分别提供运行回流、预算、Secret 安全引用和 CNY 事实。生产实现必须等待 BACK-SYSTEM-09A 冻结共享契约；匿名原型不是 API。
- UI-SYSTEM-09 Rev 2 继续使用同一可操作原型 `C:\Users\ComradeGu\.codex\visualizations\2026\08\18\01a012fb-e9f3-7801-a660-b22518eeea0c\ui-system-09-ordered-routing\index.html`；Rev2 最小变化证据在同目录 `evidence/rev2/`，FIFO288 其余49项继续引用原 `evidence/`。示例部署名、数量、版本、requestId 和金额只用于匿名设计验收，生产禁止保留。

## 6. 公共组件边界

首批公共组件只覆盖真实复用点：

- `AppShell`：全局侧栏、顶栏和内容出口。
- `ModuleNav`：从模块注册表生成导航。
- `ProjectContextBar`：项目身份、状态和项目级流程。
- `PageToolbar`：筛选、搜索和主操作。
- `DataTable`：密集数据、排序、分页、行操作和空状态。
- `TaskStatus`：统一任务状态和语义颜色。
- `UploadQueue`：分块上传、暂停、续传和失败恢复。
- `EvidencePane`：字幕、音频、截图和规则证据。
- `DecisionActions`：通过、修改、驳回和暂不确定。
- `ConfirmAction`：删除、清理、交付等高风险操作确认。

没有两个真实使用点前不新增通用组件。业务状态不得存入纯 UI 组件。

整剧素材确认对话框已形成键盘闭环：打开后聚焦安全返回操作，`Tab` / `Shift+Tab` 焦点保持在对话框内，非提交期间可按 `Escape` 关闭，关闭后焦点返回触发按钮；提交期间禁用重复操作。

## 7. 视觉基线

视觉方向为冷灰画布、白色工作面和克制靛青强调色。鬼手仅作为布局参考，不复制其广告横幅、渐变、品牌色或素材。

| 角色 | 基线颜色 |
| --- | --- |
| 主强调 | `#4458D8` |
| 主强调悬停 | `#3549BD` |
| 主文字 | `#20202A` |
| 次文字 | `#747481` |
| 侧栏底色 | `#F3F3F7` |
| 页面底色 | `#FFFFFF` |
| 边界线 | `#E7E7EE` |
| 轻强调面 | `#F5F7FF` |

状态色只表达业务状态：成功使用绿色、处理中使用靛蓝、等待或警告使用琥珀色、失败或阻断使用红色。模块本身不各自选择品牌色。

布局使用 `8px` 基础节奏；常用圆角为 `8px`，主要容器为 `12px`。同一层级只选择边框或阴影中的一种，不叠加装饰。

## 8. 桌面布局与窄屏降级

- 正式 UI 验收以电脑屏幕尺寸为准；`>= 1024px` 使用完整 `204px` 侧栏，数据表和工作台使用全部可用宽度。
- `768px–1023px` 与 `< 768px` 的现有样式只作为非承诺降级：允许收紧间距、重新分栏或让表格在自身容器内横向滚动，不纳入当前正式验收。
- 正式移动端工作范围需另行确认；不得用隐藏核心功能来声称移动端兼容。

## 9. 状态与数据所有权

- PostgreSQL/后端是项目、任务、审核和交付状态的唯一权威来源。
- 前端只保存筛选、选中项、面板开合和未提交表单等展示状态。
- 整剧素材页的本地扫描结果、配对选择和待分配输入均属于浏览器草稿；即使页面显示“本地调整未确认”，也不得替代最新后端清单版本或表现为已确认。
- 队列和 Worker 不直接驱动 UI；后端将任务状态转换为稳定接口。
- `上传中`、`处理中`、`等待人工确认`、`失败可重试`、`失败需人工处理` 和 `已完成` 必须是不同状态。
- AI、ASR 和 OCR 只提供候选或证据，不能在 UI 中表现为已经覆盖人工结果。
- 公司稿、ASR 原文和采纳稿是三个不同对象；切换每集对照基准不修改前两个只读来源，也不能覆盖已经人工确认的采纳稿。
- 人工修改保存与学习事件归档是同一后端命令的可审计结果；学习归档失败不能伪装为成功。界面只显示当前工作保存状态，不向普通员工暴露跨项目学习事件和候选规则。

## 10. 本地工作台功能映射

| 本地工作台能力 | 云端统一位置 |
| --- | --- |
| 项目与文件、工作副本校验 | 项目概览与素材 |
| AI 提取、提示词与模型运行 | 术语模块的提取任务 |
| 术语候选审核、证据和正式表格 | 术语模块 |
| 中文识别、双 ASR、热词与质量门禁 | 中文识别模块 |
| 画面字 OCR、截图证据与时间轴 | 画面字模块 |
| 公司字幕、ASR 证据对照与大范围人工修正 | 前置审改工作台 |
| OCR 候选保留/排除与初始画面字时间 | 画面字模块与前置审改工作台 |
| 最终视频定位、字幕叠加和时间轴复核 | 视频字幕验收工作台 |
| 逐集/整剧通过与交付就绪确认 | 视频字幕验收工作台；不可变验收快照由交付命令在后台固定 |
| 交付确认、批量导出、产品归档和正式下载 | 完整交付确认页 + 全局交付产品库 |
| 内容扫描、人物/人脸视觉风险 | 未来扩展位；近期导航隐藏，不作为交付硬门禁 |
| 任务恢复、运行历史与资源锁 | 全局任务中心 |
| 项目清理、延迟删除和恢复 | 回收站 |

## 11. 当前阶段与后续接入

当前已落地统一应用外壳、模块注册表驱动的项目中心导航，以及项目创建、检索、表格和加载、空数据、失败状态。项目概览与素材已落地项目上下文条、本地元数据扫描、按集素材配对台账、确认阻断与待分配区，以及后端清单版本与本地未确认草稿的分离展示；显式确认后才形成新版本，旧版本继续保留。上传任务与回收站仍属本阶段待接入功能；其余全局入口可以在预览中显示为“后续”，但正式产品在功能完成前应隐藏。

后续每接入一个本地能力，必须同时完成：

1. 在模块注册表登记范围、路由、分组和功能状态。
2. 复用现有页面模板与状态组件。
3. 明确后端权威状态和失败恢复方式。
4. 补齐正常、加载、空数据、失败和禁用状态，并在目标电脑屏幕尺寸下验收。
5. 更新本文的本地功能映射与模块状态。

## 12. UI 验收门槛

- 侧栏、项目上下文和主要工作区在目标电脑屏幕尺寸下均可识别；窄屏降级不属于当前正式验收门槛。
- 同一页面只有一个可见页面标题。
- 新模块不复制应用外壳、导航或任务状态逻辑。
- 任何异步任务都能看到失败原因和下一步操作。
- 删除、清理、交付和付费调用有明确确认与审计提示。
- 页面颜色、间距、表格、按钮和状态文案来自统一系统。
- 本地已验证的人工裁决和证据边界在云端映射后仍保持不变。
