{
  "name": "qimao-terms-cloud",
  "version": "0.0.0",
  "private": true,
  "description": "审改工作台云端版模块化单体",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": "24.19.0",
    "pnpm": "11.21.0"
  },
  "volta": {
    "node": "24.19.0",
    "pnpm": "11.21.0"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter @qimao-terms-cloud/backend --filter @qimao-terms-cloud/frontend run dev",
    "dev:backend": "pnpm --filter @qimao-terms-cloud/backend run dev",
    "dev:frontend": "pnpm --filter @qimao-terms-cloud/frontend run dev",
    "build": "pnpm --recursive --if-present run build",
    "lint": "pnpm --recursive --if-present run lint && node --check tools/verify-repository.mjs && node --check tests/backend/health.test.mjs && node --check tests/repository/skeleton.test.mjs",
    "test": "node --test",
    "check:repo": "node tools/verify-repository.mjs",
    "check": "pnpm run check:repo && pnpm run lint && pnpm run test && pnpm run build"
  }
}
