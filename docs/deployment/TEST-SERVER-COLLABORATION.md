# 测试服务器协作看板

> 实时状态页｜协作协议 v6.1｜2026-08-21  
> 历史全文已原样归档至 [`archive/TEST-SERVER-COLLABORATION-v5.0-20260821.md`](archive/TEST-SERVER-COLLABORATION-v5.0-20260821.md)，故障过程以 [`TEST-SERVER-ISSUES.md`](../testing/TEST-SERVER-ISSUES.md) 为准。

- 2026-08-23｜ROS真实浏览器探针 REAL-06 Phase-A Rev3｜**artifact_written / READY**：按修正合同仅独立核验 archive SHA/size、tar topology/无链接、解包无链接、入口普通文件/SHA，并以 qimao 绝对 Node 的 non-main import=`IMPORT_OK` 作为完整传递依赖权威门；未增加 checksums 路径猜测门。唯一 root-only、10 分钟硬上限 supervisor 已到 `READY_WAITING`：helper listening、内部页和公网同源页均通过 HTTP 200、HTML/no-store/CSP；临时 Nginx 引用经绝对二进制测试且服务 active，39131 仅回环监听。一次性 URL 仅写入当前 Windows 用户独占、禁继承 ACL 的仓库外 handoff，READY marker 仅含 `READY`。本阶段未调用 capability/result、未触发浏览器、ROS request/Create/PUT/object/multipart=0；等待规划应用内浏览器唯一导航或自动截止，禁止另开页面、重复请求或业务部署。

- 2026-08-23｜ROS真实浏览器探针 REAL-06 Phase-A Rev2｜**blocked / 独立依赖门**：固定 archive/manifest 本地 SHA、大小与协调文件起始状态通过；服务器唯一临时根、上传、远端 SHA/size、tar topology/无 symlink、解包后无链接与权限、入口普通文件/SHA 均独立命令通过。独立依赖门在 AWS checksums 依赖存在性检查返回稳定类别=`AWS_DEPENDENCY_MISSING`，按任一门失败即停，不进入 non-main import、真实 env、helper、Nginx 临时 location、page、handoff/READY。已精确删除本轮临时根并复核 helper=0、39131=0、Nginx/location 未变、ROS request/Create/PUT/object/multipart=0；本地 handoff/READY 仍 absent、残留=0。该结果不能归因雨云；需由规划以 manifest 明确的 checksums package/路径做一次只读合同对齐后再签发新身份，禁止复用本轮临时根或现场改参重试。

- 2026-08-23｜ROS真实浏览器探针 REAL-06 Phase-A Rev1｜**blocked / 远端闭包验证启动门**：固定 archive 与 manifest 的本地格式、SHA、大小、tar/entry/依赖合同均匹配，协调 handoff/READY 起始均为 absent。服务器前门（Nginx active、39131 未监听、受保护 env 仅元数据与必需键存在）通过，已将冻结 archive 上传到唯一临时根；但远端闭包验证执行未进入任何可归类的 archive/tar/import 门即退出，故不能把它归因 ROS 或闭包内容。按“失败不重试”门立即精确删除该临时根并复核 helper=0、39131=0、Nginx/location 未变、ROS request/Create/PUT/object/multipart=0；本地 handoff/READY 未写入、残留=0。需由规划重新签发新的 Phase-A 身份与单次远端验证执行令牌；不得复用本次临时根或现场改参重试。

- 2026-08-23｜ROS真实浏览器探针 REAL-06 Phase-A｜**blocked / 本地闭包门**：起始 handoff/READY 均为 absent。既有已消费的 portable closure 输入未在约定本地暂存根或工作区根找到；为避免复用部署 release 或现改探针结构，仅尝试一次本地临时 `pnpm deploy` 闭包物化。该产物含 reparse/symlink，未满足此前 portable-closure 的无链接运行时门，故未上传或启动，已精确删除并复核临时目录=0。未连接服务器，未加载受保护 ROS env，helper/Nginx/location/browser/ROS request/Create/PUT/object/multipart=0；本地 handoff/READY 仍 absent、残留=0。需要由规划提供一份已冻结、无 symlink 的 portable closure（及其脱敏完整性合同）后，才能签发新的 Phase-A；不得在本任务现场重打包或改用部署依赖目录。

- 2026-08-23｜ROS真实浏览器探针 REAL-05 Phase-B direct-minimal 收尾｜**blocked / capability=unknown**：按固定 PID 的 root owner 与 timeout/supervisor cmdline 身份门后发送 TERM，既有 trap 终态=`CLEANED_READY`。同一 root-only stdout/status 的脱敏提取未得到可解析 final summary；仅得到 stderr 类别=`helper_stderr`，不能诚实报告 `browserPut/etag/sdk/elapsed/cleanup` 的布尔或数值，也没有同一 object/UploadId 身份可供安全查询，故 object/multipart residual=**unknown**（未扫描、未盲写）。这不是可归类的雨云 provider 错误，不应据此查询官方 ROS 文档或联系客户支持。基础设施恢复已证实：supervisor/helper/PID/status/stdout/stderr/workroot/39131=0，临时 location/include/backup 与渲染引用=0，绝对 `/usr/sbin/nginx -t` 通过且 Nginx=active；原临时公网路径返回 non-helper。本地 READY/handoff 已 absent，已精确删除并复核 consumed closure archive+manifest=absent。由于最终协议证据与对象清理身份均不可证明，候选不支持进入业务实施；不得重跑、重建对象或依据本轮推断 ROS 能力。

- 2026-08-23｜ROS真实浏览器探针 REAL-05 Phase-A direct-minimal｜**artifact_written / READY**：正式未调用 transient。新 closure 仅按第一性原理身份门在远端复核：SHA lowercase-normalized 与 size 匹配、tar/extract 无 symlink、入口/package/AWS client/presigner/checksums 均存在，qimao non-main import=`IMPORT_OK`；未重算历史 tar/包数/aggregate 口径。唯一 root-only direct supervisor 经 `bash -n` 后由绝对 timeout 启动，PID owner/cmdline 与 `420s` 上限门通过；真实受保护 env 仅运行期加载并覆盖 browser_direct/600，token 为服务器 CSPRNG root-only，未输出。helper=`listening`，internal 与同源 public page 均通过 `200`、HTML/no-store/CSP 门；固定临时 location 存在、绝对 `/usr/sbin/nginx -t` 通过且 Nginx=active，39131 listener=present。一次性 URL 已仅写入当前 Windows 用户独占、禁继承 ACL 的 handoff，marker 仅为 `READY`；随后本地只读核验发现这两个精确协调文件均已 absent，未重写或另发，原因未知。supervisor 仍 active 等待规划 Phase-B。Phase-A 未调用 capability/result、未触发浏览器/ROS/object/multipart；不得另发 URL、刷新页面或启动第二个 supervisor。

- 2026-08-23｜ROS REAL-04 archive guard 本地逐项审计｜**artifact_written**：仅按上一 direct runner 的 `ARCHIVE_GATE` 表达式在本地隔离目录复算，临时目录已清零；服务器/进程/服务/Nginx/ROS/object/multipart 写入=0、残留=0。archive SHA 原始值与冻结值均为 `2BF0FE6EC5D101B8F63686DA8C6A819AB78B82C866578F89C296617D1EFB4BCC`，lowercase-normalized 亦相等；size=`3642551/3642551` match。`tar -tvzf` 口径 total/regular/dir/symlink=`5203/4673/530/0` 均 match，且 directory=`530` 中**包含恰好一个** `./` 根目录项，故不存在根目录或大小写修正。解包后 non-map dist=`270/270`、入口 SHA=冻结值、node_modules files=`4402/4402`、AWS client/presigner/checksums 均为 true。实际首次 false 是 runner 的递归表达式 `find node_modules -type f -name package.json | wc -l`：actual=`90`、expected=`82`；冻结 `82` 是仅直接 package root（含 scoped 的第二层）口径，故该式在 90→82 前就触发 `CLEANED_ARCHIVE_GATE`。若继续假设性计算，runner 的 sorted sha256sum-line aggregate actual=`C6E62D11BDE5F8028733EB1813E446907C7BEBD86DB8FC52025064902D16361B`、manifest expected=`FDC189F88B994CD1344751E1AF2F0CDF26C92CBD2D63F8F9B35ACBC22D16E831`，亦不 match；因此不能只改 expected count 后重签。下一步应先冻结**同一** aggregate 算法及直接 package-root 计数表达式（非递归 `package.json`），再由规划授权一次新窗口；本轮未改 archive/manifest或业务仓库内容，仅追加本条审计看板。

- 2026-08-23｜ROS真实浏览器探针 REAL-04 Phase-A direct-supervisor｜**blocked**：按 launcher 审计正式弃用 transient；新 closure 的本地 SHA/长度/manifest 冻结门、固定身份与 Nginx 基线均通过。唯一 root-only supervisor 经 `bash -n` 后，由绝对 `/usr/bin/timeout` 启动为一次直接后台进程，PID 的存活、root owner 与固定 timeout/supervisor 命令身份门通过，硬上限=`420s`。但首次阶段轮询唯一终态为 `CLEANED_ARCHIVE_GATE`，脱敏 stderr 类别=`none`；该 guard 内各个静默校验未逐项持久化，不能诚实细分为 tar、计数或聚合摘要差异。按失败不重试门未启动第二个 supervisor，未到 helper、39131、Nginx location、内部/公网 page、handoff/READY，未调用 capability/result、ROS/object/multipart=0。trap 已精确恢复：supervisor/PID file、临时根、listener、location/include/backup=0，绝对 `/usr/sbin/nginx -t` 通过且 Nginx=active；本地 READY/handoff=absent。下一门只能先做 closure archive guard 的只读、逐项脱敏审计，再决定是否签发新窗口；不得把该失败归因 ROS 或修改业务发布。

- 2026-08-23｜ROS REAL-03 transient launcher 只读审计｜**artifact_written / no-evidence**：仅查询精确 unit；`systemctl show` 为 load=`not-found`、active=`inactive`，`systemctl status` 类别同为 `not_found`；限定日期窗的该 unit journal 无可归类记录，先前 root-only launcher stdout/stderr/status 已按收口删除，不能扫描或猜测恢复。唯一可证事实是上一启动调用 exit=`1`；无 unit collision、invalid property、exec/path/permission、runtime timeout 或 runner exit 的稳定证据，故不能把它归因 ROS。与成功 REAL-02 的已保存 `systemd-run` 结构相比，现有证据未见 launcher flag 差异（均为非持久 collect/no-block、有限 RuntimeMaxSec/TimeoutStopSec、`Type=exec` 与 Bash supervisor）；变化仅是固定身份和闭包输入，均不足以解释 exit=`1`。唯一最小建议：下一次签发应弃用 transient，改用此前已通过 fake `listening→page→TERM→residual=0` 的直接 `runuser` 受控闭环，并在同一 root-only执行器内保留脱敏 launcher exit/类别到收口；本轮服务器写入/服务/Nginx/ROS/object/multipart=0、残留=0。

- 2026-08-23｜ROS真实浏览器探针 REAL-03 Phase-A absolute-nginx｜**blocked**：新 portable closure 的本地 archive SHA、长度与 manifest 冻结事实门均通过（`5203/4673/530/0`、非 `.map` dist=`270`、入口与依赖聚合摘要、AWS client/presigner/checksums）；新固定身份、39131 与本地 READY/handoff 起始均为 absent，Nginx 测试与 active 基线通过。闭包上传及 root-only runner 的 `bash -n` 通过后，唯一 `systemd-run` transient 启动命令返回 exit=`1`；按失败不重试门未换参数、未再次启动。随后仅查询同一固定身份，unit 已 inactive、无可识别 status（`NO_STATUS`），不能诚实进一步归因 launcher 与早期 runner；未到 helper listening、Nginx 临时 location、内部/公网 page 或 handoff/READY 门，未调用 capability/result、ROS/object/multipart=0。精确恢复已完成：unit/listener=0、临时根及 status/stdout/stderr/location/include/backup=0，绝对 `/usr/sbin/nginx -t` 通过且 Nginx=active；本地 READY/handoff=absent。唯一下一步是保留并以安全方式取得该**同一 transient 启动**的稳定 launcher 错误类别后再签发新窗口，不得把本轮归因 ROS 或现场重试。

- 2026-08-23｜ROS真实浏览器探针 Phase-B absolute-nginx 收尾｜**artifact_written / capability=unknown（不支持进入业务候选）**：规划声明的唯一 Edge 导航后，固定 transient unit 已自然到期并完成自身清理；同一身份仅查询到脱敏终态 `CLEANED_READY`：`browserPut=false`、`etag=false`，SDK `create/presign/listParts/complete/head/privateRead/delete/abort` 均为 `false`，`elapsedMs=302743`，`abortCount=0`、`deleteCount=0`、`residual=0`。稳定结论为**未观察到浏览器 capability 触发**，不是 ROS/供应商错误；因连 `Create` 都未发生，未产生可查询的 object/multipart 身份，未作扫描或盲写。随后仅清理固定身份并复核：unit=inactive/removed、39131 listener=0、临时根及 status/stdout/stderr=0、临时 location/include/backup=0、渲染专用 location=0；绝对 `/usr/sbin/nginx -t` 通过且 Nginx=active。原临时公开路径的 exact location 已不存在（无可复用 handoff 身份，未再导航）；本地 READY/handoff 已为 absent，已精确删除并复核 portable closure archive 与 manifest=absent。未变更业务 release/current、数据库、DNS/TLS、Cloudflare、Bucket 属性或持久服务配置；ROS/object/multipart 残留=0。

- 2026-08-23｜ROS真实浏览器探针 Phase-A absolute-nginx ready｜**artifact_written / READY**：全新固定身份以用户提供 portable closure 完成 SHA、tar=`5203/4673/530/0`、非 `.map` dist=`270`、入口 SHA、两项 AWS SDK、无 symlink 与 qimao non-main `IMPORT_OK` 门；未读取部署 current/node_modules、未安装依赖。唯一功能差量已生效：root 侧启用、reload 后与 trap 清理的 Nginx 语法检查均使用绝对 `/usr/sbin/nginx`，helper 最小 PATH 保持不变。真实受保护 env 仅在 helper 运行期加载并覆盖 browser_direct/600；服务器本地 CSPRNG token 为 root-only，未输出。helper listening、内部 page 与一次精确同源公网 page 的 `200`/HTML/no-store/CSP 门均通过；Nginx active，临时 location 仅在固定 8080 server 层渲染。一次性 URL 只写入当前 Windows 用户独占、禁继承 ACL 的 handoff，本地 marker 仅为 `READY`。当前 transient unit=active、39131 listener=present，等待规划唯一 Edge 导航；Phase-A 未调用 capability/result，未触发浏览器 PUT、ROS/object/multipart=0。下一门仅为规划读取 handoff 后一次 Edge page 导航，再由 SERVER Phase-B 按同一身份取 summary 并完整清理；不得另发新 URL 或重试。

- 2026-08-23｜ROS临时 Nginx 构造只读审计｜**artifact_written**：从 portable-closure runner 上下文复原：临时 `location ^~ /__ros-probe/<private-token>/` 被写入固定 root-only snippet，内容为 `access_log off`、`Cache-Control=no-store`、回环 `proxy_pass`、关闭代理缓冲与标准转发头；runner 用单个 server-scope `include` 将该 snippet 插到 `127.0.0.1:8080`、`server_name _` 的首个匹配行之后。live `nginx -T` 证实该 8080 block 就是当前 `qimao-loopback` 配置；该文件当前为普通启用文件（非 symlink），server 内仅按 location 显式 include 既有 proxy-header snippet，且不存在 snippets glob 或残留的 probe 引用。可复现首因不是 ROS、也不是 Nginx location 语法：runner 先把 root `PATH` 固定为 `/usr/local/bin:/usr/bin:/bin`，随后在该环境调用裸 `nginx -t`；该 PATH 下 `nginx` lookup=absent、等价命令 exit=`127`，而实际二进制位于 `/usr/sbin`，绝对 `/usr/sbin/nginx -t` 当前通过。`FAILED_NGINX_TEST` 是将 command-not-found 合并为 Nginx-test 失败的状态名；临时配置从未进入 Nginx parser，故无可归因的 Nginx 行号。与已通过的纯 ready 差异是 portable runner 的全局最小 PATH 与“snippet + server include”载体；已通过 ready 使用同一 8080 server 层，但其成功证据只证明 Nginx 可解析，未留下独立 runner PATH 文本。唯一最小修正：仅将 runner 的 root Nginx 检查改为绝对 `/usr/sbin/nginx -t`（清理分支同样使用绝对路径），或在 Nginx 操作局部恢复 `/usr/sbin`；保持 qimao helper 的最小 PATH 不变。该修正足以重新签发一次有界 Phase-A 以验证真实 Nginx parse/page 门，但不构成 ROS/browser capability 通过。审计期间服务器写入=0、服务/Nginx reload=0、helper/systemd/ROS/browser=0，固定身份残留=0。

- 2026-08-23｜ROS真实浏览器探针 Phase-A portable-closure｜**blocked**：用户提供的 portable closure 与 manifest 已验证：archive SHA-256 匹配冻结值、tar=`5203`（普通文件=`4673`、目录=`530`、symlink=`0`）、非 `.map` dist=`270`、入口 SHA-256 匹配、两项 AWS SDK 存在；远端解包与 qimao non-main `IMPORT_OK` 门通过，未读取部署 current/node_modules、未安装依赖。固定 transient runner 通过 `bash -n` 后启动，但唯一终态为 `CLEANED_FAILED_NGINX_TEST`：失败发生在临时 8080 同层 location 的 `nginx -t`，位于公网 page 与 handoff/READY 之前；未保存临时 Nginx 原始 stderr，不能进一步归因。该阶段不是 ROS 请求错误：未调用 capability/result、未触发浏览器、ROS/object/multipart=0。trap 后复核并精确清理 unit、39131、临时根、location、备份、status/stdout/stderr；Nginx `-t` 通过且 active、渲染专用 location=0，本地 ready/handoff=absent，residual=0。保留用户提供的 portable closure 输入不动。按失败不重试停止；下一步需由配置 Owner 审核临时 include 注入方式，再签发一次新的 Phase-A，不得现场复试或将该故障归因 ROS。

- 2026-08-23｜ROS真实浏览器探针 Phase-A nonmap-count恢复｜**blocked**：规划冻结的本地前门已复核通过：`backend/dist` 全量=`405`、source map=`135`、非 `.map` 闭包=`270`，入口 SHA-256 与冻结值一致。固定 unit/temp/status/派生日志/Nginx 临时 location、39131 与本地 ready/handoff 亦全部为 absent；Nginx active。随后受限远端临时根曾建立以只读接入已部署依赖，但 `current` 不可解析，且 active backend 的精确运行目录相邻既有 root/backend 依赖候选均不含两项 AWS SDK；按“不安装、只读复用现有依赖闭包”硬门立即删除该空临时根。未创建 token、runner、unit、helper 或 Nginx location，未加载真实 env，未请求页面或 capability/result，ROS/object/multipart=0；本地非 `.map` 归档已删除，所有固定身份残留=0。唯一下一步：由规划恢复一个可解析、含两项 SDK 的已部署只读依赖闭包身份，或明确签发独立受审计的隔离依赖闭包供 SERVER 使用；不得现场安装依赖或改变 release。

- 2026-08-23｜ROS真实浏览器探针 Phase-A fixed-identities｜**blocked**：固定身份预检通过：预声明 transient unit、`/tmp` 临时根、`/run` status/派生 stdout/stderr/Nginx 备份、39131 listener、临时 Nginx location 以及本地 ready/handoff 均为 absent；Nginx 语法检查通过且服务为 active。随后在本地完整闭包前门停止：当前 `backend/dist` 普通文件数实测为 `405`，不满足本租约冻结的“完整 270 文件闭包”身份，不能用不同闭包继续执行 lowercase SHA、import、真实环境或短时入口。失败发生在任何本地/远端临时目录、归档、token、unit、location、helper、受保护 S3 env 与页面请求之前；未调用 capability/result，ROS/object/multipart=0。复核本地临时根/归档仍为 absent，服务器保持预检基线，残留=0。唯一下一步：规划/BACK 重新冻结可审计的当前完整闭包文件清单及 SHA 门，或恢复确实对应 270 文件的已审核产物；不得由 SERVER 现场裁剪闭包。

- 2026-08-23｜ROS Phase-A CRLF轮有界恢复｜**artifact_written / residual=0**：仅按授权 UTC 窗 `09:34:00Z`–`09:39:00Z`、固定父目录与完整专用命名规则取证；仅识别到 3 个 root-only 普通文件（supervisor stdout/stderr、稳定 status），其 `root:root`、`0600`、大小与时间窗元数据门均通过，随后仅对这 3 个精确身份执行 present→absent 清理。复核：transient unit=0、固定 helper 进程=0、`127.0.0.1:39131` listener=0、精确临时根/runner/token/status=0、临时 Nginx location=0；`nginx -t` 通过、Nginx=`active`、渲染配置中专用 location=0、loopback 基线首页=`200`。本地 ready/handoff 均为 absent。浏览器未取得探针 URL，browser trigger=false；未调用 capability/result、未手工访问 ROS，object/multipart 残留=0。未取得 bootstrap 是否加载受保护 env 的可证明证据，未作该项断言；未改业务 release/current、数据库、DNS/TLS、Cloudflare 或持久服务配置。

- 2026-08-23｜ROS真实浏览器探针 Phase-A CRLF恢复｜**blocked**：本轮唯一功能差量为 token 读取同时剥离 CR/LF；本地 ready/handoff 仍保持 absent，未输出 token/URL/Secret。归档、runner 与 token 已按唯一高熵临时身份上传后，远端 bootstrap 在产生任何脱敏状态行前以 exit=`2` 结束；因此无法证明其是否进入 runner、是否启动 unit 或完成精确清理。Nginx 注入、helper、受保护 S3 env 加载、内部/公网 page、capability/result 与对象/multipart 均未获得执行证据，不能宣称未发生。一次性本地 PowerShell 身份未持久化，当前不能在不猜测或扫描的前提下定位远端暂存资源，故残留=**unknown**；按失败不重试边界停止。下一步需要新的明确审计/清理授权，且须先恢复该轮的精确临时身份后再查询或删除。

- 2026-08-23｜ROS真实浏览器探针 Phase-A ready｜**blocked**：两条本地协调路径在启动前为 absent，运行期 token 已生成且未输出；但 Windows 管道写入远端 root-only token 文件时带入 CRLF，runner 去除换行但未去除回车，URL-safe token 格式门稳定停在 `TOKEN_INVALID`。该门位于 helper、真实 S3 env 加载、Nginx 临时 location、内部/公网 page GET 与 capability/result 之前，故上述动作均未发生，未创建对象/multipart。bootstrap 已对同一临时身份执行 stop 与精确清理；本地 handoff/ready 未创建，临时本地包/runner 已清理。未改 current/release、数据库、DNS/TLS/Cloudflare、持久服务配置。本轮按失败即停止，不重试；若重签，唯一修正是在 root-only token 文件读取时同时剥离 CR 与 LF，再重新从 Phase-A 前门开始。

- 2026-08-23｜ROS transient corrected-gates fake诊断｜**artifact_written / runtime_pass**：唯一高熵 transient supervisor 以 `RuntimeMaxSec=60`、`TimeoutStopSec=10` 运行；root-only runner 先通过 `bash -n` 与 import 字面量单反斜杠核对。完整 270 文件闭包、只读已部署依赖 symlink 与 lowercase SHA 对账=`MATCH_NORMALIZED`；qimao non-main import exit=`0`、stderr=`NONE`、单行 `IMPORT_OK`。唯一 fake-env helper lifecycle=`listening`，回环 page=`200`、`no-store`、CSP=present；TERM/wait 后 helper exit=`0`、stderr=`NONE`。`systemd-run --wait` exit=`0`，unit 最终 `inactive`（collect 后 Result/ExecMainStatus 不再可独立读取）。trap/外层复核：unit inactive、helper=0、39131 listener=0、临时闭包/依赖 symlink/runner/status=0、root-only stdout/stderr=0、residual=0。未加载真实 S3 env，未执行 Nginx/公网入口/marker/browser/ROS/capability/result/object/multipart，未改 current/release 或服务配置。

- 2026-08-23｜ROS runner精确单文件清理｜**artifact_written / residual=0**：仅使用上一轮内部保存的唯一精确 `/tmp` runner 身份，未重新 `find`、扫描或使用删除 glob。先验证绝对父目录、完整专用 basename 形态；`test -e` 为 present 后，`stat` 门全部通过：普通文件、`root:root`、mode=`0600`、size=`8666`、mtime 仍落在授权 UTC 窗。随后仅用 `rm -- <精确单文件>` 删除，复核为 present→absent。由同一身份派生的此前工作目录=0，helper process=0，`127.0.0.1:39131` listener=0；无 env/systemd/Nginx/marker/browser/ROS/capability/result/object/multipart 动作。临时 runner、工作目录、helper 与监听残留均为 0，本 `UPLOAD-ROS-PROBE-DIRECT-RUN-DIAG-01-SERVER` 租约关闭。

- 2026-08-23｜ROS runner绝对时间窗身份恢复｜**artifact_written**：按授权固定 UTC 窗 `09:12:00Z`–`09:18:00Z`，仅对 `/tmp` 一级目录执行一次无循环 GNU `find -printf`；约束为普通文件、owner=`root`、完整查询模式 `qimao-ros-direct-marker-*.runner`。结果恰好 1 个，精确 basename 与绝对身份已保存于本任务内部；对用户侧仅脱敏尾部=`e506c.runner`。元数据=`root:root`、mode=`0600`、size=`8666`、mtime 落在授权窗口（服务器本地时区显示）；未读取内容、未 hash、未执行或删除。helper/env/systemd/Nginx/marker/browser/ROS/capability/result/object/multipart 动作=0。本轮为身份取证，不改变此前 residual 状态；下一门可仅对该已保存精确身份进行 `test -e` 与精确清理复核。

- 2026-08-23｜ROS runner身份 printf 定向恢复｜**blocked**：本轮要求复用上一轮“同一绝对开始/结束时间窗”，但上一轮实际 runner 查询仅使用相对 `-mmin -30`，未记录或保存可复原的绝对时间戳；当前不能诚实地把它改写为新的绝对窗口，也不得自行扩大或重新定义相对窗口。故 GNU `find -printf` 的单条输出修正未运行，未连接服务器、未读取候选元数据或内容、未保存 basename/绝对身份，更未删除文件。helper/env/systemd/Nginx/marker/browser/ROS/capability/result/object/multipart 动作=0，runner residual 继续为 **unknown**。唯一下一步是由授权方提供上一轮精确绝对时间窗，或明确授权一次新的固定绝对时间窗；在此之前不得查询或清理。

- 2026-08-23｜ROS runner身份只读取证｜**blocked**：从上一轮模板恢复了固定父目录=`/tmp`、完整专用前缀=`qimao-ros-direct-marker-`、后缀=`.runner`、root-owned 普通文件与上一轮附近 30 分钟窗口；随机 UUID 未持久化，故只执行了一次严格受限的 `maxdepth=1` 查询。查询已返回候选计数=`1`，但在候选元数据/脱敏尾部输出循环发生 Bash 解析错误，未得到路径尾部、owner/group、mode、size 或 mtime，亦未将精确身份保存到可续接状态。按单次查询、不得重试/扫描/猜测/删除的边界停止：未读取候选内容、未删除文件、未启动 helper 或任何进程；env/systemd/Nginx/marker/browser/ROS/capability/result/object/multipart 动作=0。runner 身份与 residual 继续为 **unknown**；需新的明确授权才可对同一严格查询协议重做一次只读枚举。

- 2026-08-23｜ROS direct runuser runner精确清理复核｜**blocked**：上一轮外层 bootstrap runner 的唯一绝对路径只存在于一次性 PowerShell 运行期变量，未输出、未持久化至报告或可安全复原的状态文件；当前上下文不能恢复其精确身份。按“不得扫描、不得猜测相似路径、不得删除未知临时文件”边界，本轮未连接服务器，未执行 `test -e` 或删除，故该 runner 的存在性与 residual 仍为 **unknown**。此前已由 runner 本身报告 helper process=0、39131 listener=0、工作目录=0 与依赖 symlink 已清理；本轮未启动 helper、未加载 env，systemd/Nginx/marker/browser/ROS/capability/result/object/multipart 动作=0。仅当能由原执行记录提供该精确路径时，才允许对同一身份执行 `test -e` 与精确删除；否则此租约不能以 residual=0 关闭。

- 2026-08-23｜ROS direct runuser import-marker定向恢复｜**blocked（仅收尾残留未证实）**：root-only 生成 runner 的 import 字面量逐字门通过（`IMPORT_OK` 后为单个 `5C 6E`，非 `5C 5C 6E`），远端 `bash -n` 通过；完整 270 文件闭包与 lowercase SHA 对账=`MATCH_NORMALIZED`。qimao non-main import= `IMPORT_OK`、exit=`0`、stderr=`NONE`；唯一 fake-env main 随后输出 lifecycle=`listening`，回环 page=`200`、`no-store`、CSP 均成立，TERM/wait 后真实 exit=`0`、stderr=`NONE`。未加载真实 S3 env，未执行 systemd/Nginx/marker/browser/ROS/capability/result/object/multipart。runner 自身已报告 helper process=0、39131 listener=0、工作目录=0，临时依赖 symlink 已随工作目录删除；本地临时包/runner 已清理。但外层 bootstrap 在 runner 完成后返回 exit=`127`，且未输出其 root-only runner 文件的最终删除确认；为避免扫描或删除身份不可复原的临时文件，记 residual=unknown 并停止。唯一下一步是用本轮外层 bootstrap 已知的精确 runner 身份完成只读存在性核对及精确删除/复核；不得重新启动 helper 或探针。

- 2026-08-23｜ROS direct runuser import-literal定向恢复｜**blocked**：完整 270 文件闭包、只读依赖 symlink 与 lowercase SHA 比对均通过（`MATCH_NORMALIZED`）；qimao 非 main import 子进程真实 exit=`0`、stderr 类别=`NONE`，但 stdout 未精确等于单行 `IMPORT_OK`，稳定阶段=`IMPORT_GATE_FAILED`。唯一差异仍在 runner 的 `-e` 标记输出：JavaScript 字面量里的换行继续被双重转义，产生了字面量反斜杠 `n` 而非用于单行标记的换行，故严格 marker 核对失败；这不是闭包、依赖、qimao 或 import 运行时失败。为保持 non-main，入口仍作为 `argv[2]` 动态 import，`argv[1]` 为相对哨兵，未触发 direct-main。主 fake helper 未运行，未发起回环 page；未读取真实 S3 env，未执行 systemd/Nginx/marker/browser/ROS/capability/result/object/multipart。清理：依赖 symlink 已随临时根删除，helper process=0、39131 listener=0、服务器与本地临时目录=0、residual=0。若重签，唯一修正是把 import 代码逐字写为单引号内 `await import(process.argv[2]); process.stdout.write("IMPORT_OK\n");`（仅一个反斜杠的 `\n`、双引号前无反斜杠），其余 runner 不变；本轮不重试。

- 2026-08-23｜ROS direct runuser SHA-normalized隔离诊断｜**blocked**：按既有口径仅打包 270 个非 source-map 的完整 `dist` 文件加 `package.json`，并复用已部署依赖的只读临时 symlink。入口 SHA 比较已将 PowerShell 本地期望值与远端 `sha256sum` 实际值均归一化为 lowercase，结果=`MATCH_NORMALIZED`；由此确认上轮 `ENTRY_SHA_MISMATCH` 仅为 PowerShell 大写与 `sha256sum` 小写表示差异，不是内容差异。qimao 的唯一非 main `await import()` 门未获得 `IMPORT_OK`，稳定阶段=`IMPORT_GATE_FAILED`，因此未进入 fake-env main、未启动 helper 或回环 page。该 runner 的 import `-e` 字面量仍把 JavaScript 双引号写成了在 Bash 单引号内无须存在的反斜杠转义；root-only stderr 已随临时目录清理，故其实际 Node 稳定错误码未保留，但该命令构造是唯一已知阻断候选。清理：临时依赖 symlink 曾建立且已随临时根删除，helper process=0、39131 listener=0、服务器与本地临时目录=0、residual=0。未读取真实 S3 env，未执行 systemd/Nginx/marker/browser/ROS/capability/result/object/multipart。若重签，仅允许的下一步是修正 import `-e` 的字面量转义后从同一完整闭包与 SHA-normalized 前门重启；本轮不重试。

- 2026-08-23｜ROS direct runuser隔离诊断｜**blocked**：唯一变化为去除 systemd/transient，以 root shell 直接执行一次 `runuser -u qimao -- env -i` fake-env 运行前门；完整闭包仍为 270 个 `dist` 文件加 `package.json`，入口为绝对普通文件。远端提取后在入口 SHA 前门停止，稳定阶段=`ENTRY_SHA_MISMATCH`：本地 PowerShell SHA 字符串为大写十六进制、远端 `sha256sum` 输出为小写十六进制，而本轮 runner 使用大小写敏感比较，故该结果是哈希表示未规范化导致的前门误阻，**不构成内容不一致证据**。因此未实际调用 `runuser`、未加载 fake env、无 lifecycle、无子进程真实退出码或 stderr 分类可记录；没有启动 helper、没有回环 page 请求。终态由 runner 报告 listener=0、精确临时目录/归档清理完成，依赖 symlink 未创建；本地临时包已在 finally 清理。未执行 systemd/Nginx/真实环境/marker/browser/ROS/capability/result，object/multipart=0、residual=0。下一步若重新获授权，唯一修正是只在 SHA 比较前规范化大小写，再从完整闭包前门重新开始；本轮不重试。

- 2026-08-23｜ROS transient full-closure隔离诊断｜**blocked**：唯一变化为 helper 使用已通过验证的完整运行时闭包（backend `dist` 270 文件、`package.json`、已部署依赖的只读临时 symlink），未退回单入口包；单文件/完整闭包校验通过，依赖两项存在，qimao 绝对 Node 的唯一非 main import exit 0、`IMPORT_OK`，supervisor `bash -n` 通过。main guard 复核：import 命令传入布局为扁平包根的 `dist/tools/ros-browser-probe.js`，但其 `argv[1]` 是相对哨兵而非入口，故 direct-main 后缀 predicate 必然不满足，`import.meta` 与 `pathToFileURL(argv[1])` 也不为同一身份，import 未进 main 已获既有命令证明。相对地，supervisor helper 以绝对普通文件 `dist/tools/ros-browser-probe.js` 直接执行；入口普通文件、非 symlink 已由前门记录，绝对路径必以 `/dist/tools/ros-browser-probe.js` 结尾，因此 direct-main 后缀与身份比较均满足，不能归因为未进 main。helper 子进程退出码、在 cleanup 前是否仍存活及是否被 timeout 均未保存，记为 unknown；仅知未见 lifecycle，最终 helper=0、listener=0，unit 结果=success（诊断脚本收敛失败）。唯一允许的 guard 修正条件已满足，故不指定新的 guard 修正。失败即停，未改闭包/依赖/启动方式重试；清理复核 unit=inactive、status/服务器与本地临时目录/symlink=0。未读真实环境，未改 Nginx，ROS/capability/result/object/multipart/marker=0、residual=0。

- 2026-08-23｜ROS完整运行时闭包import诊断｜**completed**：本地临时包包含完整 backend `dist`（排除 source map/env/log/源码），相对清单=270 文件、聚合 SHA-256=`7A048527138F9AD7E9CC36BE6374A302F294DAF0CE6300EBFA7552088A2110B8`；通过只读临时 symlink 接入已部署依赖闭包，`@aws-sdk/client-s3` 与 `@aws-sdk/s3-request-presigner` 均存在。qimao 使用绝对 Node 对临时入口执行唯一一次非 main `await import()`，exit 0、`IMPORT_OK`；direct-main guard 未触发 helper。复核 helper=0、39131 listener=0，Nginx/ROS/object/multipart/marker 动作=0；服务器与本地临时包/symlink=0、residual=0，未改 current/release/依赖或其他服务器配置。

- 2026-08-23｜ROS transient absolute-node隔离诊断｜**blocked**：相较 env隔离诊断的唯一差异为所有 check 与 helper 均固定使用 `/usr/local/bin/node`，且 `env -i` PATH 固定为 `/usr/local/bin:/usr/bin:/bin`；未改变 fake env、runuser、端口或协议。单文件 SHA 对账、root/qimao 各一次绝对 Node `--check` 与 supervisor `bash -n` 均通过，唯一 transient unit 成功创建；稳定状态仍为 `FAILED_HELPER_LISTENING_UNKNOWN`，首条 allowlist lifecycle 未出现，故未进入 page/TERM 成功阶段，原始 stderr 不保留且首因 unknown。unit 最终结果=success（诊断脚本收敛失败）；清理复核 unit=inactive、helper=0、39131 listener=0、status/服务器与本地临时目录=0。未读真实环境，未改 Nginx，ROS/capability/result/object/multipart/marker=0、residual=0。

- 2026-08-23｜服务器Node绝对路径诊断｜**completed**：root 非登录 shell 与 qimao 非登录 shell 的 `command -v node` 均 exit 0；qimao 登录 shell 因账户不可登录而 exit 1，Node 解析不适用。唯一可输出 `v24.19.0` 的冻结入口为 `/usr/local/bin/node`，其为符号链接，realpath=`/opt/node-v24.19.0-linux-x64/bin/node`，目标为普通文件、owner:group/mode=`qimao-deploy:qimao-deploy:755`。root/qimao 对该 realpath 的 `test -x` 均为 true，且各一次绝对路径 `--version` 均 exit 0、输出 `v24.19.0`；目录链四级 qimao 穿越均为 true（各级 owner/mode 均为 `755`）。后续 fake `env -i` 的最小 Node PATH 应包含 `/usr/local/bin`（冻结最小 shell PATH：`/usr/local/bin:/usr/bin:/bin`）；上一轮 `/usr/bin/node` 失败由此确定为错误二进制路径，而非代码或 qimao 权限。服务器写入=0、残留=0。

- 2026-08-23｜ROS transient helper env隔离诊断｜**blocked**：与上一轮的唯一实现差异原定为 `runuser -u qimao -- env -i` fake-env 启动，不继承环境；但在该前门即停止，未创建 transient unit。单文件 SHA 对账通过；root `node --check` exit 127、qimao 同入口 exit 1，稳定类别为 CLI/path 启动层，故 supervisor `bash -n` 与 runuser helper 均未执行，禁止换路径、降权工具或 Node 版本重试。清理复核：unit/status/helper/listener/服务器与本地临时目录=0；未改 Nginx 或服务器配置，未读取真实环境，ROS/capability/result/object/multipart/marker=0、residual=0。

- 2026-08-23｜ROS transient helper隔离诊断｜**blocked**：同一编译入口 SHA 对账通过，root/qimao 各一次 `node --check` 均 exit 0，supervisor `bash -n` 通过。唯一 transient unit `qimao-ros-probe-diag-cb5fe7278b` 成功创建，最终 unit 结果=success（诊断脚本自行收敛失败）；稳定状态停在 `FAILED_HELPER_LISTENING_CLI_OTHER`，未到 page/TERM 成功阶段，原始 helper stderr 未保留，唯一运行时首因仍为 unknown。全过程仅使用 fake env；未改服务器配置/Nginx，未读取真实环境，未调用 ROS/capability/result，object/multipart/marker=0。清理复核：unit=inactive、helper process=0、39131 listener=0、服务器/本地临时目录=0、稳定状态文件=0、residual=0；未改其他仓库文件或线上配置。

- 2026-08-23｜ROS transient supervisor Phase-A｜**blocked**：编译入口单文件 SHA-256 已对账；root 与 qimao 各一次 `node --check` 均 exit 0、类别=ok，supervisor `bash -n` 通过。`systemd-run` 已成功创建唯一 transient unit `qimao-ros-probe-7281b00b77`（设有 420 秒运行上限与 20 秒停止超时），但短时只读轮询未观察到 remote ready，unit 随后为 inactive；其具体退出类别/退出码未保存，记为 unknown。marker=absent，Edge 未导航；helper 与临时 Nginx location 是否曾在短窗口内建立均未保留，记为 unknown，SERVER 未调用 capability/result，未触发浏览器 PUT 或手工 ROS 操作。最终复核：unit=inactive、location=0、39131 listener=0、服务器/本地临时目录=0，Nginx active；ROS/object/multipart 无已记录动作，残留=0。未改业务发布、数据库、DNS/TLS、Cloudflare、Bucket 属性、防火墙、持久 systemd 或其他仓库文件。

- 2026-08-23｜ROS node-check隔离诊断｜**completed**：服务器 Node=`v24.19.0`。同一已对账入口以 root:qimao `0440` 置于 root:qimao `0750` 目录链，确认普通文件、非符号链接、长度与本地/远端单文件 SHA-256 均一致；root 与 qimao 各一次 `node --check` 均 exit 0、类别=ok、稳定标识=`NODE_CHECK_OK`。此结果排除当前服务器该单文件的语法、路径、读取权限及 qimao 身份差异；此前 runner 的 `NODE_CHECK_GATE_FAILED` 因原始 stderr/退出码未保留，仍无法唯一归因或指定修正。未启动入口、未上传依赖、未读取真实环境；server/helper/Nginx/ROS/object/multipart/marker 动作=0。服务器与本地临时目录均已验证为0，residual=0；未改其他仓库文件或线上配置。

- 2026-08-23｜已验证ready门Edge能力探针｜**blocked**：共享 ready marker 经精确路径复核仍为 absent，故 Edge 未导航、未等待浏览器。首个运行门为临时编译闭包的 `node --check` 未通过，runner 以 root 身份执行该门；原始退出码与 stderr 未保存（条件分支仅输出稳定标识 `NODE_CHECK_GATE_FAILED`），故错误类别只能归为 **CLI/runtime/other**，不能据此判定 JS syntax、MODULE_NOT_FOUND/path 或 EACCES。目标入口在该门前已通过 `-f` 存在性与本地/远端单文件 SHA-256 对账；其是否为普通文件或符号链接、以及 qimao 实际可读性均未单独记录，记为 unknown。完整 runner 的 `bash -n` 已通过；runner 随即停止于该门，未启动 helper、未生成私有 token/provider 身份、未写入 Nginx location、未发起 ROS 操作。唯一技术根因与下一步单一修正均为 unknown，禁止据此猜测或重跑。按协调停止令清理：临时 runner 目录（服务器/本地）=0、临时 Nginx location=0、39131 listener=0，Nginx active；ROS/helper/object/multipart/marker=0、residual=0。未改业务 release/current、数据库、DNS/TLS、Cloudflare、Bucket 属性、防火墙、systemd 或其他仓库文件。

- 2026-08-23｜公网ready差分诊断｜**completed**：在员工回环入口临时加入 exact GET ready 后，回环与公网各一次均为 curl 成功、HTTP 200、`text/plain`、自定义 ready 头=`1`、`Cache-Control=no-store`、15 bytes 且摘要一致、正文精确匹配；回环无 Age/缓存状态，公网无 Age 且为动态缓存状态。由此排除当前 Cloudflared→回环入口链路、该 exact location 与缓存导致的 marker 不匹配；历史 runner 的具体响应未保留，旧失败首因仍不可反推。恢复：Nginx 两次语法检查与 reload 成功，原配置摘要一致；延迟复核回环与公网自定义头均已消失，渲染配置 location=0，临时/备份=0，Nginx/后端/Tunnel 保持 active。未启动 helper/真实环境/浏览器，ROS/helper/object/multipart/marker=0、residual=0；未改业务 release/current、数据库、DNS/TLS、Cloudflare、Bucket 属性或其他仓库文件。

- 2026-08-23｜ROS Bash 修正共享 marker Edge 探针｜**blocked**：完整临时 runner 已先通过远端 `bash -n`，证实条件链与花括号命令组的空白修正生效；运行时 helper 内部 listening/回环 page 与 Nginx 启用步骤通过，但公网 ready 未返回精确标记，runner 按既定失败路径恢复并停止。只读路由追踪确认 Cloudflared 对员工站使用 `http` 回环 `8080`；由 `qimao-loopback` 的 `127.0.0.1:8080`、`server_name _` 接收。该 server block 无 server 级 include，API location 才按需引入 `qimao-proxy-headers`；上轮临时 ready location 写入的正是该 server block，故与 tunnel 目标同层同监听，非配置层错位。历史 ready 响应的 HTTP 状态、Content-Type 与类别仍为 **unknown**：runner 只裁定正文未精确匹配后删除临时响应，且临时 location 关闭 access log；Nginx/cloudflared 无可用历史命中，Age/Cache-Status 亦未保留，不能据此判定缓存或上游类别。共享 marker 未写入，Edge page 未触发，故 capability/ROS/object/multipart 写入均未发生。复核：临时 Nginx location=0、39131 listener=0、服务器与本地临时目录=0、marker=absent；未改业务 release/current、数据库、DNS/TLS、Cloudflare、Bucket 属性、服务或其他仓库文件。

- 2026-08-23｜ROS 共享 ready 标记 Edge 探针｜**blocked**：首因在远端 `bash -s` runner（非本地 PowerShell）解析阶段；退出码=`2`、类别为 Bash syntax error。触发构造为条件链后直接紧接花括号命令组（`&&{…}`），缺少 Bash 要求的命令分隔空白，故在 helper 启动后、Nginx 临时入口写入前停止。只读复核 Nginx 临时 location=0、端口已在清理后为 0，故未形成公网 ready、未写共享协调 marker，也未触发 Edge page。helper TERM 正常，终态 `browserPut=false`、ETag=false，SDK `create/presign/listParts/complete/head/privateRead/delete/abort` 均为 false；ROS request、对象与 multipart=0，耗时 `55366ms`。清理：服务器/本地临时目录=0、marker=absent、residual=0；未改业务 release/current、数据库、DNS/TLS、Cloudflare、Bucket 属性、服务或其他仓库文件。

- 2026-08-23｜ROS Edge 浏览器直传能力探针｜**blocked / ready 通道 unknown**：指定 Edge 的无脚本 ready 标记读取在浏览器控制层超时，故未打开 probe 页面、未进行第二次导航。helper 的内部 `listening` 与回环 page HTTP 200/no-store/CSP、闭包 SHA-256 对账、`node --check`、临时 Nginx 启用测试/reload 通过；终态 `browserPut=false`、ETag=false，SDK `create/presign/listParts/complete/head/privateRead/delete/abort` 均为 false，ROS request=0，16 MiB + 1 KiB 两片未创建，耗时 `68896ms`、吞吐量不适用。恢复 `nginx -t`/reload 通过，ready 标记已消失；TERM 正常、helper/listener/location/服务器与本地临时目录=0，对象/multipart residual=0。未改业务 release/current、数据库、DNS/TLS、Cloudflare、Bucket 属性或服务配置。

- 2026-08-23｜ROS 浏览器直传能力探针｜**blocked / 浏览器通道 unknown**：真实页面 Origin=`https://milaidi.online` 的唯一导航由协调端发出后等待超时，未重试。helper 已记录终态：`browserPut=false`、ETag=false，预检/PUT 未证实；SDK `create/presign/listParts/complete/head/privateRead/delete/abort` 均为 false，故未产生两片（设计尺寸 16 MiB + 1 KiB）、耗时 `110202ms`、吞吐量不适用。编译闭包 SHA-256 对账与 `node --check`、helper `listening`、内部页 HTTP 200/no-store/CSP 通过；临时 Nginx 入口启用及恢复各 `nginx -t`/reload 通过，移除后公开路径不再命中 helper。清理：TERM 正常、helper/listener/location/服务器与本地临时目录均 0；对象/multipart residual=0（创建未发生），无业务 release/current、数据库、DNS/TLS、Cloudflare、Bucket 属性或服务配置残留变更。未继续实施业务路径。

- 2026-08-23｜ROS helper 权限修正复核｜**runtime_pass**：新隔离目录为 `root:qimao 0750`，代码与依赖只读、token/输出文件 root-only；编译闭包本地/远端 SHA-256 对账一致，远端 `node --check` 通过。假 env 与受保护 env 均仅一次启动并输出 `listening`，各自回环探针页均为 HTTP 200、`no-store`、CSP；受保护 env 仅验证文件元数据与必需键存在，未输出值。两次均已 TERM 并等待退出。未访问 `/capability`/`/result`，ROS request=0、对象/multipart=0。清理：服务器隔离目录 1、本地隔离目录 1、helper 0、监听 0；Nginx/location/业务变更 0，残留 0。仅确认运行时，不进入真实能力探针。

- 2026-08-23｜ROS helper 远端运行时诊断 02｜**blocked**：新编译闭包本地/远端 SHA-256 对账一致，远端 `node --check` 通过。假 env 的唯一启动未产生 lifecycle，精确子进程退出码为 `1`；真实受保护 env 未加载。首因：诊断临时根按要求为 `root:root 0700`，而 helper 以 `qimao` 身份运行，无法穿越父目录读取入口，故 Node 主程序未启动，`listening`/`startup_blocked` 均无从输出。未访问 `/capability`/`/result`，ROS request=0、对象/multipart=0。清理：服务器隔离目录 1、本地隔离目录 1、helper 0、监听 0；Nginx/location/业务变更 0，残留 0。

- 2026-08-23｜ROS helper 远端运行时诊断｜**blocked**：编译闭包本地/远端 SHA-256 对账一致；`node --check` 通过，`QIMAO_ROS_PROBE_ENABLE=false` CLI 返回预期 blocked 摘要。唯一固定回环端口的假环境 helper 启动后，限定窗口内未取得可验证的 200/no-store/CSP 页面，运行时仅能归类为 `START_OR_RUNTIME_ERROR`；按单 helper、单端口、不得重试边界停止，未加载真实受保护对象存储环境。未访问 capability、未触发 ROS 网络或对象/multipart 写入。清理：服务器隔离目录 1、本地隔离目录 1、helper 0、端口监听 0；残留 0。未改 Nginx、业务服务、current、数据库或其他仓库文件。

- 2026-08-23｜ROS 浏览器直传能力探针 Rev3｜**blocked**：已编译探针闭包的本地/远端 SHA-256 对账一致，并仅以只读依赖目录在隔离目录中启动；多次启动均未取得可验证的 helper 监听，故真实浏览器 Origin、PUT、ETag 均未执行，耗时/吞吐量不适用。SDK `create`、`presign`、`listParts`、`complete`、`head`、`privateRead`、`delete`、`abort` 均为 false，未触发对象或 multipart 写入。停止后 Nginx 配置测试与 reload 成功，临时 location=0、业务 current/监听/服务保持基线。清理：服务器隔离目录 8、本地隔离目录 1、对象 0、未完成 multipart 0；残留 0。未修改业务 release、数据库、Bucket、DNS/TLS 或其他仓库文件。

- 2026-08-23｜ROS 直传探针 Rev2 审计恢复｜**unknown / blocked**：前轮实际动作仅为两次服务器只读基线核对（服务、监听、运行时可用性与受保护配置元数据），未读取配置值；未生成 UploadId、对象身份、helper 临时目录/PID/端口、Nginx location 或备份。浏览器 Origin 与 SDK `CreateMultipartUpload`、预签名 `UploadPart`、`ListParts`、`CompleteMultipartUpload`、`HeadObject`、`AbortMultipartUpload` 均未执行，私有读边界亦未执行，故能力结论为 unknown。恢复核对中未发现可恢复资源；Nginx 配置测试与无配置变更 reload 成功，业务 current/监听/服务状态保持此前基线。清理：对象 0、未完成 multipart 0、helper 0、临时文件 0、Nginx location/备份 0；残留 0。

- 2026-08-23｜ROS 浏览器直传能力探针｜**blocked**：此前两轮仅进行了本地协议/浏览器能力读取与一次页面连接尝试；未连接服务器、未读取受保护配置、未创建 multipart 或对象。真实浏览器 Origin 的 OPTIONS/PUT 与 ETag 可读门未执行：当前可用浏览器控制面不支持在不部署测试入口、不注入脚本的条件下发起携带短时预签名凭据的跨域 `PUT`，不能以 SDK 或 curl 替代。SDK `CreateMultipartUpload`、预签名 `UploadPart`、`ListParts`、`CompleteMultipartUpload`、`HeadObject`、隔离 `AbortMultipartUpload` 均未运行；私有读边界未运行。清理：对象 0、未完成 multipart 0、临时资源 0；残留 0。未改 Bucket 配置/公私属性、线上服务或仓库其他文件。

## 1. 当前结论

- `UPLOAD-OBJECT-STORAGE-01-SERVER Rev3` 已以 server relay 成功发布：backend/员工静态均切至 `20260822-object-storage-server-relay-v4948-r1`，源码归档本地/远端 SHA-256=`85E971A48AB8BCF18EF7F5720F5D0AEE5E6C57227B7AEF5781990A815B0C1B64`；服务器 pnpm `11.21.0` frozen install、根 build、contracts/backend/S3 adapter/两站 frontend 产物通过。正式 `ProductionS3CompatibleUploadStorage` 以受保护 env 的 `server_relay` 模式完成极小 create→capability→relay part→list→complete→head/get→delete 摘要探针，唯一对象已删除；未调用或重试 ROS CORS。
- systemd 新增可选对象 env 引用并通过 `systemd-analyze verify`；env 仅核验非链接、`root:qimao 0640` 和 `QIMAO_S3_UPLOAD_MODE=server_relay` 键，未读取任何值。发布前确认 schema=`39`、真实 backend 回滚=`20260822-upload-tus-v4945-r1`、员工旧静态=`frontend-upload-tus-v4945-r1`；旧 unit/env 备份保留。管理员静态仍为 `system-frontend-v4880`。
- 切换后 qimao-backend、Nginx、cloudflared、PostgreSQL 均 `active`；loopback/公网首页、`/uploads`、`/health` 为 `200`，未知 API=`401 application/json`、source map=`404`，新员工 asset=`index-Fy5Fj8GD.js`。3001/5432 继续仅回环；未改迁移、业务数据、Worker、DNS、Cloudflare、防火墙或 Bucket 公共访问。历史 ROS `PutBucketCors=501` 保留为已知 provider 限制。
- `UPLOAD-OBJECT-STORAGE-01-SERVER Rev2` 已完成未切换候选构建：r2 release=`20260822-object-storage-v4947-r2`，本地/远端源码归档 SHA-256=`D1A96622A36B4C966E5453FA1C16E1B898F9D6B0372B157059760FE53A8226D7`；服务器 pnpm `11.21.0` frozen install、根 build 与 contracts/backend/两站 frontend 产物均通过。受保护对象存储 env 仅核验为非符号链接、`root:qimao 0640`，未读取值。ROS 精确私有 CORS 写入在 `PutBucketCors` 返回 `NotImplemented`/HTTP `501`，故未放宽 Origin、未创建 multipart 或对象、未切换 current、未重启服务；等待规划取得 ROS CORS 控制面支持/限制的权威口径后再签下一步。此前 r1 候选因部署脚本无法按 pnpm workspace 解析 backend AWS SDK 而未发起 ROS 请求，保留作失败证据。
- 本轮只读发现既有 backend `current` 软链接目标已缺失（v4945-r1 目录不存在），但员工静态 current 当前可解析；qimao-backend、Nginx、cloudflared 与 PostgreSQL 均 active。本任务未触碰这些软链接、服务或回滚状态，backend 回滚目标缺失仍是下一次发布前必须先处置的独立风险。
- `UPLOAD-DIRECT-INGRESS-01-SERVER Phase-C` 在外部 DNS 前置处停止：公开查询 `upload.milaidi.online` 尚无 A 记录；无可用 Cloudflare DNS connector，内置浏览器无法加载控制台且 Chrome/Edge/外部浏览器均不可用。未改 DNS、TLS、443、防火墙、env、current 或员工静态；候选 `20260822-upload-direct-ingress-v4946-r1` 与 v4945-r1 回滚目标均保留，等待用户在 Cloudflare Dashboard 创建 DNS-only A 后续接。
- `UPLOAD-DIRECT-INGRESS-01-SERVER Phase-B` 仅完成 A-C 发布准备里程碑：唯一候选 release=`20260822-upload-direct-ingress-v4946-r1`，本地/远端源码归档 SHA-256=`7D3444C6FE1E163564FF31F2CD175BE6C7AC3F86E16A4F3BFBA3D9FEC8D75539`；服务器 pnpm `11.21.0` frozen install 与根 `pnpm build` 完成，contracts/backend/员工与管理员 frontend 产物存在。current 与员工静态仍为 `20260822-upload-tus-v4945-r1`，管理员根未变；未注入直连 origin、未改 DNS/TLS/443/防火墙/服务或业务数据，等待单独授权的下一阶段。
- 服务器已续费一个月；Debian 12、PostgreSQL、Fastify、Nginx、Cloudflare Tunnel 和国内测试时间定时器保持运行；仅专用项目清理 worker active，其他 Worker 保持停止。
- 员工站 `https://milaidi.online` 已部署站内登录与 14 天安全 Cookie 会话；旧 Basic Auth 文件只作受保护回滚资产，不是当前入口。
- 管理员站 `https://milaidi.top` 由 Cloudflare Access 保护；未登录返回 302，直接绕过 Access 访问管理 API 返回 403。管理员邮箱会话最终 200 仍须由用户本人完成可见登录后复验。
- 项目、任务和基础读取已经走真实 Fastify + PostgreSQL；ASR/OCR 仍是 zero-network fake，Worker 未启动，不能宣称真实识别能力。
- 当前进入治理恢复冻结。`TEST-SRV-20260821-002` 的素材文件夹修复尚未部署且存在跨项目 P0；后端 v4896、管理员静态根与其他系统继续冻结。
- `SERVER-MATERIAL-CANARY-01 Rev4` 已完成唯一获准的员工静态 canary：新不可变静态目录的 SHA256SUMS 对账通过，员工软链接已原子切换；loopback 与 HTTPS 首页/health 均为 200，管理员静态根未变。该 canary 仅供用户测试，不代表完整质量门或生产发布。
- `SERVER-MATERIAL-RECYCLE-CANARY-01 Rev1` 在只读预检阶段停止：现有 `recycled` 项目 6 个，`cleanup_jobs` 的 `scheduled` 队列 6 个；未生成 release、未启动 cleanup worker、未切换 backend 或员工静态根，等待规划裁决。
- `SERVER-MATERIAL-RECYCLE-CANARY-01 Rev3` 在本地 release 敏感扫描阶段停止：纯 `DATABASE_URL` 键名已按裁决允许，但归档仍含真实 PostgreSQL URI；候选已清理，未上传、未切换 backend/员工静态、未启动 cleanup worker，等待规划裁决。
- `SERVER-MATERIAL-RECYCLE-CANARY-01 Rev4` 已通过精确扫描与 SHA 门，但正式 Fastify 重启暴露 workspace contracts 依赖链接与 backend 产物不一致；backend/员工静态已原子回滚到既有 v4896/v4914，旧 health 复验 200。专用 cleanup worker 从未启动，管理员静态、数据库、Nginx 配置与其他 Worker 不变；新 release/员工静态目录保留作失败证据，本轮停止等待规划重签。
- `SERVER-MATERIAL-RECYCLE-CANARY-01 Rev5` 按 FIFO330 标准源码 release 完成 frozen install、contracts/backend/frontend build、精确扫描、普通 Node production import、SHA 与 backend/Nginx health 门；worker unit 启动即正常退出，暴露 current 软链接入口缺少 `--preserve-symlinks-main` 的首因。backend/员工静态已原子回滚到 v4896/v4914，旧 health 与公网/loopback health 复验 200；管理员静态、数据库、Nginx 配置与其他 Worker 不变。新 release/员工静态保留作失败证据，本轮 blocked，不再 Rev6。
- `SERVER-PROJECT-CLEANUP-UNIT-01 Rev1` 已完成可回滚 canary：仅修正专用 unit 的 `--preserve-symlinks-main`，复用保留 release/员工静态；production preflight、SHA、systemd verify、Fastify/Nginx health、due=0 与数据库不变性门通过。backend/员工静态现为 `20260821-material-recycle-v4930`，管理员静态保持 `system-frontend-v4880`；专用 cleanup worker enable+active，连续 5 个轮询窗口未 claim，其他 Worker=0。loopback/公网页面、health 与 JSON API 只读 smoke 通过，系列/三角色文件夹、单槽替换提示与永久删除/清空入口文案可见；旧目标保留可回滚，canary 供用户测试。
- `SERVER-FRONT-UPLOAD-REFRESH-ACTION-01 Rev1` 已完成员工静态 canary：三文件 frontend release `frontend-upload-refresh-v4932` 通过敏感扫描、SHA 与 root-owned 不可变目录门，source map 排除并公网 404；员工链接从 `frontend-material-recycle-v4930` 原子切换，管理员/backend/Worker/数据库不变。loopback/公网首页、`/projects`、`/uploads`、health、未知 API JSON 与安全头基线通过；旧员工目录保留可回滚，canary 供用户继续测试。
- `SERVER-FRONT-UPLOAD-HASH-PERF-01 Rev1` 已完成员工静态 canary：新鲜 frontend 生产产物三文件归档，source map 排除；敏感扫描阻断命中=`0`，远端 SHA256SUMS 对账通过，员工链接从 `frontend-upload-refresh-v4932` 原子切换至 `frontend-upload-hash-perf-v4933`。loopback/公网 `/`、`/projects`、`/uploads` 为 `200 text/html`，`/health` 为 `200 application/json`，未知 API 为 `401 application/json`，map 为 `404`，JS/CSS 响应 SHA 与清单一致；管理员/backend/Worker/数据库、Nginx 配置与端口边界不变，旧目录保留可回滚。
- `UPLOAD-DIRECT-START-01` 已执行迁移与失败回滚：候选 release frozen install/build、SHA/扫描通过；新增迁移使 `schema_migrations` 从 37 到 38，项目/上传核心计数不变。原子切换后 Fastify 在限定健康窗口首检连接拒绝，立即恢复 backend `20260821-material-recycle-v4930` 与员工静态 `frontend-upload-hash-perf-v4933`；管理员静态、Worker、Nginx 配置与端口不变，未做合成上传，候选 release 保留首因证据。
- `UPLOAD-DIRECT-START-01` 已按最终口径完成 canary：backend `20260821-upload-direct-start-v4934` 与员工静态 `frontend-upload-direct-start-v4934` 原子切换；bounded readiness 第 4 轮、`3160ms`，health=200 且 database=connected。旧基线与候选 loopback/公网首页、`/uploads`、health、未知 API JSON、`.map` 404 均未退化；管理员静态、schema=38、Worker/时间 timer/端口边界不变。未做合成上传，候选保持在线供用户 Ctrl+F5 后用现有项目实测，旧 backend/员工静态保留回滚。
- `UPLOAD-TUS-DEPLOY-01` 已完成不可变发布：源归档本地/远端 SHA-256 一致（`969BDCCC59E62FB0F527A6243C42C43E9F61FBAF0EC4735D6E456CAF8C8FB65D`），远端 pnpm `11.21.0` frozen install、根 build 与 contracts/backend/两站产物门通过。backend 原子切至 `20260822-upload-tus-v4942-src`、员工静态切至 `frontend-upload-tus-v4942`，第 4 次健康探测成功；管理员静态根保持不变。schema 为 `39/39`，迁移前后 projects=15、upload_sessions=42、upload_parts=16、assets=16、upload_commands=74，旧会话 `multipart=42` 无丢失。loopback/公网首页和 `/uploads`=200、未知 API=401 JSON、`.map`=404；Tus OPTIONS=204/`Tus-Resumable: 1.0.0`，未认证 POST/HEAD 均被 403/401 JSON 拒绝。完整 authenticated create→POST/PATCH/HEAD→complete 未执行：需要密码、Cookie 或 OTP，按任务边界不读取、不绕过；未上传真实素材或创建测试业务数据。切换前 `v4940` 和更早 `v4934` 均保留回滚。

## 2. 当前运行事实

| 项目 | 当前事实 |
| --- | --- |
| 当前 release | `20260822-upload-tus-v4942-src`（当前 canary；`20260822-upload-tus-v4940-src` 与 `20260821-upload-direct-start-v4934` 保留回滚） |
| 员工静态根 | `frontend-upload-tus-v4942`（当前 canary；`frontend-upload-tus-v4940` 与 `frontend-upload-direct-start-v4934` 保留回滚） |
| 管理员静态根 | `system-frontend-v4880` |
| 数据库 | PostgreSQL 15，仅 `127.0.0.1:5432`，迁移 39/39 |
| 后端 | Fastify 仅 `127.0.0.1:3001`，`/health=200` |
| 公网 | 域名通过 Cloudflare Tunnel；回退端口 18080/18081 保留 |
| 监听边界 | 公网 22/18080/18081；3001/5432/8080/8081 仅回环；80/443 无本机监听 |
| 数据 | 发布前后 projects=15、upload_sessions=42、upload_parts=16、assets=16、upload_commands=74；旧 upload_sessions 均为 `multipart`；只允许匿名测试数据，不上传真实 5.17 GB 素材 |
| Worker | 仅专用 project-cleanup worker active；其他 Worker=0；不得伪造任务完成 |
| 时间基线 | 国内三源 relaxed trial；仅适用于本月国内测试，不等同生产可信时间 |

## 3. 当前冻结状态

| 范围 | 状态 |
| --- | --- |
| 后端 v4896 / 管理员静态根 | 保持现状；本轮未修改 |
| 素材文件夹修复 | `quarantined`；不部署、不继续实现或验收 |
| FRONT/BACK/UI/TEST | 全部冻结，无活动租约 |
| SERVER | `UPLOAD-TUS-DEPLOY-01` 已完成发布；v4942 backend/员工静态在线，v4940/v4934 保留回滚，迁移 39 保留；正向 Tus 真传等待用户可见员工会话 |

执行租约、允许路径和任务身份只以 [`CURRENT.md`](../status/CURRENT.md) 为准。文档本身不会自动启动任务；必须存在明确派发消息和目标任务的新 turn 回执。

## 4. 恢复前置

1. 规划完成工作树文件归属与可信阶段审计。
2. 六个用户可见固定角色任务逐一完成权限探针；只读任务不派发。
3. 用户确认新的恢复基线和是否保留素材在途实现。
4. 规划签发新的 authority generation 与精确租约后，业务协作才恢复。

## 5. 当前部署边界

保持管理员静态根、backend、数据库 schema、DNS/Tunnel/Access、防火墙与其他 Worker 不变。员工静态当前为经 SHA256 验证的 upload-refresh canary；仅既有专用 project-cleanup worker 按原任务保持运行。治理冻结期间不得制作其他 release、再次切换员工 current、上传真实素材、点击永久删除/清空或启动其他 Worker。

## 6. 用户反馈方式

用户继续在“测试服务器”任务里直接描述即可：页面、点击动作、实际结果和可选截图。不要发送密码、Cookie、Authorization、Tunnel token、数据库连接串、真实字幕或视频。

## 7. 权限与接力说明

- 账号、模型或 Codex 宿主变化只触发权限重探针，不重置仓库事实，不重复业务 Rev。
- 固定角色任务若是 `read-only`，工作包直接阻塞；不得用隐藏子代理或内部 successor 代写。
- 规划与文档书记负责 CURRENT/开发日志；业务执行者只写其租约内代码、测试或指定验收文档。
- 不调整用户已经手动固定的模型和推理强度。

最后同步：`UPLOAD-TUS-DEPLOY-01`（v4942 已上线；认证正向 Tus 真传受密码/Cookie/OTP 边界阻止）；本任务未改写 `CURRENT`、开发日志或 Git 历史。
