# 当前项目交接状态

最后更新：2026-08-12

## 当前结论

- 里程碑 1 工程骨架已经形成可回退的本地 Git 基线：提交 `4c90ac5`，标签 `m1-baseline`。
- 里程碑 2 产品范围已经确认：项目中心、大文件上传生命周期和回收站。
- M2.0 产品、架构、状态/API 契约和 UI 基线已落盘，业务代码尚未开始。
- 原本地应用仍是只读参考；没有复制素材、字幕、数据库、凭据、缓存或历史构建物。

## 当前任务看板

| ID | Owner | Status | Goal | Inputs | Deliverables | Next owner / blocker |
| --- | --- | --- | --- | --- | --- | --- |
| PLAN-M2-01 | 规划对话 | `ready` | 完成 M2 一次性技术选型与成本边界 | `PRODUCT.md`、M2 里程碑、两个 ADR | 更新 `ADR-0002` 为已接受；记录唯一技术路径 | 下一角色：开发；涉及购买时必须先交用户确认 |
| UI-M2-01 | UI 对话 | `ready` | 设计项目中心、上传任务、回收站的完整状态 | `DESIGN.md`、M2 里程碑、M2 契约 | 三页预览；加载、空、失败、禁用、恢复和窄屏验收记录 | 下一角色：开发；不得更改接口状态含义 |
| DEV-M1-BASELINE | 开发对话 | `done` | 建立可回退的 M1 Git 基线 | 当前全部 M1/M2.0 文件、仓库检查结果 | 本地提交 `4c90ac5`；标签 `m1-baseline` | 已完成；未创建远程或推送 |
| DEV-M2-01 | 开发对话 | `blocked` | 实现 M2 第一个端到端切片 | 已接受的 `ADR-0002`、UI 交付物、M1 Git 基线 | 项目、上传、回收功能与测试 | 阻塞：等待技术选型与 UI 交付 |

一次只把本角色的一个 `ready` 任务改为 `in_progress`。详细规则见 `docs/WORKFLOW.md`。

## 权威文档读取顺序

1. `docs/WORKFLOW.md`：三对话职责、任务状态和接力规则。
2. `PRODUCT.md`：用户、工作量、MVP、非目标和成功标准。
3. `docs/milestones/M2-upload-lifecycle.md`：当前里程碑范围、流程和验收。
4. `docs/contracts/M2-upload-lifecycle.md`：状态、数据所有权、API 和恢复边界。
5. `DESIGN.md`：UI 信息架构、视觉变量、公共组件和页面状态。
6. `docs/architecture.md`：长期模块边界和数据所有权。
7. 本文件和 `docs/logs/DEVELOPMENT_LOG.md`：最新进展与交接证据。

若对话记忆与这些文件冲突，以用户最新明确要求为最高优先级；否则先更新权威文档，再修改实现。

## 进入业务编码前的门禁

- [ ] 确定前端框架与路由、数据请求和测试方式。
- [ ] 确定后端框架、PostgreSQL 驱动与迁移工具。
- [ ] 确定测试用 S3 兼容对象存储、短时授权方式和生命周期清理能力。
- [ ] 确定本地开发替身以及开发、测试、生产环境配置边界。
- [ ] 根据选择补一份技术选型 ADR，不在多个实现方向之间并行试做。
- [x] 由用户明确授权后，建立里程碑 1 初始提交和标签；本地提交为 `4c90ac5`，标签为 `m1-baseline`；未创建远程。
- [ ] 若启用远程仓库，调整 `tools/verify-repository.mjs` 中“禁止任何远程”的里程碑 1 临时规则。

门禁完成前可以继续产品/UI评审和技术调研，但不应开始数据库、对象存储或大批量页面实现。

## 最近真实验证

2026-08-12 运行 `pnpm check`：

- 仓库边界验证通过。
- JavaScript 语法检查通过。
- 4 项测试通过。
- 后端构建检查与前端静态构建通过。
- 提交前敏感信息扫描未发现私钥、常见云密钥或带口令数据库 URL。

该结果只证明里程碑 1 骨架健康，不代表 M2 上传、回收或并发行为已实现。

## 下一步

1. 进行一次技术选型决策，形成 ADR。
2. UI 对话只设计 M2 三个页面及加载、空、失败、禁用、恢复状态。
3. 技术选型和 UI 交付都完成后，将 `DEV-M2-01` 改为 `ready`。
4. 开发对话再按 M2 实施顺序完成第一个端到端切片。

## 已知限制

- 当前前端只是静态占位页，后端只有健康检查。
- 当前没有数据库、对象存储、上传实现、回收任务、账号、部署和生产安全入口。
- 当前 `check:repo` 会拒绝任何 Git 远程；这是里程碑 1 的临时保护，不是长期策略。
