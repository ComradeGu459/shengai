# 当前项目活跃快照

最后更新：2026-09-01
同步协议：`v6.11`（2026-09-01 精简）
同步版本：`v4-1761`
目标状态：`objective_status=blocked`
连续推进：`continuation=awaiting_user_authorization`
活动切片：`active_slice=SCREEN-TEXT-REVIEW-USER-AUTH-1760`
下一责任：`next_owner=USER`
暂停原因：`pause_reason=platform_explicit_external_write_authorization`
正式 execution lease：`0`（1759在进程启动前被平台拒绝，SERVER不得重发）
持续执行：`product_goal=blocked`

## 1. 当前生产事实

- 上传：用户项目 `102/102` 个物理文件、`5.17 GB`、失败 `0`；私有腾讯 COS multipart 与后台恢复已上线。
- 术语/热词：confirmed TermVersion 可锁定到 ASR；无 confirmed 版本允许无热词运行并明确标记。
- ASR：Worker active；既有批次 `28 completed + 22 localPolicyBlocked`。1727 已上线预算默认只统计/提醒，只有管理员显式启用才阻断。
- OCR：OpenVINO active，screen-text Worker 在队列清空后 inactive；现有结果为 `45 review_pending + 5 failed + 1 reconciliation_required`，queued/running 为0。
- 后续工作流：前置审改与字幕验收真实来源、不可变 Release 和员工页面已上线；尚未用当前真实项目完成最终人工验收。
- 当前 release：backend 为 `admin-summary-20260901-r1`，管理员站为 `system-frontend-admin-summary-20260901-r1`，员工站仍为 `frontend-asr-budget-default-off-20260901-r1`。backend active；ASR/OpenVINO PID未变，screen Worker保持停止；固定发布残留0。

## 2. 当前里程碑

- `OCR-RUNTIME-CONFIG-BACK-1728` 已通过：OpenVINO 抽帧间隔与每集最大帧数写入不可变批次/Attempt 快照及 digest，Worker/extractor 实际消费；并发仍唯一来自 route 容量。
- 冻结范围：`frameIntervalMs=250..10000`（默认1000），`maxFramesPerEpisode=1..600`（默认600）；旧版本保持原默认行为，范围外由后端拒绝。
- 新鲜证据：contracts/backend typecheck 通过；4个专项文件合计62项通过；迁移、两表新列及不可变触发器已在本地 PostgreSQL 实库确认；未执行真实 OCR、素材、Secret 或部署。
- 规划接收曾因超长回合输出截断晚于 BACK 终态，现已通过 `read_thread(includeOutputs=true)` 取得结构化交付并核对精确文件；记为已恢复的 `observer_degraded`，不再阻塞下一跳。
- FRONT 1732 物理回合运行约13分钟后空 `completed`，但目标文件在同一时段写入完整 OCR 调参实现与测试；规划镜像审核新运行 `system-control-plane` 43/43、管理员站 typecheck 和 production build 均通过，候选按事实接收，不为补状态词重跑业务实现。
- 协作根因已修复为产品机制：普通 turn/任务消息不再承担持续调度，当前规划已建立 active `/goal`；只有定时外部观察才另用 heartbeat。
- 1734 发布前门确认 fixed v2.1 无法在切换 backend+管理员站时保持员工指针不变；构建、归档、连接、上传、迁移、重启和数据库写均为0。规划尝试允许内容相同的新员工 immutable 指针被平台外部动作门拒绝，现改走本地 `employeeStatic:preserve` 合同，不绕过平台门。
- 1735 本地 fixed 合同已通过：runner `39799B/b44236b0…c5bf`，validator `22440B/449f3bbc…8fe0`；validator 36 cases、runner syntax/self-test 19 cases与preserve真实symlink fixture通过。v1/旧v2保持replace，preserve归档禁止`static/`且员工stage/next/link/rollback均不变；服务器动作0。
- 1736 唯一发布包已本地冻结：declaration `1111B/6b08358a…137d4`、archive `8967309B/7eee0746…6bec`、migration helper `10002B/83c26bbf…80f5`、SHA闭集 `295B/cdeffbec…0a8c`，另复用1735 fixed两件；archive根仅`backend+system-static`，员工静态成员0，服务器动作0。规划核对六项bytes/SHA、validator 36 cases、helper自测与声明emit；runner在冻结时同一`MSYS=winsymlinks:sys`环境复跑19 cases通过，plain Git-Bash失败仅是Windows symlink语义差异，目标Debian仍须切换前原生自测。
- 1737 已通过：六项一次上传，目标Debian原生runner 19 cases、validator 36 cases与archive/helper门通过；fixed唯一swap、固定入口唯一调用、6037000 gated migration和backend单次restart成功。员工指针逐字未变，ASR/OpenVINO PID未变，screen保持inactive；HTTP 6项200、未认证engines 403/json/nonhtml，发布残留0，Provider/COS/真实OCR调用0。
- 管理员站真实页面已登录并读到OpenVINO v1；新增版本表单真实显示`250..10000ms`与`1..600帧`，规划已填写未激活候选`1200ms/480帧`并核对submit可用。页面自动刷新曾中断观察但没有写入；当前仅等待浏览器动作时确认后创建一次不可变v2并回读，不启动OCR任务。
- 用户动作时确认后，管理员页已且仅创建一次OpenVINO v2：version `2df4aa12-8560-446e-9f51-398ed9b57b38`，`1200ms/480帧`，digest `b0c4e734cd876250…6dfd`；UI同时回读v1/v2。1739只读事务确认v1不可变保留、active screen_text route仍只指向v1，v2创建后batch/attempt均为0，数据库/服务器/Provider/COS写0。OCR管理员调参产品门closed。
- UI 1740 回合空completed但`DESIGN.md` 5.11.2与`SYSTEM-control-shell-acceptance.md`同时间形成完整冻结，规划按文件事实镜像审核并接收：首页只保留单一总状态、解释、唯一下一步、24h处理量和最多3条当前原因，技术事实深链既有详情页。代码审计确认后端当前把`review_pending`计入running，并用24h历史failed/过期租约决定当前overallStatus，因此先由BACK修唯一状态源，不能只改前端文案。
- BACK 1741 回合虽无结构化末条，但精确目标文件已形成可审计实现；规划逐段复核后新运行专项7/7、backend typecheck与diff-check均通过。当前状态现只由当前失败/当前过期执行租约/待对账事实驱动；24h的22个历史`localPolicyBlocked`仍保留为统计但不染红整体，45个`review_pending`不再计入运行/排队/失败。该空交接按文件与新鲜证据镜像收口，不重跑实现。
- FRONT 1742 同样空终态，但`ControlShell.tsx`与`system-control.test.tsx`在回合内形成精确实现；规划镜像审核确认首页仅投影后端overall/freshness，提供单一状态、解释、唯一下一步、24h一行处理量和最多3条业务原因，技术详情保留既有导航。新运行前端12/12、typecheck、production build通过，Impeccable detector为0；不因缺末条重复实现。
- TEST 1743 首轮与最小`CHANNEL_ACK`均为completed、无消息/工具/文件，明确记为`invalid-empty-handoff`与`orchestration_gap`，不记产品失败、也不继续显示active。规划以同一公开验收口径联合新运行后端+前端2文件19/19并通过，target diff-check通过；前序两端typecheck与前端build已通过，发布门以这些可复核证据关闭。
- SERVER 1744 已本地冻结唯一三件套并通过全部本地门，但远端只读前置脚本错误假定`systemctl --value`字段顺序，按“一次失败即停”返回blocked；上传/切换/重启/数据库写均为0。规划逐项复核 declaration `1028B/c9313b55…26b5`、archive `8963703B/76e27747…03bb`、SHA闭集 `202B/c5a5534f…9de1`。1745动作回合在平台直接确认前空结束；规划已导航到SERVER页面，当前只等待用户在该页面确认一次，不重冻、不重复传输。
- SERVER 1745 经动作任务内一次确认后通过：1744冻结三件套一次上传、fixed入口一次调用、backend单次restart；employee指针逐字不变，ASR/OpenVINO PID不变，screen保持inactive，数据库/route/budget/engine/Provider/COS/真实任务写0，残留0。生产overview只读回读为fresh，`1项目/51集、running0、queued3`，整体failed来自当前OCR失败/待对账事实而非历史22条ASR统计。
- 规划随后用内置浏览器真实读取生产管理员首页：首屏只显示“当前处理受阻”、唯一“处理当前问题”、24h `1个项目/51集·正在0·等待3`与3条当前业务原因；未显示四资源池、版本、requestId或reasonCode。OCR v2 `1200ms/480帧`已创建并只读核验，active route仍保持v1，未产生OCR任务。两个产品门均closed。
- 下一产品切片已续接为画面字员工工作台候选审改逻辑与交互重构。复用现有`ScreenTextCandidate/DecisionEvent/Draft/Release`、证据接口和四态合同，不新增状态机或控制面；本切片不切OCR路由、不启动Worker/真实OCR，也不处理当前队列。
- UI 1747 已冻结最小差量：行选择与证据同步、证据读取期间统一写锁、保留/忽略（明确不纳入且可恢复）、显式批量范围及1440/1024三栏。BACK 1747 仅在现有write repository增加未拆分左右父候选服务端门，非法approve/edit返回422且事件/命令/候选/批次零副作用；规划新运行screen-text 28/28、backend typecheck与diff-check均通过。
- FRONT 1748 已形成精确员工审改差量：服务端四态仍为唯一事实，切换候选时证据成功前统一锁写；普通候选使用“保留/忽略”并明确可从已忽略恢复，rejected只允许恢复，未拆分左右父候选无保留入口；批量只提交显式pending范围。固定FRONT回合仍在active但无可见工具/消息，记为`observer_degraded`，规划依据目标文件和新鲜镜像证据接管：ScreenTextWorkspace 15/15、frontend typecheck、production build、target diff-check通过，Impeccable detector为0；不重复实现。
- TEST 1749 再次以completed空终态结束，无消息、工具或文件事实，按硬规则记为`invalid-empty-handoff`与`orchestration_gap`，不记产品失败。规划使用同一冻结口径联合新运行screen-text后端+前端2文件44/44并通过，target diff-check通过；前序两端typecheck、frontend production build和界面扫描证据仍对应同一文件状态，因此候选发布门closed。
- SERVER 1750 在服务器写入前按门停止，服务器连接/上传/部署/数据库/重启均为0：fixed v2.1仅支持employee static preserve，而schema v2强制替换system-static，无法声明“员工站更新、管理员站保持”。规划核对validator/runner与1735成功模式后裁决补齐对称`systemStatic.mode=preserve`；旧v1/v2默认replace必须兼容，禁止退回无管理员声明的schema v1或使用口头约束。
- SERVER 1751 已在既有validator/runner补齐对称`systemStatic.mode=preserve`，管理员preserve路径不建stage/.next/new、不切指针，并在baseline/candidate/switch/rollback/post-switch/cleanup守住同一symlink；旧v1/v2与employee两种mode保持兼容。规划独立复核最终身份：validator `26901B/A3748E2A…BC3DF`、runner `45743B/D86A8907…A7911`，node/bash语法、validator 46 cases、runner 28 cases与diff-check均通过；服务器连接/安装/发布写0。
- SERVER 1752 的typecheck/build均通过，但首次portable命令遗漏既有Runbook要求的`--config.inject-workspace-packages=true`，pnpm在目标物化前返回`ERR_PNPM_DEPLOY_NONINJECTED_WORKSPACE`；临时stage精确清理，declaration/archive/SHA未生成，服务器动作0。规划对照仓库03J/122A成功证据与pnpm 11官方deploy合同后确认唯一修正为命令行临时启用injected并保持`--offline`/non-legacy，不修改workspace配置、不用legacy、不重跑已通过构建。
- SERVER 1753 已使用官方non-legacy injected offline命令物化portable backend（170/170本地复用）并冻结唯一五件套；规划独立复核：runner `45743B/d86a8907…a7911`、validator `26901B/a3748e2a…bc3df`、declaration `927B/ba64dddc…e3738`、archive `9328201B/c47c9fd3…e5fbc`、SHA `207B/1969685d…f447a`。actual declaration validate/emit、SHA files=2、archive roots=`backend,static`、system-static=0均通过；system old/next均锁定现有管理员目标，migration none。服务器动作0。
- SERVER 1754 的SSH身份只读检查返回`qimao-deploy`，随后PowerShell双引号提前展开远端`$()`，使发布前只读基线失真；按首因停止。remote connection=2但SCP/目录创建/fixed安装/inbound/入口调用/重启/数据库写均为0，残留0，1753五件套未消费。规划复用1737已验证的无插值stdin脚本边界：UTF-8无BOM/LF原始字节送入远端`bash -s`，不再让远端脚本进入PowerShell插值。
- SERVER 1755 已证明无插值stdin脚本UTF-8无BOM/LF/CR=0且bash语法通过，但误用Git Bash SSH，其独立known_hosts上下文没有目标host key，严格校验在远端脚本执行前停止；上传/目录/fixed/inbound/入口/重启/数据库写均为0、残留0。规划冻结为Windows OpenSSH安全上下文：显式项目config、绝对IdentityFile与UserKnownHostsFile，StrictHostKeyChecking/BatchMode/IdentitiesOnly保持yes；禁止读取/修改host key、禁止Git Bash SSH或放宽校验。
- SERVER 1756 使用Windows OpenSSH但把`-o UserKnownHostsFile=...`错误拼成单一ArgumentList token，OpenSSH在接收stdin前以255退出；remote connection/upload/install/entry/restart/DB均为0、残留0。规划删除多余覆盖：1754已由最小`ssh.exe -F <project-config> qimao-test-server id -un`实证返回qimao-deploy，project config本身已声明key、BatchMode、IdentitiesOnly与StrictHostKeyChecking；1757仅以逐token`-F/config/alias/bash/-s/--`承载raw stdin，SCP同样只复用`-F`。
- SERVER 1757 已以最小Windows OpenSSH+raw stdin成功读取真实基线：current backend=`admin-summary`、employee=`budget-default-off`、system=`admin-summary`、五项页面均200；但手写preflight把未认证API误写为`127.0.0.1:8081/overview`，断言前停止，SCP/远端目录/fixed/inbound/入口/服务/DB写均为0、残留0。规划删除重复探针：fixed会从declaration emit唯一读取`probe_path=/api/system-control/overview`并请求backend `127.0.0.1:3001`，负责403/json/nonhtml与失败回滚；preflight仅保留指针/unit/页面基线。
- SERVER 1758 已通过Windows OpenSSH/raw stdin再次确认三指针、unit/PID与五项页面200，但qimao-deploy直接重定向root-owned `/run/lock/qimao-application-release.lock`权限拒绝；SCP/目录/fixed/inbound/入口/服务/DB写均为0、残留0。规划冻结唯一正确锁门为`sudo -n /usr/bin/flock -n <固定锁> -c /usr/bin/true`，不删除/改写锁；fixed自身仍用同一锁并在占用时安全停止。
- SERVER 1759 在任何进程启动前被平台安全审批拒绝：此前泛化“允许”不构成对本次SCP、root fixed安装、backend切换/重启与失败回滚的具体授权。SSH/SCP/远端写/fixed安装/入口/服务/DB均为0，临时脚本已删除，1753五件套完整保留。当前只等待用户在已打开的SERVER固定任务直接确认这组精确副作用；确认前不再派发或重试。
- 相同平台授权缺失已连续三个goal回合复核：SERVER始终idle、无新用户回合或外部状态变化。按持续目标硬规则，1761正式将本产品目标标为blocked；恢复条件仅为用户在SERVER固定任务直接发送1760所列精确授权，恢复后继续消费同一1753五件套，不重冻、不重测、不重传。

## 3. 唯一推进顺序

1. 用户在“测试服务器｜长期固定”任务直接确认：消费1753五件套、一次SCP、原子安装fixed、发布screen-text-review并允许backend切换/重启，失败自动回滚清理。
2. SERVER仅在该精确确认后恢复1759；发布通过后由规划真实员工页只读验收，不创建OCR任务。

停止条件：出现真实 external unknown、数据破坏、重复付费/写入或需要第二状态源时停止；普通编译/夹具问题留在同一 Owner 定向修复。

## 4. 固定角色

| 角色 | 固定任务 ID | 状态 |
| --- | --- | --- |
| 规划 | `01a02d47-a546-7002-a677-eedf51596d08` | blocked goal；等待一次精确平台授权 |
| BACK | `01a05bc7-a710-7890-bccc-ccc910ead05d` | idle；1747由规划镜像验收 passed |
| FRONT | `01a05a6b-ee66-77e1-bf7a-6cd78504fd4c` | observer_degraded；1748由规划镜像验收 passed，禁止继续修改 |
| UI | `01a02392-4d7b-7df1-8fa9-f16e539f2102` | idle；1747文件事实镜像passed |
| TEST | `01a023b6-e562-7e61-8302-f6b1f2f7d136` | invalid-empty-handoff；规划联合44/44替代验收passed |
| SERVER | `01a05a90-07bd-7571-bf47-5873425e7319` | blocked；1759平台授权门前写0，等待用户直接精确确认 |

## 5. 协作约束

- `wait_threads` 只等待；任何 ACK/中断/归档/换代前必须 `read_thread(includeOutputs=true)` 加精确文件/命令/外部事实复核。
- 默认最多2个执行角色；任务交付只需 `outcome / evidence / remaining_gap / next_owner / files / residual / requires_user`。
- 平台要求动作任务内确认时，只导航用户一次；规划不代转授权、不重复发起同一传输。
- 保留脏树；禁止 reset、checkout、clean、stash、commit、push、批量覆盖或新 worktree。
